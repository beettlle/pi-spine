### Verdict: PASS

### Summary

SP-374 delivery is complete and satisfies GitHub #56. `collectStaleFileScopeMustChangeWarnings` in `validate-prompt.mjs` compares `fileScopeMustChange` paths against changes on `main` since the task PROMPT first landed; warnings (not hard failures) surface via `spine tasks validate --warnings-only` and the `prelanded-file-scope` preflight check, with amendment guidance in the message text. Operator runbook § Contract documents the warning and recovery workflow, issue #56 is closed, `.DONE` is present, and machine contract verification passes (testCommand, `fileScopeMustChange` on `spine-preflight-lib.mjs`, 88.28% line coverage ≥ 77%).

```json
{"verdict":"PASS","feedback":"All completion criteria met. Required behaviors: (1) advisory warning when pending tasks have fileScopeMustChange paths already changed on main since PROMPT intro, (2) wired into validate --warnings-only and preflight/plan via checkPrelandedFileScopeWarn, (3) amendment workflow suggested in STALE_FILE_SCOPE_AMENDMENT_HINT and runbook recovery steps. Contract: testCommand passed, fileScopeMustChange matched src/config/spine-preflight-lib.mjs, minLineCoverage 88.28% >= 77%. Changes stay within file scope (validate-prompt.mjs, spine-preflight-lib.mjs, bin/spine-tasks.mjs, two test files, operator-runbook.md). Issue #56 closed (2026-06-30) and .DONE present."}
```
