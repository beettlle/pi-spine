### Verdict: PASS

### Summary

SP-503 successfully extracts discovery and tasks-validate logic into `src/config/preflight/discovery.mjs` (332 LOC, under the 500 LOC cap) and thins `spine-preflight-lib.mjs` via re-exports that preserve the public API. Contract verification passes: targeted preflight tests (23/23), file-scope changes on both required paths, and line coverage 88.65% (≥77%). Independent checks confirm `npm run typecheck` and `env -u SPINE_IS_WORKER npm run coverage:check` (88.49%) succeed.

### Completion criteria

| Criterion | Status |
|-----------|--------|
| `discovery.mjs` exists, ≤500 LOC | ✅ 332 LOC |
| Required symbols moved (`resolveTasksRoot`, `discoverTaskFolders`, `discoverTaskIds`, `taskIdFromFolder`, `checkTasksRoot`, `checkDependenciesJson`, `checkWorktreeSetupHook`, `checkTasksValidate`) | ✅ |
| Public API unchanged via re-exports from `spine-preflight-lib.mjs` | ✅ |
| Contract `testCommand` | ✅ 23/23 pass |
| Contract `fileScopeMustChange` | ✅ both paths changed |
| Contract `minLineCoverage` ≥77% | ✅ 88.65% (machine), 88.49% (reviewer) |
| Scope respected (no git-clean/batch/integrate extraction) | ✅ |
| `.DONE` marker | ✅ present |

### Notes (non-blocking)

- `readUtf8FilesBatchSync` is exported from `discovery.mjs` for internal use by `listPrelandedFileScopeStaleTasks` in `spine-preflight-lib.mjs` but not re-exported publicly; documented in STATUS and acceptable for SP-504/505 scope.
- Prior contract-fail log (`contract-fail-2026-07-06T19-51-46-582Z.log`) reflects `npm test` running the full suite under worker context (171 cancelled tests), not a regression in SP-503 preflight tests.

```json
{"verdict":"PASS","feedback":"Strangler Fig extraction complete: discovery module at 332 LOC, spine-preflight-lib re-exports preserve public API, contract and coverage gates pass. Task ready to merge."}
```
