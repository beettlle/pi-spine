### Verdict: PASS

### Summary

SP-521 delivers scoped `testCommand` contract warnings for Size S/M patch tasks: `collectTestCommandScopeWarnings` flags `coverage:check` and `npm test` chains, wired through `validateContract` with `taskSize` from `validate-prompt.mjs`. SP-516's contract is fixed to scoped `node --test` only, and `create-spine-tasks/SKILL.md` documents the anti-pattern. Contract `testCommand` passes (12/12), machine verifier passes, `.DONE` exists, and issue #141 has a Partial-close comment.

Integration edits in `validate-contract.mjs` and `validate-prompt.mjs` are necessary wiring within reasonable blast radius and do not block merge.

```json
{"verdict":"PASS","feedback":"All completion criteria met. Validator warns on S/M coverage:check and npm test; L-size contracts remain quiet; SP-516 uses scoped testCommand; skill anti-pattern row added; tests and spine tasks validate SP-516 pass. Delivery artifacts (.DONE, #141 Partial comment) confirmed."}
```
