## Plan Review: spine rules commands

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` and the existing `spine deps` / `spine settings` CLI split is sound: put testable command logic in `src/cli/rules.mjs` (`runRulesDiscover`, `runRulesSelect`, `runRulesSync`, `runSpineRules`, `printRulesHelp`), expose a thin `bin/spine-rules.mjs` entry, and route `spine rules` from `bin/spine.mjs`. `discover` and `sync` should call SP-090 `discoverCursorRules` after `loadRulesProfile` and write `.spine/rules-manifest.json`; `select --task` should resolve PROMPT File Scope via `discoverTasks` + `parsePrompt`, require a committed manifest, then call SP-091 `selectRulesForWorker` with `config.standards` append and `neverLoad` blocklist. Scope correctly defers init profile copy, `standards: []` default, doctor checks, and CLI tests to Steps 2–3 and avoids worker injection (SP-092).

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| `discover [--json]` | `loadRulesProfile` → `discoverCursorRules({ writeManifest: true })`; human summary or full JSON payload; profile/manifest errors exit 1 with structured `--json` |
| `select --task <id> [--json]` | `loadSpineConfig` → `resolveTasksRootPath` → task packet `fileScope` → `loadRulesManifest` (fail with `spine rules sync` hint) → `selectRulesForWorker` with append semantics for `standards[]` |
| `sync [--json]` | Re-run discovery and atomically refresh committed manifest (same write path as `discover`; distinct subcommand for operator/docs clarity) |
| Router wiring | `cmdRules` in `bin/spine.mjs` + help/examples listing the three subcommands |
| Dependencies | Consumes SP-091 selection API and SP-090 manifest paths/constants; no changes to `select.mjs` / worker context |
| Do NOT | No `buildWorkerContextAsync` / journal injection (SP-092) |
| Step boundaries | Init (`templates/rules-profile.json`, `standards: []`), doctor (`RULES_MANIFEST_MISSING` / `STALE`), and `tests/cli/spine-rules.test.mjs` remain Step 2–3 per PROMPT |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-093-cursor-rules-cli-init/PROMPT.md` (File Scope)** — Add `src/cli/rules.mjs` explicitly (TP-033/034 precedent) so the `src/cli/` + `bin/` split is not mistaken for scope creep.
2. **`spine-tasks/SP-093-cursor-rules-cli-init/STATUS.md`** — Record a short Step 1 plan blurb before marking the step complete; at Review Level 1, keep Step 1 **In Progress** until this plan review returns APPROVE, then commit Step 1 artifacts only.
3. **Step 2 hygiene** — `src/doctor/rules-manifest.mjs` appears started early; keep it out of the Step 1 commit and wire `buildRulesManifestDoctorCheck` into `bin/spine-doctor.mjs` in Step 2 only.
4. **Step 3 tests** — Cover discover/sync write, select happy path + missing manifest/profile/task, and `--json` error shapes; add a router smoke case in `tests/cli/spine-router.test.mjs` if not already present.
5. **Imports** — Prefer `src/config/cursor-rules/index.mjs` for `selectRulesForWorker` / discover exports (optional cleanup).

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan matches PROMPT outcomes and established CLI patterns: src/cli/rules.mjs core, bin/spine-rules.mjs entry, spine.mjs router, discover/sync via discoverCursorRules, select via task fileScope + selectRulesForWorker. Defer init, doctor, and tests to later steps; do not bundle early doctor work in Step 1."
}
```
