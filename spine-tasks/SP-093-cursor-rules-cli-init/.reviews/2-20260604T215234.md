## Plan Review: Init + doctor

### Verdict: APPROVE

### Summary

The Step 2 plan implied by `PROMPT.md`, SP-090/091 artifacts, and the TP-043 doctor-module pattern is sound: extend `bin/spine-init.mjs` to copy `templates/rules-profile.json`, run `discoverCursorRules({ writeManifest: true })` via `runInitRulesDiscover`, and stop merging `DEFAULT_SPINE_INIT_STANDARDS` so greenfield init keeps `standards: []` for auto-discovery. Extract `runDoctorChecks` / `cmdDoctor` into `bin/spine-doctor.mjs` (listed in File Scope), add `src/doctor/rules-manifest.mjs` with advisory `RULES_MANIFEST_MISSING` / `RULES_MANIFEST_STALE` warnings and `spine rules sync` remediation, and gate the check when `.cursor/rules` or `.spine/rules-profile.json` exists. Confirm `.spine/rules-manifest.json` stays out of `SPINE_GITIGNORE_ENTRIES`.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Init copies profile + discover | `loadRulesProfileTemplate()` → write `.spine/rules-profile.json`; `runInitRulesDiscover()` after scaffold/gitignore; dry-run skips write but plans manifest when `.cursor/rules` exists |
| `standards: []` default | Template already `[]`; `applySpineInitDefaults` removes SP-073 `DEFAULT_SPINE_INIT_STANDARDS` merge — selection append stays in SP-091/`spine rules select` |
| Doctor missing/stale manifest | `buildRulesManifestDoctorCheck` in `src/doctor/rules-manifest.mjs`; fingerprint rescan (ignores `generatedAt`) for stale; `checks.push` advisory (`ok: true`, `warning: true`) with codes `RULES_MANIFEST_MISSING` / `RULES_MANIFEST_STALE` |
| Manifest not gitignored | `SPINE_GITIGNORE_ENTRIES` unchanged; assert in `tests/cli/spine-init-rules.test.mjs` |
| Step boundaries | `tests/cli/spine-rules.test.mjs` and broader CLI/coverage gate remain Step 3; no worker injection (SP-092) |
| Dependencies | Reuses SP-090 discover/write paths and SP-091 profile constants; no changes to `select.mjs` |

### Suggestions (non-blocking)

1. **Step 2 commit boundary** — Land init + doctor module/wiring + `tests/cli/spine-init-rules.test.mjs` and the collateral `tests/config/worker-context.test.mjs` standards assertion. Keep `tests/cli/spine-rules.test.mjs` and `tests/doctor/rules-manifest.test.mjs` for Step 3 unless you need them green before the Step 2 commit.
2. **Incidental manifest regen** — Omit `.spine/rules-manifest.json` from the Step 2 commit if the diff is only a `generatedAt` bump from local init/discover (no rule inventory change).
3. **`bin/spine.mjs` re-export** — Preserve `export { runDoctorChecks } from "./spine-doctor.mjs"` so existing doctor tests importing from `bin/spine.mjs` stay stable.
4. **STATUS closure** — Record this review under Step 2 in `STATUS.md` before marking the step complete; at Review Level 1, keep Step 2 **In Progress** until after the Step 2 commit.
5. **Empty rules tree** — Init may write an empty manifest when `.cursor/rules` is absent; acceptable baseline for operators to commit and refresh later via `spine rules sync`.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 2 plan: init copies rules-profile and runs discover with standards:[] default; extract spine-doctor.mjs and wire fingerprint-based RULES_MANIFEST_MISSING/STALE advisory checks; keep manifest out of gitignore. Defer spine-rules CLI tests and doctor unit tests to Step 3; exclude generatedAt-only manifest churn from Step 2 commit."
}
```
