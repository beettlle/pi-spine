### Verdict: PASS

### Summary
The task successfully removes fake `async` declarations from the targeted batch functions (`mergeWaveLanesToOrch`, `skipTaskDoneOnDisk`, and `spawnReviewerPi`). An architecture guard test was added to prevent future regression in these batch files, and all contract tests pass successfully.

```json
{"verdict":"PASS","feedback":"All target functions were correctly de-asynced, the arch guard accurately validates the files, and contract tests pass."}
```