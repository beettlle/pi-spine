## Plan Review: Fix and test

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` and `STATUS.md` is sound: replace the broken `` `\${ch}` `` template literal in `escapeRegexChar` with `'\\' + ch`, add a focused regression file under `tests/planner/scope-glob-escape.test.mjs`, and leave `globToRegExp` / lane packing untouched. Using synthetic `discoveredTasks` with nested `folderPath` values is the correct workaround for `discoverTasks` scanning only top-level folders, and the four planned cases cover the audit's dotted/parenthesized paths plus a `TP-*` glob regression via real discovery.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Fix `escapeRegexChar` | One-line change in `src/planner/scope.mjs` — `'\\' + ch` for meta characters in `/[\^$.*+?()[\]{}|]/` |
| `src/foo.bar/**` | Synthetic task under `src/foo.bar/TP-101`; negative control `src/other/TP-104` |
| `docs/(api)/**` | Synthetic task under `docs/(api)/TP-102` |
| `TP-*` unchanged | Existing glob path via `discoverTasks` + `${basename(tasksRoot)}/TP-003-*` |
| File Scope | Only `scope.mjs` and new test file — no planner algorithm changes |
| Do NOT | No lane packing or `globToRegExp` semantics beyond the escape fix |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-110-glob-scope-regex-escape/STATUS.md`** — Record this plan review artifact (`.reviews/1-20260605T200905.md`) under Step 1 before marking the step fully closed; keep Step 2 scoped to full suite + coverage gate.
2. **Plan-before-implementation ordering** — Commit `eaf5430` landed the fix and tests before this plan review. Going forward, Step 1 commit should follow plan APPROVE; if already implemented, the landed diff is acceptable and limited to File Scope.
3. **Step 2** — Run full `npm test` and `coverage:check` as the formal verification pass; no additional escape cases required unless coverage flags `scope.mjs`.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan: one-line escapeRegexChar fix ('\\\\' + ch), new tests/planner/scope-glob-escape.test.mjs with synthetic nested discoveredTasks for dots/parens/plus and discoverTasks-backed TP-* regression; no globToRegExp or lane packing changes. Record plan review in STATUS; proceed to Step 2 full suite + coverage gate."
}
```
