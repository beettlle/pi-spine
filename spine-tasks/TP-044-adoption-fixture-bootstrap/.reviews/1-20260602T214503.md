## Plan Review: Fixture layout

### Verdict: APPROVE

### Summary

Step 1 correctly scopes a minimal consumer fixture under `tests/fixtures/adoption-repo/` plus an intro-only section in `docs/adoption/bootstrap-checklist.md`, matching PROMPT checkboxes without pulling Step 2 full checklists or Step 3 integration assets into this boundary. Deferring checked-in spine config and creating `.spine/spine-config.json` via `spine init` on first use is the right product choice (PROMPT's `.pi/spine-config.json` is obsolete; canonical path is `.spine/spine-config.json`). AD-001 uses Review Level 0, discoverable folder `AD-001-smoke`, and `dependencies.json` keyed `AD-001`, aligned with `discoverTasks` / `TASK_FOLDER_RE`.

### Issues Found

None blocking for Step 1 plan approval.

### Missing Items

- **Completion marker alignment** — AD-001 `PROMPT.md` scopes `DONE.txt`, while TP-044 Step 3 and spine batch semantics use **`.DONE`** (stub worker in `bin/spine-worker-runner.mjs` writes `.DONE` only). Stub CI will still pass, but fix AD-001 before Step 3 so the fixture documents the real contract (e.g. single step: create `.DONE`, optional `DONE.txt` only if you want a visible artifact).
- **PROMPT vs mission wording** — Top-level PROMPT says "touch `DONE.txt`"; integration deliverable says "assert `.DONE`". Treat `.DONE` as authoritative in AD-001 and checklist copy.
- **Forward references** — `bootstrap-checklist.md` cites `./scripts/adoption-smoke.sh` and `tests/adoption/fixture-batch.test.mjs`, which Step 3 owns. Acceptable in intro if Step 3 lands in the same task; otherwise add a one-line "available after Step 3" guard so operators are not blocked mid-task.
- **`real-project-readiness.md`** — PROMPT Must Update is task-level; not required in Step 1 commit, but keep an explicit Step 4 / completion bullet so the adoption-plan link is not dropped.

### Suggestions

- **Git in tests, not in fixture tree** — "Tiny git repo layout" is satisfied by copying the fixture into a temp dir and calling `initGitRepo` (per `integration-abc.test.mjs` / `tests/helpers/git-fixture.mjs`); do not commit a `.git` directory under `tests/fixtures/`.
- **README** — Fixture `README.md` is helpful; keep it the single operator-facing layout doc until Step 2 expands the checklist.
- **Step boundary** — Step 1 commit should remain fixture + checklist **intro** only; reserve greenfield/migrate sections, `adoption-smoke.sh`, and `fixture-batch.test.mjs` for Steps 2-3 per File Scope.
- **Batch scope string** — Step 3 should start batch with `AD-001` (discovered id), not the folder slug `AD-001-smoke`.
- **Init preset** — Document or test `spine init --preset taskplane-compat` (or project default) when copying fixture so `taskplane-tasks` root matches `dependencies.json`.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "TP-044",
  "feedback": "Step 1 plan delivers minimal adoption-repo fixture (AD-001-smoke, dependencies.json, README) and bootstrap-checklist intro without Step 2/3 scope creep; deferring .spine config to init is correct. Before Step 3, align AD-001 with .DONE (not DONE.txt alone), clarify forward refs to smoke script/test, and plan real-project-readiness update at task completion. Proceed with Step 2 checklist body and Step 3 integration."
}
```
