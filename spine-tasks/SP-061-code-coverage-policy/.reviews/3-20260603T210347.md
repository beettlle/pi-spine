# Code Review — SP-061 Step 3: Agent + skill policy text

**Reviewer:** Code review agent  
**Step:** 3 — Agent + skill policy text  
**Worktree:** `lane-1` (`task/spine-lane-1-20260603T204004`)  
**Date:** 2026-06-03

## Scope

Step 3 per `PROMPT.md`: document the **≥77% line coverage** gate in worker/reviewer agent templates and in the `create-spine-tasks` skill + prompt template so workers, reviewers, and task authors treat coverage as a completion gate.

**Commit reviewed:** `03418b0` — `feat(SP-061): complete Step 3 — agent and skill coverage policy text`

**Changed files (4):**

| File | Change |
|------|--------|
| `templates/agents/worker.md` | New "Code coverage (code-related tasks)" standing-order section |
| `templates/agents/reviewer.md` | Code-review bullet: verify coverage; REVISE when below threshold |
| `skills/create-spine-tasks/SKILL.md` | Config table, Step 4 guidance, Key Principles |
| `skills/create-spine-tasks/references/prompt-template.md` | Testing-step and STATUS coverage checkboxes |

## Summary

Step 3 delivers the policy text required by `PROMPT.md` with no scope creep into CI, npm scripts, spine-config, or PRD (correctly deferred to Steps 1–2 and 4). Worker and reviewer templates consistently reference **≥77% line coverage**, in-scope modules (`src/`, `bin/`, `extensions/`, or consumer equivalent), and `testing.testWithCoverage` (pi-spine default `npm run coverage:check`). The create-spine-tasks skill and prompt template wire the coverage gate into task authoring and verification steps, aligned with Step 2's `templates/spine-config.json` default.

## Step 3 Checklist (PROMPT.md)

| Requirement | Status |
|-------------|--------|
| Worker: maintain ≥77% on changed/in-scope modules | Done — dedicated section with scope, threshold, and `.DONE` gate |
| Worker: run `testing.testWithCoverage` before `.DONE` | Done — references `.spine/spine-config.json`; fallback when command empty |
| Reviewer: verify coverage on code reviews; REVISE if missing | Done — explicit REVISE trigger for sub-threshold or untested changed paths |
| Skill: Testing step references coverage gate | Done — Step 4 bullet + Key Principles |
| Prompt template: coverage verification checkbox for code deliverables | Done — PROMPT Testing step + STATUS mirror with "(code-related tasks only)" qualifier |

## Strengths

- **Consistent vocabulary** — All four files use the same threshold (≥77% line coverage), command (`testing.testWithCoverage` / `npm run coverage:check`), and in-scope paths established in Steps 1–2.
- **Consumer-project aware** — Worker text allows empty `testWithCoverage` with PROMPT.md fallback; prompt template qualifies coverage as code-related only.
- **Threshold guardrail** — Worker section repeats PROMPT "Do NOT lower threshold without operator approval."
- **Minimal diff** — 17 insertions across exactly the Step 3 artifact files; no unrelated agent prompt expansion.

## Findings

### Minor (non-blocking)

1. **Reviewer bullet placement** — The new coverage bullet sits as a lone list item after a blank line under "Code review" rather than as a sub-bullet under the existing bullets. Readable and enforceable; optional polish to nest under the code-review list for structure consistency.
2. **Worker checkpoint list vs coverage section** — Checkpoint step 5 still cites `./scripts/worker-verify.sh` / typecheck+test only; coverage is covered in the adjacent section before `.DONE`. Consider cross-linking in Step 4 if `worker-verify.sh` gains a coverage hook.
3. **Prompt template Completion Criteria** — Testing step and STATUS include coverage; the "Completion Criteria" block still lists only "All tests passing." Out of Step 3 scope; optional follow-up when Step 4 updates PRD/normative docs.

## Recommendation

**Approve Step 3.** Policy text is complete, consistent with prior steps, and scoped correctly. Proceed to Step 4 (PRD + full verification).

### Verdict: APPROVE

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "SP-061",
  "feedback": "Step 3 commit 03418b0 adds ≥77% line coverage standing orders to worker.md and reviewer.md, and wires testing.testWithCoverage into create-spine-tasks SKILL.md and prompt-template.md Testing/STATUS checkboxes. Aligns with Step 2 spine-config default (npm run coverage:check). Scope limited to four artifact files; no CI/PRD drift. Minor formatting/consistency nits only. Proceed to Step 4 PRD and verification."
}
```
