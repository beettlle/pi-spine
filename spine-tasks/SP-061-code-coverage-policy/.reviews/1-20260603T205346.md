## Plan Review: Step 1 — Coverage tooling + npm scripts

### Verdict: APPROVE

### Summary

Step 1 correctly delivers npm coverage tooling without pulling Step 2 CI wiring, spine-config defaults, agent template policy text, or PRD edits into this boundary. The plan uses Node's native `--experimental-test-coverage` with `--test-coverage-include` globs (no `c8` dependency), which fits the repo's Node ≥22 engine, existing `node --test` `.test.mjs` layout, and `--experimental-strip-types` usage. A three-module split — `scripts/coverage-policy.mjs` (single source of truth for `COVERAGE_THRESHOLD=77`, in-scope includes, and test globs), `scripts/coverage-parse.mjs` (aggregate line-percent parser), and `scripts/run-coverage.mjs` (spawn test run, parse summary, enforce threshold) — satisfies PROMPT checkboxes for `test:coverage`, `coverage:check`, and a documented threshold constant. In-scope coverage is limited to `src/`, `bin/`, and `extensions/` as required. `tests/coverage/policy.test.mjs` wires threshold constants, include globs, parser behavior, and a drift guard that `TEST_GLOBS` stay aligned with `package.json`'s `test` script.

### Issues Found

None blocking for Step 1 plan approval.

### Missing Items

- **Step 1 commit boundary** — Keep Step 1 scoped to `package.json` (scripts + `tests/coverage` glob in `test`), `scripts/coverage-policy.mjs`, `scripts/coverage-parse.mjs`, `scripts/run-coverage.mjs`, and `tests/coverage/policy.test.mjs`. Defer `.github/workflows/ci.yml`, `templates/spine-config.json`, agent templates, skill references, and `docs/PRD.md` to Steps 2–4.
- **Unrelated lockfile noise** — Current worktree also changes `package-lock.json` (`pi-ai` bin path `./dist/cli.js`). Exclude from the Step 1 commit unless reproducibly required by a dependency install in this task.
- **Step 0 / STATUS closure** — Mark Step 0 preflight complete (Node ≥22 test runner confirmed; no prior coverage tooling) and record this plan review in `STATUS.md` before marking Step 1 done.
- **`prepublishOnly` timing** — Adding `npm run coverage:check` to `prepublishOnly` is reasonable npm wiring but not a Step 1 checkbox; acceptable in the Step 1 commit if kept, or defer to Step 4 verification if you prefer publish gates to land with full verification.

### Suggestions

- **Script split** — `test:coverage` with `--report-only` (emit report, propagate test exit status) vs `coverage:check` (fail when line coverage < 77%) is the right operator/CI split; Step 2 should wire `coverage:check` into CI, not the report-only variant.
- **Dual test-glob source** — `TEST_GLOBS` in `coverage-policy.mjs` duplicates `package.json`; the alignment test mitigates drift. Consider a brief comment in `run-coverage.mjs` that both must stay in sync when adding test directories.
- **Threshold enforcement order** — `run-coverage.mjs` should continue to fail on sub-threshold coverage *and* propagate non-zero test-runner status when coverage passes (current tail `process.exit(result.status ?? 0)` is correct).
- **Step 4 baseline** — Local run shows ~81% in-scope line coverage today, above the 77% gate; Step 4 still owns confirming green `npm run typecheck && SPINE_WORKER_STUB=1 npm test && npm run coverage:check` after all steps land.
- **Do NOT** — No CI, spine-config, worker/reviewer/skill template, or PRD edits in Step 1; no scoping of `spine-tasks/` or `.worktrees/`.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-061",
  "feedback": "Step 1 plan: Node native --experimental-test-coverage via run-coverage.mjs, coverage-policy.mjs (COVERAGE_THRESHOLD=77, src/bin/extensions includes), coverage-parse.mjs, npm scripts test:coverage (--report-only) and coverage:check (fail <77%), plus tests/coverage/policy.test.mjs. No CI, spine-config, agent/skill, or PRD scope. Step 1 commit = package.json + scripts/coverage-*.mjs + tests/coverage only; exclude unrelated package-lock.json delta. Record plan review in STATUS; proceed to Step 2 CI + spine-config."
}
```
