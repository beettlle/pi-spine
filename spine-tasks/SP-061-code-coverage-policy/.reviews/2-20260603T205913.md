## Plan Review: Step 2 — CI + spine-config defaults

### Verdict: APPROVE

### Summary

Step 2 correctly wires **threshold enforcement** (`npm run coverage:check`, not report-only `test:coverage`) into GitHub Actions after typecheck and unit tests, and sets `templates/spine-config.json` `testing.testWithCoverage` to the same command so `spine init` consumers and integrate-gate evidence collection get a usable default. CI step order (typecheck → `npm test` → coverage → CLI smoke) matches PROMPT and Step 1 review guidance. Scope stays within `.github/workflows/ci.yml` and `templates/spine-config.json` — no agent templates, skill text, or PRD edits. Local `npm run coverage:check` passes at ~81% in-scope line coverage, above the 77% gate.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Plan-before-implementation ordering** — Uncommitted worktree changes for `ci.yml` and `spine-config.json` exist before this plan review. Land the Step 2 commit after plan APPROVE; if already implemented, limit the commit to the two artifact files only.
- **Step 2 plan review closure** — After this review file is written, record `.reviews/2-20260603T205913.md` under Step 2 in `STATUS.md` (mirror Step 1) before marking Step 2 complete.
- **Commit scope / unrelated diff** — Revert incidental `agents` block pretty-printing in `templates/spine-config.json`; Step 2 should change only `testing.testWithCoverage` (and any required trailing-comma hygiene), not unrelated JSON formatting.
- **PROMPT doc checkbox** — Step 2 asks for a brief comment in template or docs; JSON cannot carry comments. Add one sentence to README CI section (coverage step + `testWithCoverage` default) in Step 2 or explicitly defer to Step 4 with a STATUS note — not blocking if Step 4 owns README/PRD updates per PROMPT “Check If Affected.”

### Suggestions

- **`--if-present` on coverage** — Matches existing `npm test --if-present` pattern from TP-003; acceptable for a shared workflow template. pi-spine after Step 1 always defines `coverage:check`; CI will enforce 77% on this repo.
- **Double test run** — `coverage:check` re-runs the full test suite after `npm test`; acceptable given Step 1’s script design; optimize later only if CI time becomes painful.
- **`spine init` test assertion** — Optional follow-up: assert `config.testing.testWithCoverage === "npm run coverage:check"` in `tests/spine-init.test.mjs` so template drift is caught (not a Step 2 checkbox).
- **Dogfood config** — `.spine/spine-config.json` in this repo may still have empty `testWithCoverage`; updating it is out of Step 2 File Scope but helps local integrate evidence — optional operator follow-up.
- **Do NOT** — No worker/reviewer/skill template, `docs/PRD.md`, or coverage script changes in Step 2; no lowering the 77% threshold.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-061",
  "feedback": "Step 2 plan: CI step runs npm run coverage:check --if-present after typecheck and npm test; templates/spine-config.json sets testing.testWithCoverage to npm run coverage:check for spine init and integrate evidence. Revert unrelated agents JSON reformat in template commit. Record plan review in STATUS; optional README one-liner or defer to Step 4. Step 2 commit = .github/workflows/ci.yml + templates/spine-config.json only. Proceed to Step 3 agent/skill policy after Step 2 lands."
}
```
