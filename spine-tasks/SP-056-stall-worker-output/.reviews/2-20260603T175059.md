## Plan Review: Step 2 — Persist logs + wire stall kill

### Verdict: APPROVE

### Summary

Step 2 correctly bridges Step 1 capture primitives into runtime behavior without touching merge/.DONE policy or stall-kill semantics. The planned orchestration surface — `finalizeWorkerOutput`, `persistWorkerOutputLog`, `workerOutputLogPath` / `workerOutputLogRef`, and `recordStallKilled` in `worker-output.mjs` — keeps redact → tail → persist ordering intact and gives `worker-host.mjs` a single integration point.

Terminal capture wiring is complete for all PROMPT classifications:

| Path | Trigger | Capture hook |
|------|---------|--------------|
| `aborted` | abort signal → `SIGTERM`/`SIGKILL` → `await childDone` | `buildWorkerFailureResult` |
| `stall_timeout` | stall deadline → `SIGTERM` → `await childDone` | `buildWorkerFailureResult` + `recordStallKilled` |
| `failed` | child exit without `.DONE` | `finalizeWorkerOutput` on normal return |
| `succeeded` | `.DONE` + exit 0 | skip unless `retainWorkerOutputOnSuccess` |

The stall path preserves the existing kill sequence (`child.kill("SIGTERM")` then await buffered output via `collectChildOutput`), which directly addresses SAT-020 / I-01 empty-output failures. Persisting to `.spine/runtime/<batchId>/lanes/lane-<N>/worker-output-<taskId>.log` matches FR-STALL-01.3 and the PROMPT path.

`lane.stall_killed` journaling on the stall branch only — with `exitCode`, vault-relative `logPath` (`logRef ?? logPath`), `stallDeadline`, and `progressSignals` (plus flattened signal fields) — satisfies FR-STALL-01.5 while leaving `lane.died` / `task.failed` emission in the engine layer for Step 3. Event ordering is correct: `recordStallKilled` appends inside `finalizeWorkerOutput` during `runWorker` return, before the engine emits downstream failure events.

The agent-session plan is sound: no subprocess stdout/stderr exists, so accumulate streamed assistant/tool text via `session.subscribe` into `state.transcript`, merge into failure `output` through `appendAgentSessionOutput`, and document the gap inline. That satisfies the PROMPT “forwarding (or document gap)” bullet without weakening stall kill or adding fake pipes.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Step 2 commit boundary** — Keep Step 2 scoped to persist + stall-kill wiring + agent-session transcript. Defer `evidence.mjs` gate refs, `journal.mjs` diagnosis hint priority / `summarizeJournalEvent` logPath suffix, `bin/spine-worker-runner.mjs` stub hang hooks, and `tests/batch/stall-output.test.mjs` to Steps 3–4 per PROMPT (or split into follow-on commits within the branch).
- **`lane.stall_killed` when capture is empty** — `recordStallKilled` runs only when `persistWorkerOutputLog` returns non-null (non-empty post-redaction tail). Consider journaling `lane.stall_killed` with `logPath: null` on every `stall_timeout` so diagnose always surfaces a stall-kill episode even when stderr is silent. Optional hardening, not required to proceed.
- **`task.failed.output` verification** — Engine already spreads `...workerResult`; Step 2 should note that journal payload population and `capPayloadSize` interaction are Step 3 verification concerns, not engine edits here.
- **STATUS hydration** — Record Step 2 files touched, stall SIGTERM → capture sequence, and agent-session transcript note before marking the step done (aids Step 3 code review).

### Suggestions

- **`buildWorkerFailureResult`** — Keep as the sole failure return builder so abort / stall / review-spawn failures do not diverge; pass `stallDeadline` and `signals` only on the stall branch.
- **Journal `logPath`** — Prefer vault-relative `workerOutputLogRef` in the event payload (plan already uses `logRef ?? logPath`) so downstream diagnose works without absolute paths.
- **`review_failed` preflight** — Leave outside `shouldCaptureWorkerOutput` (not a terminal worker death); no Step 2 change needed.
- **Stub stall repro** — `SPINE_WORKER_STUB_HANG_MS` / `SPINE_WORKER_STUB_OUTPUT` belong in Step 4 test harness, not the Step 2 feature commit.
- **Do NOT** — No stall-timeout weakening, no `.DONE` / merge policy changes, no SP-057/058/059 scope.

### Step 2 Checklist (PROMPT)

| Item | Plan coverage |
|------|---------------|
| Capture on terminal failure including stall `SIGTERM` path | `buildWorkerFailureResult` on abort / stall / failed; stall awaits output after `SIGTERM` |
| Write `.spine/runtime/<batchId>/lanes/lane-<N>/worker-output-<taskId>.log` | `persistWorkerOutputLog` + path helpers |
| Journal `lane.stall_killed` (exitCode, logPath, stallDeadline, signals) | `recordStallKilled` on stall branch |
| Agent-session stdout/stderr forwarding (or document gap) | `subscribe` → `transcript`; inline comment documents no subprocess stream |

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-056",
  "feedback": "Step 2 plan is sound: finalizeWorkerOutput orchestrates persist + stall_killed journal; worker-host routes abort/stall_timeout/failed through buildWorkerFailureResult with SIGTERM-then-await-output stall path; agent-session uses subscribe/transcript with documented gap. Restrict Step 2 commit to persist/stall-kill/agent-session wiring; defer evidence refs, diagnose hints, and integration tests to Steps 3–4. Optional: journal stall_killed when log is empty. Proceed to implementation."
}
```
