## Plan Review: Step 1 — Config + capture utilities

### Verdict: APPROVE

### Summary

Step 1 correctly front-loads FR-STALL-01's bounded, redacted capture primitives before any `worker-host` or journal wiring. A dedicated `src/batch/worker-output.mjs` with `resolveWorkerOutputConfig`, `redactWorkerOutput`, and `captureWorkerOutputTail` (plus `TRUNCATION_MARKER` and brief-aligned defaults) matches the PROMPT checkboxes and the stall-recovery brief config block. Applying **redact → line tail → byte cap with marker** in that order satisfies FR-STALL-01.6 without leaking secrets into truncated tails. `templates/spine-config.json` lane keys (`workerOutputMaxBytes`, `workerOutputTailLines`, `retainWorkerOutputOnSuccess`) align with the brief; optional `lanes.workerOutputDenyPatterns` covers configurable deny lists. Step 2 should consume these exports only—persist paths, `lane.stall_killed`, and `task.failed` population stay out of the Step 1 commit.

### Issues Found

None blocking.

### Missing Items

- **Explicit Step 1 commit boundary** — PROMPT Steps 2–3 own persist, stall-kill journal, and evidence; Step 1 should not land `finalizeWorkerOutput` / `worker-host` integration in the same commit as utilities (acceptable to stub exports in the module if already drafted, but wire in Step 2).
- **`workerOutputDenyPatterns` in template** — Not listed as a PROMPT checkbox but required by FR-STALL-01.6; optional empty array or comment in `templates/spine-config.json` helps operators discover the knob.
- **`bin/spine-config.mjs` validation** — File Scope mentions it; lane keys can follow existing pattern (read via `config.lanes` without `settings-fields` registration). No schema change required for Step 1 approval.

### Suggestions

- **Built-in deny list** — Cover `DATABASE_URL`, connection URIs, bearer/token/password/api_key literals, and common key prefixes (`sk-`, `ghp_`) as in the brief; skip journal `redactSecrets` reuse (key-based) because stderr needs line-oriented regex.
- **`shouldCaptureWorkerOutput` helper** — Fine to add in Step 1 for Step 2, but keep it pure (classification + `retainWorkerOutputOnSuccess`); no filesystem or `appendJournalEvent` calls in Step 1 tests.
- **Unit tests** — Step 1 may add focused tests for config resolution, truncation marker, line/byte limits, and redaction; defer `runWorker` stall integration and evidence refs to Steps 2–4 per PROMPT.
- **Agent-session gap** — Note in STATUS or Step 2 plan that `agent-session-worker` stdout/stderr forwarding is out of Step 1 scope (PROMPT Step 2 bullet).

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan is sound: worker-output.mjs utilities with brief-aligned lane config, redact-before-truncate pipeline, and built-in plus deny-pattern redaction. Proceed with template keys and pure exports; defer persist, stall_killed journal, worker-host wiring, and integration tests to later steps."
}
```
