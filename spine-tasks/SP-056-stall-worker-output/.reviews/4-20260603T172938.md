## Code Review: Step 4 — Tests + verification

### Verdict: APPROVE

### Summary

Step 4 delivers the PROMPT verification gate: a dedicated `tests/batch/stall-output.test.mjs` suite and stub-worker hooks that make stall repro deterministic. `bin/spine-worker-runner.mjs` adds `SPINE_WORKER_STUB_HANG_MS` (100 ms sleep slices so stall detection can fire) and `SPINE_WORKER_STUB_OUTPUT` (stderr injection before hang). The integration test drives `runWorker` with a sub-minute stall timeout, asserts non-empty captured `output`, on-disk `worker-output-<taskId>.log`, `lane.stall_killed` journal payload (exitCode 124, vault-relative `logPath`), diagnosis hints, and `summarizeJournalEvent` path citation. Unit tests cover config resolution, line/byte tail capture with truncation marker, and redaction. A separate evidence test and a success-path skip test satisfy the brief acceptance bullets.

Verification ran clean: `npm run typecheck` pass; `SPINE_WORKER_STUB=1 npm test` — 344 pass (including all 6 stall-output tests).

### What looks good

- **Deterministic stall fixture.** Stub hang + injected stderr replaces flaky real hangs; stall test completes in ~5 s with configured `stallTimeoutMinutes: 0.02`.
- **Brief-aligned acceptance coverage.** Non-empty tail on stall, persisted log, diagnose cites path, truncation marker, redaction, and success skip without `retainWorkerOutputOnSuccess` are all exercised.
- **Env hygiene in async tests.** Stall and success integration tests save/restore `SPINE_WORKER_STUB*` env vars and clean temp dirs in `finally`.
- **Full-suite gate.** Step 4 requires the whole test run, not only the new file; no regressions across the 344-test matrix.

### Issues Found

None blocking.

### Non-blocking notes

- **Untracked core artifacts.** `src/batch/worker-output.mjs` and `tests/batch/stall-output.test.mjs` remain `??` in git status and do not appear in `git diff --name-only`. Stage both (with the rest of the SP-056 diff) before merge.
- **Terminal paths not integration-tested.** Only `stall_timeout` is covered end-to-end via `runWorker`. `aborted` and non-stall `failed` paths rely on `buildWorkerFailureResult` / `finalizeWorkerOutput` wiring from Steps 2–3 without dedicated tests.
- **Agent-session failure output.** `agent-session-worker.mjs` subscribe/transcript accumulation is not tested on terminal failure; existing `worker-backend.test.mjs` covers success only. Acceptable given subprocess is the primary SAT-020 repro path; optional follow-up mock with `subscribe` emitting deltas.
- **`retainWorkerOutputOnSuccess: true`.** Success path tested only for the default skip behavior; override not asserted.
- **Engine journal payload.** Tests assert `runWorker` return values, not `task.failed.payload.output` after engine spread (unchanged from Step 3 note). On-disk log + return `output` are sufficient for FR-STALL-01 acceptance.
- **Journal cap vs capture max.** 262 KiB capture vs 32 KiB journal `capPayloadSize` remains a follow-up if large tails must appear intact in journal JSON.

### Verification

| Check | Result |
|-------|--------|
| `tests/batch/stall-output.test.mjs` present | yes (6 tests) |
| `npm run typecheck` | pass |
| `SPINE_WORKER_STUB=1 npm test` | 344 pass |
| Stall repro: non-empty `output` | integration test |
| Log on disk + diagnose cites path | integration test |
| Truncation marker + redaction | unit tests |
| Success skips log (default) | integration test |
| Evidence ref on gate | `collectEvidenceBundle` test |

```json
{
  "verdict": "APPROVE",
  "step": 4,
  "taskId": "SP-056",
  "feedback": "Step 4 tests and verification complete: stall-output.test.mjs covers config/tail/redaction units plus stall_timeout integration with stub hang hooks; typecheck and full SPINE_WORKER_STUB=1 suite pass (344/344). Stage untracked worker-output.mjs and stall-output.test.mjs before merge. Optional follow-ups: aborted/failed integration tests and agent-session failure transcript test."
}
```
