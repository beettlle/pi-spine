## Code Review: Step 4 — Tests + verification

### Verdict: APPROVE

### Summary

Step 4 satisfies the PROMPT verification gate. New file `tests/batch/stall-output.test.mjs` adds six tests: unit coverage for `resolveWorkerOutputConfig`, `captureWorkerOutputTail` (line tail + byte truncation marker), and `redactWorkerOutput`; an integration test that reproduces `stall_timeout` via stub hang hooks; evidence-bundle ref scanning; and a success-path test confirming logs are skipped when `retainWorkerOutputOnSuccess` is false.

Stub support in `bin/spine-worker-runner.mjs` (`SPINE_WORKER_STUB_HANG_MS`, `SPINE_WORKER_STUB_OUTPUT`) makes stall repro deterministic without weakening `.DONE` or merge policy. The stall integration test asserts non-empty `result.output`, on-disk `worker-output-SP-056.log`, `lane.stall_killed` (exitCode 124, vault-relative `logPath`), diagnosis hints, and `summarizeJournalEvent` path citation.

Verification: `npm run typecheck` pass; `SPINE_WORKER_STUB=1 npm test` — 344/344 pass (including all stall-output tests).

### What looks good

- **Brief-aligned acceptance.** Stall repro yields truncated output, persisted lane log, and diagnose cites log path; truncation and redaction are unit-tested; success path skips log by default.
- **Fast, deterministic stall fixture.** `stallTimeoutMinutes: 0.02` with stub hang completes in ~5 s instead of multi-minute real hangs.
- **Env hygiene.** Integration tests save/restore `SPINE_WORKER_STUB*` variables and remove temp dirs in `finally`.
- **Full-suite gate.** Step 4 requires the entire stub test matrix, not only the new file; no regressions observed.

### Issues Found

None blocking.

### Non-blocking notes

- **Untracked artifacts.** `src/batch/worker-output.mjs` and `tests/batch/stall-output.test.mjs` are `??` in git status and absent from `git diff --name-only`. Stage both with the tracked SP-056 diff before merge.
- **Terminal paths not integration-tested.** Only `stall_timeout` is exercised end-to-end via `runWorker`. `aborted` and non-stall `failed` rely on `buildWorkerFailureResult` / `finalizeWorkerOutput` from earlier steps without dedicated tests.
- **Agent-session failure output.** `agent-session-worker.mjs` subscribe/transcript path is untested on terminal failure; subprocess backend is the primary SAT-020 repro path.
- **`retainWorkerOutputOnSuccess: true`.** Success path tested only for default skip; override not asserted.
- **Journal cap vs capture max.** 262 KiB capture vs 32 KiB `capPayloadSize` may truncate large tails in journal JSON while on-disk logs remain correct (noted in Step 3 review).

### Step 4 Checklist (PROMPT)

| Item | Result |
|------|--------|
| `tests/batch/stall-output.test.mjs` | yes (6 tests) |
| `npm run typecheck && SPINE_WORKER_STUB=1 npm test` | pass (344/344) |
| Stall repro: non-empty `output` | integration test |
| Log on disk + diagnose cites path | integration test |
| Truncation marker + redaction | unit tests |
| Success skips log (default) | integration test |

### Verification

| Check | Result |
|-------|--------|
| `git diff --name-only` (tracked) | 7 files + STATUS |
| `npm run typecheck` | pass |
| `SPINE_WORKER_STUB=1 npm test` | 344 pass |

```json
{
  "verdict": "APPROVE",
  "step": 4,
  "taskId": "SP-056",
  "feedback": "Step 4 complete: stall-output.test.mjs covers config/tail/redaction units plus stall_timeout integration with stub hang hooks; typecheck and full SPINE_WORKER_STUB=1 suite pass (344/344). Stage untracked worker-output.mjs and stall-output.test.mjs before merge. Optional follow-ups: aborted/failed integration tests and agent-session failure transcript test."
}
```
