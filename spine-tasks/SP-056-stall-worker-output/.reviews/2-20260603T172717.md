## Plan Review: Step 2 — Persist logs + wire stall kill

### Verdict: APPROVE

### Summary

Step 2 correctly consumes Step 1 primitives (`shouldCaptureWorkerOutput`, `redactWorkerOutput`, `captureWorkerOutputTail`) through a single orchestration surface — `finalizeWorkerOutput` in `worker-output.mjs` with `persistWorkerOutputLog`, `workerOutputLogPath` / `workerOutputLogRef`, and `recordStallKilled` — and wires it from `worker-host.mjs` via `buildWorkerFailureResult` on **abort**, **stall_timeout** (post-`SIGTERM` + `await childDone`), and the normal **failed** exit path. Persisting to `.spine/runtime/<batchId>/lanes/lane-<N>/worker-output-<taskId>.log` matches FR-STALL-01.3 and the PROMPT path. Journaling `lane.stall_killed` only on `stall_timeout` with `exitCode`, vault-relative `logPath`, `stallDeadline`, and `progressSignals` (plus flattened signal fields) matches FR-STALL-01.5 and keeps `lane.died` / `task.failed` in the engine layer for Step 3. The agent-session plan is sound: no subprocess streams, so accumulate streamed assistant/tool text via `session.subscribe` into `state.transcript` returned from `wait()` — with an inline comment documenting the gap, satisfying the PROMPT “forwarding (or document gap)” bullet without weakening stall kill.

`collectChildOutput` already buffers subprocess stdout/stderr; routing all terminal paths through `finalizeWorkerOutput` ensures the stall kill path captures stderr emitted before `SIGTERM` (the SAT-020 / I-01 failure mode). Event ordering remains correct: `lane.stall_killed` is appended inside `runWorker` before the engine emits `lane.died` and `task.failed`.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Step 2 commit boundary** — The working tree also touches `evidence.mjs` (gate refs), `journal.mjs` diagnosis hints / `summarizeJournalEvent`, `bin/spine-worker-runner.mjs` stub hang hooks, and `tests/batch/stall-output.test.mjs`. Per PROMPT, Step 2 should land **persist + stall-kill wiring + agent-session transcript** only; defer evidence refs and diagnose hint priority to **Step 3**, integration tests and stub harness to **Step 4** (or split into follow-on commits within the step).
- **`task.failed.output` explicit wiring** — `engine.mjs` already spreads `...workerResult` into `task.failed`; Step 2 plan should note verification in Step 3 rather than editing the engine here, so journal payload size / `capPayloadSize` behavior is validated with real tails.
- **`lane.stall_killed` when capture is empty** — `recordStallKilled` runs only when `persistWorkerOutputLog` returns non-null (non-empty post-redaction tail). Consider journaling `lane.stall_killed` with `logPath: null` on `stall_timeout` even when stderr is empty, so diagnose always surfaces a stall-kill hint (optional hardening, not required to proceed).
- **STATUS hydration** — Add a short Step 2 subsection (files touched, stall path behavior, agent-session transcript note) before marking the step done; aids Step 3 code review.

### Suggestions

- **`buildWorkerFailureResult`** — Keep as the only failure return builder so abort / stall / review spawn failures do not diverge; pass `stallDeadline` and `signals` only on the stall branch (already planned).
- **Journal `logPath`** — Prefer vault-relative `workerOutputLogRef` in the event payload (plan already uses `logRef ?? logPath`) so `summarizeJournalEvent` and diagnose work without absolute paths.
- **`review_failed` preflight** — Leave outside `shouldCaptureWorkerOutput` (not a terminal worker death); no change needed in Step 2.
- **Stub stall repro** — `SPINE_WORKER_STUB_HANG_MS` / `SPINE_WORKER_STUB_OUTPUT` on the runner belong in Step 4 test harness, not the Step 2 feature commit.
- **Do NOT** — No stall-timeout weakening, no `.DONE` / merge policy changes, no SP-057/058/059 scope.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-056",
  "feedback": "Step 2 plan: finalizeWorkerOutput + persistWorkerOutputLog + recordStallKilled in worker-output.mjs; worker-host buildWorkerFailureResult on abort/stall_timeout/failed with SIGTERM stall path awaiting child output; agent-session transcript via subscribe. Restrict Step 2 commit to persist/stall-kill/agent-session wiring; defer evidence refs, diagnose hints, and stall-output.test.mjs to Steps 3–4. Optional: journal stall_killed when log is empty. Proceed to implementation commit."
}
```
