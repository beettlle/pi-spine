### Verdict: PASS

### Summary
The task successfully implements the requirements. It prevents `worker_alive` heartbeats from continuously refreshing the stall anchor when progress signals are entirely static-null. The unit and integration tests are robust and cleanly cover the proxy child processes as requested, and all tests pass with no regressions.

```json
{
  "verdict": "PASS",
  "feedback": "Code changes and tests exactly fulfill the prompt without violating any negative constraints. Well done."
}
```
