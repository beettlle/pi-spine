### Verdict: PASS

### Summary
The task successfully implements the `quota-risk` advisory doctor check as requested in SP-700. The implementation properly warns on quota-constrained pins without headroom, surfaces recent failure patterns from `run-metrics`, maintains `ok: true` to avoid blocking preflight, covers both warn and clear paths in tests, and accurately updates the release operator skill document.

```json
{"verdict":"PASS","feedback":"Excellent work. The check degrades gracefully when metrics or probes are missing and correctly adheres to the `ok: true, warning: true` advisory pattern for preflight."}
```