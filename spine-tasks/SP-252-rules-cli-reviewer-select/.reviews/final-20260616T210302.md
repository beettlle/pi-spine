### Verdict: PASS

### Summary

SP-252 delivers reviewer rule preview on `spine rules select` with `--role reviewer`, `--review-type plan|code|final`, and optional `--baseline`, while preserving worker-default behavior. Six CLI tests cover reviewer paths and worker regression; contract checks pass (coverage 86.75% on in-scope lines, ≥77% threshold). `.DONE` is present and STATUS documents example commands and the SP-248 prerequisite shim in `select.mjs`.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met. CLI flags, JSON payload (reviewType, scopePaths, selection), help text, and tests align with mission. Worker select unchanged (regression test). Minor out-of-scope change in src/config/cursor-rules/select.mjs (selectRulesForReviewer) is documented as SP-248 prerequisite and is minimal. No blocking issues."}
```
