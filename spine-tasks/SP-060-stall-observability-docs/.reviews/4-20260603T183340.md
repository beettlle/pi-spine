## Plan Review: CONTEXT + gap list

### Verdict: APPROVE

### Summary

Step 4 correctly closes the SP-060 epic bookkeeping deferred from Steps 2–3: mark **SP-056–058** and **SP-060** **Done** in `taskplane-tasks/CONTEXT.md` (Phase 11 table + priority backlog), leave **SP-059** **Staged**, add **GAP-STALL-02** through **GAP-STALL-05** to `docs/compatibility/taskplane-gap-list.md` mapping **FR-STALL-01 / 02 / 03A** and the operator/SAT-020 closeout to shipped tasks, and run the PROMPT verification gate **`npm run typecheck && SPINE_WORKER_STUB=1 npm test`**.

The plan must also land two **merge-conflict cleanups** left on `HEAD` after Step 3: `src/batch/journal.mjs` (`extractJournalDiagnosisHints` priority: `lane.stall_killed` → `review.failed` → `lane.salvage_inspection` → `lane.checkpoint_warning`) and `templates/spine-config.json` (union of SP-056 worker-output keys and SP-057 `checkpointWarningMinutes` / `extendGraceOnFileScope`). Without those fixes the branch ships invalid JS/JSON despite dashboard work in `db9b884`.

Epic test plan items **1–3** are satisfied by Step 1 (`stall-sat020-integration.test.mjs`); item **4** (`autoCommitOnStall` retry) remains runbook-only while SP-059 is staged; item **5** (`.DONE` required, mixed-outcome unchanged) is covered by the Step 1 `.DONE` regression test plus existing `integration-abc.test.mjs` / `incidents.test.mjs` coverage in the full stub suite (357/357 pass on the resolved worktree).

### Issues Found

None blocking the plan itself.

### Missing Items

- **Blocking for Step 4 commit (not plan scope)** — `HEAD` still contains `<<<<<<<` / `>>>>>>>` markers in `src/batch/journal.mjs` and `templates/spine-config.json`; include resolved files in the Step 4 commit before merge. Working-tree copies are already clean.
- **CONTEXT narrative** — Update **Current State** line 11 from “Phase 11 … **staged**” to shipped wording (e.g. SP-056–058 + SP-060 **Done**, SP-059 **Staged**); table/backlog edits alone do not satisfy PROMPT “note FR-STALL-* shipped.”
- **Gap list metadata** — Bump `**Last updated:**` in `taskplane-gap-list.md` to **2026-06-03** when adding GAP-STALL-02–05.
- **Step 4 commit boundary** — Land only `taskplane-tasks/CONTEXT.md`, `docs/compatibility/taskplane-gap-list.md`, `src/batch/journal.mjs`, and `templates/spine-config.json` (plus this review / STATUS). Do not re-bundle Step 1–3 test, runbook, PRD, or dashboard paths.
- **STATUS closure** — Record this review in `STATUS.md` Step 4; document verification (`typecheck` + `SPINE_WORKER_STUB=1 npm test`, 357 pass) before marking Step 4 complete. STATUS currently shows ✅ with “verification pending”—align after the suite run.
- **GAP-STALL-05 polish (optional)** — Mention dashboard `laneAlert` (FR-STALL-02.6) in the GAP-STALL-05 closure blurb alongside runbook + SAT-020 fixture; not required by PROMPT.

### Suggestions

- **Phase 11 one-liner** — Under the Phase 11 table, add a short bullet: “**FR-STALL-01–03A** shipped (SP-056–058); observability closeout **SP-060**; **FR-STALL-03B** pending SP-059.”
- **Suggested run order** — Optionally annotate Waves A–D as complete except Wave C (SP-059); avoids operators re-running finished waves.
- **Epic item 4** — No Step 4 test for `autoCommitOnStall`; runbook § already documents opt-in when SP-059 lands.
- **Commit message** — `docs(SP-060): Phase 11 CONTEXT, stall gap list, merge cleanup` (or split merge cleanup if you prefer a `fix` prefix).

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 4 plan: CONTEXT Phase 11 SP-056–058/060 Done (SP-059 Staged), GAP-STALL-02–05 closed for FR-STALL-* epic, resolve journal.mjs + spine-config.json merge markers on HEAD, typecheck + SPINE_WORKER_STUB=1 full suite (357 pass). Update CONTEXT Current State line 11; bump gap-list last updated; scoped Step 4 commit; record review + verification in STATUS. Epic items 1–3/5 covered; item 4 deferred (SP-059 staged)."
}
```
