## Plan Review: Operator docs

### Verdict: APPROVE

### Summary

Step 2 correctly closes PROMPT operator-doc outcomes and epic test plan item 4 (runbook-only, no integration test while SP-059 remains staged): a **Stall diagnosis (5-minute path)** section in `docs/adoption/operator-runbook.md` with SAT-020 journal order (`lane.checkpoint_warning` → `lane.stall_killed` → `lane.salvage_inspection` → `task.failed` → retry), a lane config table, `autoCommitOnStall` retry guidance cross-linked to §18.5, and links to `docs/features/stall-recovery-improvements-brief.md` (in-section + Related docs). Surgical PRD §18.4 additions (`checkpointWarningMinutes`, `extendGraceOnFileScope`, `lane.checkpoint_warning`, `lane.stall_killed`, salvage enrichment) match shipped SP-056–058 behavior.

Prior blocking issue is resolved: `docs/PRD.md` is ~1480 lines (HEAD ~1478), §18.5–§20+ intact; only §18.4 paragraphs expanded. Keep Step 2 commits limited to runbook + PRD; defer `docs/compatibility/taskplane-gap-list.md` and `taskplane-tasks/CONTEXT.md` to Step 4 per PROMPT step boundaries.

### Issues Found

None blocking.

### Missing Items

- **Step 2 commit boundary** — Land `docs/adoption/operator-runbook.md` and surgical `docs/PRD.md` only. Do not include `docs/compatibility/taskplane-gap-list.md`, `taskplane-tasks/CONTEXT.md`, dashboard files, or Step 1 test/fixture paths in the Step 2 commit (Steps 3–4 per PROMPT).
- **Epic test plan item 4** — Document `autoCommitOnStall` retry in the runbook (present); no integration test required while SP-059 remains staged. `templates/spine-config.json` does not yet expose `autoCommitOnStall` — runbook correctly describes it as opt-in when SP-059 ships.
- **STATUS closure** — Record `.reviews/2-20260603T183126.md` in `STATUS.md` Step 2 when marking complete (if not already listed).

### Suggestions

- **Optional runbook polish** — Mention `lane.stall_warning` (emitted immediately before kill) and `evidence/salvage-<taskId>.json` in step 3 for parity with PRD §18.4 and FR-STALL-03.6; not blocking.
- **PRD edit procedure** — If re-editing, `git checkout HEAD -- docs/PRD.md` then apply §18.4-only replacements; never rewrite from §18.5 onward.
- **PRD §18.5** — PROMPT "§18.4–18.5 alignment" means runbook cross-reference to atomic retry, not rewriting §18.5 body in Step 2.
- **Diagnosis flow** — Runbook table mirrors Step 1 `stall-sat020-integration.test.mjs` event order; cite `spine status --diagnose` `journalHints` as the primary entry point (already correct).

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 2 plan: runbook 5-minute stall path (checkpoint_warning → stall_killed → salvage_inspection → task.failed → retry), config table, autoCommitOnStall retry prose, brief links; surgical PRD §18.4 only. PRD truncation fixed (~1480 lines, §18.5+ intact). Defer gap-list and CONTEXT to Step 4; defer dashboard to Step 3. Epic item 4 is runbook-only. Proceed with scoped Step 2 commit."
}
```
