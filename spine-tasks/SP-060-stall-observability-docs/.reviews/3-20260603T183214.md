## Plan Review: Dashboard (if feasible)

### Verdict: APPROVE

### Summary

Step 3 correctly closes PROMPT optional dashboard work and **FR-STALL-02.6** without re-implementing SP-056–058 stall logic. The plan derives a per-lane `laneAlert` in `buildDashboardSnapshot` by reusing the existing journal tail (last 20 events, already loaded for the dashboard) and a new `resolveLaneAlert()` that scans newest-first for lane-scoped `lane.stall_killed` then `lane.checkpoint_warning` within a **30-minute** window—aligned with `JOURNAL_HINT_RECENT_MS` / `isRecentJournalHint` for checkpoint hints in `extractJournalDiagnosisHints`. The UI surfaces this as a lane-table status suffix (`· checkpoint warning` / `· stall killed`) plus warn/error CSS on the status cell, satisfying “lane badge” observability while keeping NFR-OBS-04 parity (snapshot is the single source; `dashboard.js` only renders `lane.laneAlert`).

Including the `src/batch/journal.mjs` merge-conflict cleanup (restoring `lane.stall_killed` before `review.failed` in hint priority) is appropriate in the same Step 3 commit if that file is still broken in the worktree; it unblocks consistent diagnose + dashboard behavior and does not expand stall engine scope.

### Issues Found

None blocking.

### Missing Items

- **Unit tests for `resolveLaneAlert`** — Add focused cases in `tests/dashboard/snapshot-lanes.test.mjs` (or `snapshot.test.mjs`): lane-scoped match, newest-wins, `stall-killed` over older `checkpoint-warning`, stale events (>30 min) ignored, unrelated-lane events skipped, `laneAlert: null` when absent. Not required by PROMPT but cheap regression guard for FR-STALL-02.6.
- **Step 3 commit boundary** — Land only `src/dashboard/snapshot.mjs`, `src/dashboard/public/dashboard.js`, `src/dashboard/public/dashboard.css`, and `src/batch/journal.mjs` (if merge fix included). Do **not** bundle Step 4 files (`docs/compatibility/taskplane-gap-list.md`, `taskplane-tasks/CONTEXT.md`, `templates/spine-config.json`) or Step 1–2 docs/tests in the Step 3 commit.
- **STATUS closure** — Do not mark Step 3 complete until this review is recorded in `STATUS.md` and Step 3 artifacts are committed.
- **Epic / Step 4** — Full-suite verification (`npm run typecheck && SPINE_WORKER_STUB=1 npm test`) and mixed-outcome regression remain Step 4, not Step 3.

### Suggestions

- **Recency semantics** — Applying 30 minutes to both alert types (not only `checkpoint_warning`) is reasonable for a “recent episode” badge; `stall_killed` in diagnose hints is unbounded, but the dashboard badge is intentionally time-boxed.
- **`lane.stall_warning`** — Omit from v1 badge (PROMPT targets checkpoint warning + stall killed); journal tail already surfaces `stall_warning` in the events panel if present.
- **Journal tail depth** — Tail limit 20 is pre-existing; SAT-020 stall events sit at the end of a lane kill sequence. Only revisit if noisy multi-lane batches routinely push stall events off the tail.
- **Optional polish** — Export `LANE_ALERT_RECENT_MS` from `journal.mjs` or a shared constant to avoid duplicating `30 * 60 * 1000`; defer unless touching journal module anyway.
- **Manual smoke** — After commit, run `spine dashboard` against a batch with SAT-020 replay or stub stall and confirm lane-1 status cell shows suffix + color.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 3 plan: resolveLaneAlert on journal tail (30m, lane-scoped, stall-killed > checkpoint-warning), laneAlert on buildLaneRows, status suffix + CSS in dashboard.js/css; optional journal.mjs merge-conflict fix. Matches FR-STALL-02.6 and PROMPT without re-implementing stall logic. Add resolveLaneAlert unit tests; keep Step 3 commit scoped to dashboard + journal fix; defer CONTEXT/gap-list/full suite to Step 4. Record this review in STATUS before marking Step 3 done."
}
```
