## Plan Review: Operator docs

### Verdict: REVISE

### Summary

The Step 2 operator-doc plan correctly targets PROMPT outcomes and epic test plan item 4 (runbook-only): a **Stall diagnosis (5-minute path)** section in `docs/adoption/operator-runbook.md` with SAT-020 journal order (`lane.checkpoint_warning` → `lane.stall_killed` → `lane.salvage_inspection` → `task.failed` → retry), a lane config table, `autoCommitOnStall` retry guidance cross-linked to §18.5, and links to `docs/features/stall-recovery-improvements-brief.md`. Surgical PRD §18.4 additions (checkpoint warning, `extendGraceOnFileScope`, `lane.stall_killed` / salvage sentences) match shipped SP-056–058 behavior.

The worktree still **truncates `docs/PRD.md`** (~1183 lines vs ~1478 at `HEAD`), deleting the tail of §18.5 and all of §18.6+. That must be fixed before Step 2 lands. Keep Step 2 scoped to runbook + PRD only; defer gap-list rows and `CONTEXT.md` to Step 4 per PROMPT step boundaries.

### Issues Found

- **Blocking — PRD truncation** — Uncommitted `docs/PRD.md` still removes §18.5 tail and §18.6+ (retry segment rules, abort, testing, phases, etc.). Restore from `HEAD`, then re-apply only the §18.4 paragraph additions (~3 sentences + expanded configurable keys). Verify line count stays ~1478 before commit.

### Missing Items

- **Step 2 commit boundary** — Land `docs/adoption/operator-runbook.md` and surgical `docs/PRD.md` only. Do not include `docs/compatibility/taskplane-gap-list.md`, `taskplane-tasks/CONTEXT.md`, dashboard files, or Step 1 test/fixture paths in the Step 2 commit (Steps 3–4 per PROMPT).
- **Epic test plan item 4** — Document `autoCommitOnStall` retry in the runbook (already present); no integration test required while SP-059 remains staged. Runbook correctly notes default off; `templates/spine-config.json` does not yet expose `autoCommitOnStall` — keep describing it as opt-in when SP-059 ships.
- **Optional runbook polish** — Mention `lane.stall_warning` (emitted immediately before kill) and `evidence/salvage-<taskId>.json` in step 3 for parity with PRD §18.4 and FR-STALL-03.6; not blocking.
- **STATUS closure** — Do not mark Step 2 complete until PRD is restored and this review is recorded in `STATUS.md`.

### Suggestions

- **PRD edit procedure** — `git checkout HEAD -- docs/PRD.md`, then apply three targeted replacements in §18.4 only; never rewrite from §18.5 onward.
- **PRD §18.5** — PROMPT "§18.4–18.5 alignment" means runbook cross-reference to atomic retry, not rewriting §18.5 body in Step 2.
- **Diagnosis flow** — Runbook table already mirrors Step 1 `stall-sat020-integration.test.mjs` event order; cite `spine status --diagnose` `journalHints` as the primary entry point.

```json
{
  "verdict": "REVISE",
  "feedback": "Step 2 plan: runbook 5-minute stall path, config table, autoCommitOnStall retry prose, brief links; surgical PRD §18.4 only. Blocking: restore docs/PRD.md from HEAD (still truncated at ~1183 vs ~1478 lines) before commit. Defer gap-list and CONTEXT to Step 4; defer dashboard to Step 3. Epic item 4 is runbook-only. Proceed after PRD restore and scoped commit."
}
```
