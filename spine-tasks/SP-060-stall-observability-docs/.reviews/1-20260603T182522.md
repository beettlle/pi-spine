## Plan Review: SAT-020 fixture + integration test

### Verdict: APPROVE

### Summary

Step 1 correctly closes epic test plan items 1–3 (and partial item 5) by replaying the SearchATon SAT-020 stall pattern through existing SP-056–058 behavior—without re-implementing capture, checkpoint warnings, or salvage logic. A dedicated `SPINE_WORKER_STUB_SAT020` sequence in `bin/spine-worker-runner.mjs` (2× real `reportTaskProgress` / `task.step_completed`, scoped file touch, stderr tail, hang without `.DONE`) plus `tests/fixtures/stall-sat020/README.md` and `tests/batch/stall-sat020-integration.test.mjs` is the right integration boundary. The test drives `startBatch` with sub-minute stall config (`extendGraceOnFileScope: false`, long heartbeat interval) and asserts the required journal order, non-empty `task.failed.output`, on-disk worker log, `extractJournalDiagnosisHints` for stall + salvage, and `reconcileBatch` retry guidance—matching PROMPT Step 1 and brief TP-STALL-04 items 1–3.

### Issues Found

None blocking.

### Missing Items

- **Epic test plan item 4** — `autoCommitOnStall` / WIP-commit retry retention is not Step 1 scope; defer to runbook or a follow-on test in Step 2+ (PROMPT Step 1 does not require it).
- **Mixed-outcome merge regression** — Epic item 5 “mixed-outcome unchanged” belongs in Step 4 full-suite verification, not this step’s commit.
- **Step 1 commit boundary** — Keep Step 1 limited to `tests/fixtures/stall-sat020/`, `stall-sat020-integration.test.mjs`, and the SAT-020 stub block in `spine-worker-runner.mjs`. `SPINE_WORKER_STUB_DIRTY_FILE` (salvage-inspect helper) is tangential; include only if required for the SAT-020 test path, otherwise land with salvage tests or a separate commit.
- **STATUS closure** — After this review lands, record `.reviews/1-20260603T182522.md` in `STATUS.md` Step 1 before marking complete.

### Suggestions

- **Timing contract** — Document in fixture README that sub-minute `checkpointWarningMinutes` / `stallTimeoutMinutes` / `stallGraceAfterProgressMinutes` are CI-only; production defaults stay in `templates/spine-config.json`.
- **Stub sleep epochs** — The 1s → progress → 4s → file touch → 10s → hang sequence separates checkpoint vs activity poll epochs; keep sleeps stable so the ~30s integration test stays deterministic in CI.
- **Regression test** — The separate “`.DONE` still required” stub success case is sufficient for Step 1; no need to duplicate `integration-abc.test.mjs` mixed-outcome coverage here.
- **Plan-before-implementation** — If Step 1 artifacts already exist in the worktree, proceed with a Step 1–scoped commit after this APPROVE; defer operator docs, dashboard, and CONTEXT/gap updates to Steps 2–4.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan: SAT-020 fixture README, SPINE_WORKER_STUB_SAT020 stub sequence (2× step_completed, file-scope touch, stderr + hang), and stall-sat020-integration.test.mjs asserting checkpoint_warning → stall_killed → salvage_inspection → task.failed with non-empty output, worker log path, salvage/diagnose hints, and .DONE regression. Sub-minute test config only; defer autoCommitOnStall retry and mixed-outcome regression to later steps. Keep Step 1 commit scoped; record this plan review in STATUS."
}
```
