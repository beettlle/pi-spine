## Plan Review: Step 1 — Fixture + tests

### Verdict: APPROVE

### Summary

Step 1 correctly scopes fixture-only regression work for batch `20260603T224829`, mirroring the SP-085 pattern without touching engine code. The plan delivers `tests/fixtures/incidents/resume-parallel-lane-orphan.json` (batch-state + journal tail redacted to `DEAD_PID` / `999999999`) and extends `tests/batch/orphan-reconcile.test.mjs` with two focused tests: a static snapshot asserting five `running` tasks on lane 1 (SAT-039, SAT-040, SAT-042, SAT-043, SAT-044), and an integration test that materializes the fixture and asserts reconcile diagnosis ≠ `running` with actionable `suggestedCommand`. The fixture captures the full PROMPT mission pattern — historical `task.failed` / `batch.failed`, forced `batch.resumed`, parallel lane-1 `task.started`, post-resume journal silence after heartbeats, dead `enginePid`, and multi-lane running state — and complements SP-095's narrower `resume-orphan-historical-failure.json` (Bug 1 unit fixture) with an end-to-end reconcile replay. Incident narrative (`docs/incidents/20260604-resume-parallel-lane-orphan.md`) and cross-links are correctly deferred to Step 2.

### Issues Found

None blocking.

### Missing Items

- **Step 1 commit boundary** — Land only `tests/fixtures/incidents/resume-parallel-lane-orphan.json`, `tests/batch/orphan-reconcile.test.mjs`, and task `STATUS.md`. Do not include Step 2 incident doc or operator-runbook edits in the Step 1 commit (`test(SP-098): resume parallel lane orphan fixture`).
- **Plan-before-implementation discipline** — Fixture and tests are already present in the worktree before this review. Going forward, record plan approval in STATUS before marking Step 1 complete; subsequent steps should follow the same gate.
- **Fixture differentiation from SP-095** — When Step 2 writes the incident doc, explicitly contrast `resume-orphan-historical-failure.json` (Bug 1: scoped journal window) vs `resume-parallel-lane-orphan.json` (full multi-lane parallel resume + five lane-1 running tasks). The fixture `meta.pattern` field already encodes this; the Step 2 narrative should make the split operator-visible.
- **STATUS hydration** — Add a Step 1 subsection (fixture fields, test names, commit file list) and log this review under the Reviews table before Step 1 is marked done.

### Suggestions

- **Diagnosis assertion** — PROMPT allows `engine_orphaned` or `needs_retry`; replay on current spine yields `engine_orphaned` with `spine batch retry SAT-037`. The flexible assertion is acceptable, but an optional comment in the test noting why both diagnoses are allowed (engine vs dead workerPid path) would aid future readers.
- **Forced resume signal** — Consider a one-line assertion that `fixture.batchState.resilience.resumeForced === true` in the snapshot test to document the forced multi-task resume aspect called out in the mission.
- **Helper reuse** — Reusing `loadIncidentFixture` / `materializeIncidentFixture` from the SP-085 test file is correct; no new test harness needed.
- **Verification** — Step 1 gate: `node --test tests/batch/orphan-reconcile.test.mjs` (9/9 pass on current worktree). Full suite remains Step 3 per PROMPT.
- **Dependency note** — Step 1 tests validate post-fix reconcile behavior (primarily SP-095 scoped orphan detect); SP-097 (engine crash terminal phase) affects runtime crash surfacing, not this stale-state replay. Completion criteria "fails pre-fix, passes after SP-095 + SP-097" is satisfied for reconcile diagnosis once SP-095 lands.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-098",
  "feedback": "Step 1 plan delivers resume-parallel-lane-orphan.json (batch 20260603T224829: historical failures, forced resume, parallel lane-1 starts, post-resume silence, dead enginePid, five running on lane 1) plus two orphan-reconcile tests (snapshot + actionable diagnosis). Mirrors SP-085 fixture pattern; defers incident doc to Step 2; no engine changes. Commit fixture + test file + STATUS only. Clarify SP-095 vs SP-098 fixture split in Step 2 doc. Proceed to Step 1 commit after review is logged."
}
```
