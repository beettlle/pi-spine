### Verdict: APPROVE

### Summary

The SP-540 implementation correctly promotes `npm test -- <path>` from a warning to a validation error for required-mode Size S/M contracts, while preserving warning-only behavior for Size L and optional-mode S/M. `collectNpmTestDashDashErrors` is wired into `validateContract` behind the `mode === "required"` guard, with deduplication handled by suppressing the parallel warning path for required S/M.

Tests cover unit collectors, `validateContract` integration (required S/M error, optional S/M warning, required L warning), and CLI `spine tasks validate` end-to-end behavior. Collateral fixture and CLI test updates are justified: the valid-contract fixture previously used the now-invalid pattern, and CLI tests verify preflight `tasks-validate` inheritance per Step 2. Contract test suite (24 tests) and CLI validate tests (3 tests) pass. `.DONE` and STATUS Step 4 checkboxes are complete.

```json
{"verdict":"APPROVE","feedback":"Implementation matches FR-STAB-02 / PROMPT outcomes: required S/M hard-fails on npm test -- <path>, L and optional retain warnings, preflight path covered by CLI tests. Shared helpers reduce duplication without ghost-layer overhead. Minor out-of-scope edits (FX-valid-contract fixture, tasks-validate-contract CLI tests) are necessary collateral, not scope creep. Delivery artifacts (.DONE, STATUS) present."}
```
