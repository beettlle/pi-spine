### Verdict: PASS

### Summary

SP-540 fulfills FR-STAB-02 part 1: `collectNpmTestDashDashErrors` hard-fails required-mode Size S/M contracts using `npm test -- <path>`, wired into `validateContract` behind `mode === "required"`, while Size L and optional-mode S/M retain warning-only behavior via deduplicated warning paths. All PROMPT completion criteria are met, machine contract verification passed, and delivery artifacts (`.DONE`, STATUS Complete) are present.

Contract tests (24/24) and CLI `tasks-validate` tests (3/3) pass independently. Collateral edits to `FX-valid-contract` fixture and `tasks-validate-contract.test.mjs` are justified: the fixture previously used the now-invalid pattern, and CLI tests verify preflight `tasks-validate` inheritance per Step 2.

```json
{"verdict":"PASS","feedback":"Required S/M npm test -- <path> promoted to validation error; L and optional retain warnings; preflight tasks-validate path covered by CLI tests and validatePrompt → validation.errors wiring. Contract testCommand and machine verifier green. Step 4 delivery complete."}
```
