## Code Review: Step 2 — Reconcile integration + tests

### Verdict: APPROVE

### Summary

Step 2 delivers the deferred regression surface for SP-111 with no `reconcile.mjs` edits, which is correct: Step 1's third `detectOrphanRunning` branch already maps to `kind: "engine"`, and existing reconcile routing (`orphanRunning` → `engine_orphaned` + `spine batch retry <taskId>`) covers PID-less signals. The new work is two untracked files (`tests/batch/orphan-pidless-ghost.test.mjs`, `tests/fixtures/incidents/pidless-ghost-running.json`) that together satisfy the Step 2 gate: a faithful searchATon `20260603T224829` fixture (running phase, no `enginePid`/`workerPid`, `resilience.engineStartedAt` + `resumeForced`, historical pre-resume terminal noise, post-resume `batch.resumed` + `task.started` stall), unit coverage for `journalIndicatesStalledSession` and `detectOrphanRunning`, an end-to-end reconcile assertion (`engine_orphaned`, not `running`, retry command + headline), and an SP-095 guard test proving terminal events inside the scoped window suppress the signal.

Verification: `node --test tests/batch/orphan-pidless-ghost.test.mjs` — 4/4 pass.

### Issues Found

None blocking.

### Missing Items

- **Commit boundary** — Step 2 files remain untracked; land them in a Step 2 commit (`feat(SP-111): orphan detect for PID-less ghost running` per PROMPT, or equivalent) before Step 3 full-suite gate.
- **STATUS hydration** — Add Step 2 completion notes (fixture path, test file, 4-test summary, reconcile-no-op confirmation) and log this review under a Reviews table.

### Suggestions

- **SP-095 negative: missing `engineStartedAt`** — Step 1 review suggested a unit test asserting `engineStartedAt == null` → `detectOrphanRunning` returns `null`; still worth adding in Step 3 or a small follow-up to document the detached-resume boundary explicitly.
- **Fixture catalog** — Add `pidless-ghost-running.json` to `tests/fixtures/incidents/README.md` for discoverability (mirrors `orphan-running-resume.json` entry).
- **Helper consistency** — `materializeIncidentFixture` duplicates the inline journal path used in `orphan-detect-scope.test.mjs`; optional refactor to `journalPath()` from `journal.mjs` (as in `orphan-reconcile.test.mjs`) — not required for correctness.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-111",
  "feedback": "Step 2 adds pidless-ghost-running.json fixture and orphan-pidless-ghost.test.mjs (4 tests: stalled-session unit, detectOrphanRunning unit, reconcile engine_orphaned integration, terminal-suppression negative). No reconcile.mjs changes needed. All tests pass. Commit untracked files and update STATUS before Step 3 full suite."
}
```
