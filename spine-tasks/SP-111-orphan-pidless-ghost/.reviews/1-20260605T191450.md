## Plan Review: Step 1 — Detection logic

### Verdict: APPROVE

### Summary

Step 1 correctly closes SP-106 Finding #1 by adding a third `detectOrphanRunning` branch for the PID-less ghost class: `phase === "running"`, running tasks present, no finite `enginePid` or lane `workerPid`, `resilience.engineStartedAt` set, and scoped post-resume journal shows session start (`batch.resumed` / `task.started`) without terminal closure. The plan reuses SP-095's `journalEventsSinceResume` scoping and maps the signal to existing `kind: "engine"` with `enginePid: null`, which reconcile already routes to `engine_orphaned` — satisfying PROMPT's "engine_orphaned or new kind" without taxonomy churn (SP-115 remains separate). New helpers `journalIndicatesStalledSession`, `SESSION_START_JOURNAL_TYPES`, and `hasFiniteWorkerPidForRunningTasks` stay confined to `src/batch/orphan-detect.mjs` per Step 1 scope; fixture and reconcile tests are correctly deferred to Step 2 even though they already exist in the worktree.

### Issues Found

None blocking.

### Missing Items

- **Step 1 commit boundary** — Land only `src/batch/orphan-detect.mjs` and task `STATUS.md` in the Step 1 commit (`feat(SP-111): PID-less ghost orphan detect signal`). Do not include `tests/batch/orphan-pidless-ghost.test.mjs`, `tests/fixtures/incidents/pidless-ghost-running.json`, or reconcile changes in Step 1 (Step 2 deliverables).
- **Plan-before-implementation ordering** — Detection logic, fixture, and tests are present in the worktree before this review. Going forward, record plan APPROVE in STATUS before marking Step 1 complete; Step 2 should follow the same gate for code review.
- **STATUS hydration** — Add a Step 1 subsection (new exports, third-signal predicate summary, commit file list) and log this review under a Reviews table before Step 2 proceeds.

### Suggestions

- **SP-095 guardrails** — The `engineStartedAt != null` gate correctly prevents firing outside a detached/resumed engine session; add a Step 2 negative unit test (missing `engineStartedAt` → `null` signal) to document the SP-095 boundary alongside the existing terminal-suppression test.
- **Session-start breadth** — `journalIndicatesStalledSession` treats `batch.resumed` alone as sufficient stall evidence; acceptable for the searchATon `20260603T224829` pattern (resume + `task.started` + silence). If false positives appear during legitimate resume-before-start windows, tighten to require `task.started` in a follow-up — not blocking for this incident class.
- **Reconcile path** — No `reconcile.mjs` edits required for Step 1; existing `engine_orphaned` mapping and `spine batch retry <taskId>` suggested command cover PID-less engine signals once detection returns non-null.
- **Verification** — Step 1 has no test gate in PROMPT; optional smoke: import and call `detectOrphanRunning` against scoped fixture events. Step 2 gate: `node --test tests/batch/orphan-pidless-ghost.test.mjs` (4/4 pass on current worktree). Full suite remains Step 3.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-111",
  "feedback": "Step 1 plan: third detectOrphanRunning branch for PID-less ghost running (phase running, no engine/worker PIDs, engineStartedAt present, scoped post-resume journal stall via journalIndicatesStalledSession); map to existing engine kind → engine_orphaned. Reuses SP-095 journalEventsSinceResume; no reconcile/diagnosis changes. Commit orphan-detect.mjs + STATUS only; defer fixture + orphan-pidless-ghost.test.mjs to Step 2. Record plan review in STATUS; add negative engineStartedAt test in Step 2."
}
```
