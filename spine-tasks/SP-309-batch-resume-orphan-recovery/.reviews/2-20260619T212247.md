### Verdict: APPROVE

### Summary

Step 2 adds `tests/batch/resume-orphan-recovery.test.mjs` with six focused cases that reproduce the batch `20260619T020951` journal pattern (`review.failed` → `batch.resumed` → `worker.rules_selected` stall), validate `engine_orphaned` reconciliation with attached-resume guidance, and exercise end-to-end `resumeBatch({ force: true })` until the stuck task reaches `succeeded`. Verification commands from the contract pass in this worktree: typecheck succeeds, `SPINE_WORKER_STUB=1 npm test` reports 965/965 pass, and `npm run coverage:check` reports 87.28% line coverage (threshold 77%). Changes stay within File Scope (`tests/batch/` only for this step).

```json
{"verdict":"APPROVE","feedback":"Regression test file meets PROMPT Step 2 and contract requirements. The seed fixture mirrors issue #13 (dual-dead engine/worker PIDs, plan review failure, post-resume rules_selected stall). Unit tests cover orphan handoff helpers, stale PID clearing, reconcile diagnosis, and full resume-to-success. All contract verification commands pass locally (typecheck, full test suite, coverage gate). Minor non-blocking notes: the final timeout assertion duplicates the first test; the integration test exercises resumeBatch in-process rather than the CLI --attached flag directly, which is acceptable given multi-task resume runs attached engine logic via resumeMultiTaskBatch."}
```
