## Code Review: Step 3 — Testing & Verification

### Verdict: APPROVE

### Summary

Step 3 verification gates pass. The full test suite (`npm test`) completes **569/569** with no failures, and `npm run coverage:check` meets the project threshold at **83.68%** line coverage (≥ 77%). SP-117 changes in commits `51eb681` (symmetric persistence) and `34684e6` (orphan timeout regression tests) satisfy both completion criteria: `startBatchDetached` now persists `enginePid` immediately after spawn (mirroring `resumeBatchDetached` at line 639), and the new orphan-timeout tests confirm dead-engine reconcile yields `engine_orphaned` rather than `running`.

Working-tree `git diff` is limited to an auto-regenerated `.spine/rules-manifest.json` timestamp — not SP-117 implementation. Task code was reviewed from branch commits and verified by execution.

### Build / typecheck / coverage

| Gate | Command | Result |
|------|---------|--------|
| Typecheck | `npm run typecheck` | Pass |
| Full suite | `npm test` | Pass (**569/569**) |
| Coverage gate | `npm run coverage:check` | Pass — **83.68%** line (threshold 77%) |
| Changed module | coverage report | `detached-start.mjs` **84.70%** line |
| SP-117 tests | `tests/batch/detached-start-orphan-timeout.test.mjs` | Pass (2/2) |
| Related regression | `tests/batch/detached-start.test.mjs` | Pass (included in full suite) |

**Note:** First two `coverage:check` attempts failed on an unrelated flaky dashboard CLI startup test (`tests/dashboard/cli-startup.test.mjs:95` — 3s banner timeout under load). Third attempt passed cleanly (`fail 0`). Consider bumping that test timeout in a follow-up to reduce CI flake.

### Completion criteria (PROMPT)

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Start and resume persist enginePid symmetrically | Met | `persistDetachedEnginePid` moved to line 548 immediately after `spawnDetachedBatchEngine`, before `waitForDetachedBatchStart`; resume path unchanged at line 639 |
| Timeout orphan test passes | Met | Two tests in `detached-start-orphan-timeout.test.mjs`: dead-PID reconcile fixture + integration path via `startBatchDetached` timeout with persisted spawn PID |
| Step 3: FULL suite + coverage gate | Met | Both commands exit 0 on verification run |

### Code review (Steps 1–2)

**Step 1 — Symmetric persistence:** Minimal, correct one-line relocation. Previously `persistDetachedEnginePid` ran only on the success path after wait; orphan race left no PID in state when `timeout_waiting_for_batch` returned. Moving persistence before wait matches resume semantics and closes SP-106 Finding #5/#12.

**Step 2 — Regression tests:** Well-scoped fixtures using existing helpers (`initGitRepo`, `createInitialBatchState`, `reconcileBatch`). Test 1 validates reconcile with a known-dead PID (`999_999_999`). Test 2 exercises the full start path with a fake spine that exits quickly and `waitTerminal: true`, asserting PID persistence before wait and subsequent `engine_orphaned` diagnosis. Test 2 runtime (~32s) is expected given the default 30s wait timeout.

### Issues Found

None blocking.

### Suggestions (non-blocking)

1. **Test 1 scope** — The first test validates reconcile with a manually seeded dead PID but does not invoke `startBatchDetached` or produce `timeout_waiting_for_batch` directly. Test 2 covers the integration path; consider a brief comment linking the two tests to the audit finding for future readers.
2. **Test 2 duration** — Consider injecting a shorter `timeoutMs` into `startBatchDetached` / `waitForDetachedBatchStart` for test speed if the API already supports it (optional; current runtime is acceptable).
3. **Dashboard flake** — `tests/dashboard/cli-startup.test.mjs` timed out under coverage+stub load on 2/3 runs. Unrelated to SP-117 but may cause intermittent Step 3 gate failures; increase timeout or add retry in CI.

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "SP-117",
  "feedback": "Step 3 verification complete: full suite 569/569, coverage gate 83.68%, typecheck pass. SP-117 completion criteria met — start path persists enginePid immediately after spawn (symmetric with resume), orphan timeout tests pass and reconcile dead engines as engine_orphaned. Non-blocking: dashboard CLI test flaked twice under coverage load; consider shorter timeoutMs in orphan integration test and bump dashboard startup test timeout for CI stability."
}
```
