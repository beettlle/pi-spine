## Plan Review: Step 1 — Symmetric persistence

### Verdict: APPROVE

### Summary

Step 1 correctly closes SP-106 Finding #5 at the call-site level by moving `persistDetachedEnginePid(projectRoot, enginePid)` to immediately after `spawnDetachedBatchEngine` in `startBatchDetached`, mirroring `resumeBatchDetached` (lines 639–640). The plan removes the post-wait success-only persist (previously ~line 581), confines changes to `src/batch/detached-start.mjs` per File Scope, and defers the `timeout_waiting_for_batch` + dead-engine reconcile regression to Step 2 per PROMPT. Reusing the existing `persistDetachedEnginePid` helper avoids new abstractions and respects the Do NOT constraint against splitting `detached-start.mjs`.

### Issues Found

None blocking.

### Missing Items

- **Plan-before-implementation ordering** — Commit `51eb681` landed Step 1 before this plan review. Going forward, record plan APPROVE in STATUS before marking Step 1 complete; Step 2 should follow the same gate for plan and code reviews (Review Level 2).
- **STATUS hydration** — Add a Reviews table entry for `.reviews/1-20260605T204408.md` and a short Step 1 plan blurb (move persist before wait, remove success-path duplicate) before proceeding to Step 2.
- **Commit message convention** — PROMPT Git Commit Convention specifies `fix(SP-117): …`; Step 1 used `feat(SP-117): …`. Non-blocking for this step; align Step 2/3 commits with the convention.

### Suggestions

- **Fresh-start no-op guard** — `persistDetachedEnginePid` returns early when batch-state is absent (`if (!raw) return` at `detached-start.mjs:270–271`), so pre-wait persist on a greenfield `start` is often a no-op until the child engine creates state. Resume symmetry is functional because state pre-exists; start symmetry is structural at the call site. If Step 2's `timeout_waiting_for_batch` + dead-engine reconcile test still sees plain `running`, add a bounded failure-path persist (e.g. when `!wait.ok && enginePid`) without widening Step 1 scope now.
- **Step 2 test design** — Fixture should assert `reconcileBatch` ≠ `running` with `phase: running`, dead parent-spawned PID, and no terminal journal — matching Finding #12. Stub or inject parent PID into batch-state after the wait loop observes active phase if pre-spawn persist alone is insufficient.
- **Step 1 commit boundary** — Land only `src/batch/detached-start.mjs` and task `STATUS.md` in the Step 1 commit; defer `tests/batch/detached-start-orphan-timeout.test.mjs` to Step 2.
- **Verification** — Step 1 has no test gate in PROMPT; existing `tests/batch/detached-start.test.mjs` happy-path enginePid assertion remains valid. Step 3 owns FULL suite + coverage gate.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-117",
  "feedback": "Step 1 plan: move persistDetachedEnginePid to immediately after spawnDetachedBatchEngine in startBatchDetached (mirror resumeBatchDetached), remove post-wait success-only persist; scope limited to detached-start.mjs. Defer timeout+dead-engine reconcile test to Step 2. Note persistDetachedEnginePid no-op when batch-state absent on fresh start—Step 2 may need failure-path persist if reconcile still returns running. Record plan review in STATUS before Step 2; align future commits with fix(SP-117) convention."
}
```
