## Code Review: Regression test

### Verdict: APPROVE

### Summary

Step 2 adds `tests/batch/detached-start-orphan-timeout.test.mjs` with two focused regression tests that satisfy the PROMPT requirement: after a detached-start timeout scenario with a dead engine, reconcile must not report `running`. The static dead-`enginePid` fixture mirrors the established resume orphan pattern in `orphan-reconcile.test.mjs`, and the integration test exercises the real `startBatchDetached` timeout failure path while asserting immediate PID persistence and downstream `engine_orphaned` diagnosis. Both tests pass locally; incidental doc test-count updates are consistent with the new coverage.

### Findings

**Strengths**

- Core regression is well covered: `assert.notEqual(result.diagnosis, "running")` plus `engine_orphaned`, `suggestedCommand`, and headline checks on the fixture test.
- Integration test closes the loop on the Step 1 fix — verifies `timeout_waiting_for_batch`, persisted spawn PID, dead process, and reconcile diagnosis in one flow.
- Follows existing batch test conventions (`initGitRepo`, `createInitialBatchState`, journal seeding, `destroyGitRepo` cleanup).

**Non-blocking notes**

- Test 1 title references `timeout_waiting_for_batch`, but the fixture simulates post-timeout state via `DEAD_PID` rather than invoking the timeout error; test 2 covers the actual timeout path, so combined coverage is complete.
- Test 2 runs ~31s because of the default detached-start wait timeout; acceptable unless a shorter test hook becomes available later.
- The new test file is still untracked — include it in the Step 2 commit.

```json
{
  "verdict": "APPROVE",
  "feedback": "Regression tests meet Step 2: dead engine after detached-start timeout scenario reconciles as engine_orphaned, not running. Static fixture plus startBatchDetached integration test provide solid coverage aligned with existing orphan-reconcile patterns. Both tests pass."
}
```
