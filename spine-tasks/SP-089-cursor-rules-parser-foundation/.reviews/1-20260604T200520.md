## Plan Review: Parser + profile schema

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` is sound: a minimal frontmatter parser scoped to `alwaysApply`, `description`, and `globs`; a versioned `.spine/rules-profile.json` loader with frozen built-in defaults; `micromatch` declared for SP-091; and fixtures covering the three real repo glob shapes (comma-separated string, JSON-style array, `[]` / missing key). Splitting defaults into `worker` (always-include + `globMatch`) and `discovery` (`excludePatterns` / `excludeRelPaths`) matches SP-090’s planned `discoverCursorRules` contract and correctly maps PROMPT’s exclusion list (`*-brutal-audit` → micromatch pattern; named rules/templates → `excludeRelPaths`). Scope stays inside File Scope with no directory walk, CLI, or prompt injection.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| `micromatch` dependency | Declare in `package.json` only (no usage in SP-089) — correct per PROMPT |
| `parseCursorRuleFrontmatter` | `---` fence, key-value subset, `skip` without fence, `warn` for recoverable issues — matches sampled `.mdc` files |
| `loadRulesProfile` | Built-in defaults when missing; `RULES_PROFILE_INVALID` on bad JSON; `neverInclude` wins over `alwaysInclude` on merge |
| Fixtures | Six shapes under `tests/fixtures/cursor-rules/` cover array, comma, empty/missing globs, `alwaysApply`, no frontmatter |
| Do NOT | No `discoverCursorRules`, worker injection, or `spine rules` CLI |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-089-cursor-rules-parser-foundation/STATUS.md`** — Add a short Step 1 plan blurb and tick checkboxes as artifacts land; log parser/profile API shapes in Discoveries during Step 4 for SP-090.
2. **Step boundaries** — Unit tests already exist under `tests/config/cursor-rules/`; keep Step 2 as the formal verification pass (`npm test`, coverage ≥77%, any gap cases such as invalid `alwaysApply` → `warn`).
3. **Step 4 Discoveries** — Document that `globs` arrays are parsed via JSON bracket syntax (not full YAML), which matches current repo rules.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan is aligned with PROMPT outcomes, repo .mdc frontmatter samples, and SP-090’s profile.discovery shape. Proceed with exports/fixtures completion, then Step 2 verification and coverage."
}
```
