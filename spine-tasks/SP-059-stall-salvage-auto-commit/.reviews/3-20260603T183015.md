## Plan Review: Step 3 — Retry semantics + docs

### Verdict: APPROVE

### Summary

Step 3 correctly closes **FR-STALL-03.10** as a **documentation-only** step: no `retry.mjs` or salvage code changes are required because atomic retry already resets batch state only (`resetTaskAndSegments` → `saveSpineBatchState`) and never touches lane worktrees or git history. The planned edits add one normative paragraph under **PRD §18.5** in both `docs/PRD.md` and `pi-spine-PRD.md` stating that when `lanes.autoCommitOnStall` is enabled, salvage may create a scoped `wip(<taskId>): stall salvage <iso>` commit on the **lane branch**, and that `spine batch retry` / atomic retry resets task + segment records **without** reverting that WIP — the lane worktree retains salvage work for review and re-run. The operator runbook gains a **Stall salvage WIP** row (config default, journal `lane.salvage_commit`, refusal conditions, retry retains WIP, PRD §18.5 cross-reference) and tightens the **Worker stall** row to point operators at `spine status --diagnose`, worker log, and `lane.salvage_inspection` before optional WIP commit. This matches PROMPT Step 3 checkboxes, the stall-recovery brief Phase B table, and the behavioral contract already implied by `retryTask` + lane worktree reuse on resume.

### Issues Found

None blocking for Step 3 plan approval.

### Missing Items

- **Step 3 commit boundary** — Keep the Step 3 commit limited to `docs/PRD.md`, `pi-spine-PRD.md`, and `docs/adoption/operator-runbook.md`. Do **not** include `tests/batch/salvage-auto-commit.test.mjs` (including `atomic retry retains salvage WIP commit on lane branch`) in the Step 3 commit; that file belongs to PROMPT Step 4 per Step 2 plan review, even if already written on disk.
- **STATUS discipline** — Hydrate a Step 3 subsection (files touched, link to this review, note docs-only / no `retry.mjs` diff) and avoid treating Steps 3–4 as fully complete until Step 3 docs commit and Step 4 code review land.
- **Trailing whitespace in PRD** — Uncommitted §18.5 edits add an extra blank line before EOF in `docs/PRD.md` / `pi-spine-PRD.md`; trim when committing.

### Suggestions

- **Plan-before-implementation ordering** — Doc edits are already present in the worktree before this review (same pattern as Steps 1–2). Land the Step 3 commit after this APPROVE; do not fold tests into the same commit.
- **Command naming consistency** — Runbook uses `spine batch retry`; PRD §18.5 heading uses `/spine-retry-task`. Both are valid; optional polish: parenthetical `(also /spine-retry-task)` in the runbook row for operators using pi slash commands.
- **§20.2 salvage bullet (optional)** — Wave mixed-outcome bullet 4 (“uncommitted progress”) could add a half-sentence cross-link to §18.5 when `autoCommitOnStall` may have committed scoped WIP; not required to satisfy FR-STALL-03.10.
- **`SETTINGS_FIELDS` carry-forward** — Step 1/2 reviews noted `lanes.autoCommitOnStall` is JSON-validated but not in `settings-fields.mjs`; runbook already documents JSON edit — out of Step 3 scope unless a one-line “edit `.spine/spine-config.json`” pointer is desired.
- **Step 4 test matrix** — Confirm `atomic retry retains salvage WIP` asserts `retryTask` leaves lane `HEAD` unchanged while batch segments return to `pending`; keep enabled/disabled salvage commit integration in the same test file.

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "SP-059",
  "feedback": "Step 3 plan: docs-only FR-STALL-03.10 — add PRD §18.5 paragraph (autoCommitOnStall WIP on lane branch; atomic retry does not revert WIP) in docs/PRD.md + pi-spine-PRD.md; operator-runbook stall salvage WIP row + worker stall diagnosis pointer with §18.5 cross-ref. No retry.mjs/salvage code changes. Step 3 commit = three doc files only; defer tests/batch/salvage-auto-commit.test.mjs to Step 4. Proceed with Step 3 docs commit after plan review; then Step 4 tests + code review."
}
```
