## Code Review: Step 4 — Tests

### Verdict: APPROVE

### Summary

Step 4 delivers the PROMPT-mandated test file `tests/batch/salvage-auto-commit.test.mjs` (7 tests, currently untracked) plus a STATUS hydration marking Step 4 complete. The suite covers all three Step 4 checkboxes: **enabled** path commits only scoped dirty paths on the lane branch with `wip(<taskId>): stall salvage <iso>` message and `lane.salvage_commit` journal; **disabled** path leaves `HEAD` unchanged and emits no `lane.salvage_commit` (SP-058 salvage-inspection behavior preserved); and the **full suite** is green.

Tests follow the established `salvage-inspect.test.mjs` fixture pattern (`initGitRepo`, lane worktrees, smoke PROMPT tasks) and exercise the public salvage API surface introduced in Steps 1–2. Coverage extends beyond the minimum with config default/validation unit tests, refusal gates (`merge_in_progress`, `index_conflicts`, `hook_or_commit_failed`), and the Step 3 atomic-retry contract (`retryTask` leaves lane `HEAD` at the salvage WIP SHA).

Independent verification in this worktree:

- `node --test tests/batch/salvage-auto-commit.test.mjs` — **7/7 pass** (~10s)
- `SPINE_WORKER_STUB=1 npm test` — **352/352 pass** (~119s)

### Issues Found

None blocking.

### Missing Items

- **Step 4 commit boundary** — Land `tests/batch/salvage-auto-commit.test.mjs`, `spine-tasks/SP-059-stall-salvage-auto-commit/STATUS.md`, and this review artifact in the Step 4 commit. Do not fold unrelated doc or implementation edits from earlier steps.
- **Untracked test file** — `git diff` shows only STATUS; the new test file must be explicitly added before commit.

### Suggestions

- **Evidence payload (optional)** — The enabled integration test asserts journal `lane.salvage_commit` but not `evidence/salvage-<taskId>.json` `salvageCommit` field (SP-058 inspect test covers evidence for the no-commit path). A one-line `JSON.parse` assertion on `salvageCommit.committed` would close the loop without an e2e test.
- **Refusal journal via `recordTaskFailureSalvage` (optional)** — Refusal tests call `attemptScopedSalvageCommit` directly; a follow-up could assert `lane.salvage_commit` with `refused: true` when `autoCommitOnStall: true` and `batchPhase: "merging"` flows through `recordTaskFailureSalvage`.
- **Helper duplication** — `execCommit` / `writeSmokeTask` duplicate `salvage-inspect.test.mjs`; acceptable and consistent with nearby batch tests; extract to a shared helper only if a later refactor touches both files.
- **E2e stub path (optional, out of scope)** — `salvage-inspect.test.mjs` includes a `startBatch` stall e2e for default (no auto-commit) config; an analogous stub test with `lanes.autoCommitOnStall: true` in spine-config would exercise worker-host wiring end-to-end — not required to satisfy Step 4 PROMPT.

```json
{
  "verdict": "APPROVE",
  "step": 4,
  "taskId": "SP-059",
  "feedback": "Step 4 tests satisfy PROMPT: enabled scoped WIP commit on lane branch + lane.salvage_commit journal, disabled no-commit SP-058 behavior, safety refusals (merge/index/hook), atomic retry retains WIP SHA, config default/validation. Full suite 352/352 green. Commit tests/batch/salvage-auto-commit.test.mjs + STATUS + review; optional follow-ups: evidence salvageCommit assertion, recordTaskFailureSalvage refusal journal integration."
}
```
