## Plan Review: Step 2 — WIP commit implementation

### Verdict: APPROVE

### Summary

Step 2 correctly closes FR-STALL-03B Phase B on top of SP-058 inspection: when `resolveSalvageConfig` yields `autoCommitOnStall === true` and scoped inspection is salvageable, `recordTaskFailureSalvage` calls `attemptScopedSalvageCommit` (lane `checkout`, `git add -- <dirtyPaths>`, single `wip(<taskId>): stall salvage <iso>` commit), journals `lane.salvage_commit` with stable `committed` / `refused` / `reason` / `commitSha` / `message` / `error` plus `dirtyPaths`, persists `salvageCommit` on evidence, and returns `salvageCommitted` / `salvageCommitSha` / `salvageCommitRefused` / `salvageCommitReason` for `task.failed` spread. `lane.salvage_inspection` always precedes commit; `lane.salvage_commit` is emitted only when the feature is enabled and an attempt runs (not when default-off or non-salvageable). `journal.mjs` summarize/diagnosis updates belong in this step. No `worker-host.mjs` change is required — engine/resume callers already pass `config`, `batchPhase`, and `taskBranch`.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Step boundary vs Step 1 commit** — Commit `1ce09dc` already landed the Step 2 integration (`recordTaskFailureSalvage` auto-commit branch, evidence `salvageCommit`, `lane.salvage_commit`) together with Step 1 gates. Treat remaining Step 2 work as a **narrow follow-up commit**: `src/batch/journal.mjs` summarize + `extractJournalDiagnosisHints` for `lane.salvage_commit` only; do not re-touch config validation or gate helpers in the Step 2 commit message.
- **Tests and docs out of Step 2 scope** — `tests/batch/salvage-auto-commit.test.mjs` and PRD/runbook §18.5 edits belong in PROMPT Steps 4 and 3 respectively; keep them out of the Step 2 commit even if already written on disk.
- **STATUS discipline** — Hydrate a Step 2 subsection (integration points, `lane.salvage_commit` payload fields, Step 2 commit file list, link to this review) and avoid pre-marking Steps 3–4 complete before their plan/code reviews land.

### Suggestions

- **Plan-before-implementation ordering** — Step 2 logic shipped inside the Step 1 commit before this review (same pattern as TP-047/TP-048). Going forward, land Step 2 journal polish after plan APPROVE; if integration stays in `1ce09dc`, document that in STATUS rather than re-diffing `salvage.mjs`.
- **Refusal visibility** — When enabled and salvageable but gates refuse (`merge_in_progress`, `index_conflicts`, `hook_or_commit_failed`), still append `lane.salvage_commit` with `refused: true` and `reason` (already planned); operators rely on inspection + commit events in sequence.
- **Missing `taskBranch`** — If `autoCommitOnStall` is true but `taskBranch` is absent, skip commit attempt and `lane.salvage_commit` silently; document in STATUS as a caller contract (engine/resume must pass branch).
- **`SETTINGS_FIELDS`** — Carry forward from Step 1 review: add `lanes.autoCommitOnStall` to `src/config/settings-fields.mjs` or document JSON-only edit; not required to block Step 2 implementation.
- **Step 4 test matrix** — Reserve `recordTaskFailureSalvage` enabled/disabled integration, refusal paths, and atomic retry retains WIP for Step 4; unit tests on `attemptScopedSalvageCommit` may already exist from Step 1.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-059",
  "feedback": "Step 2 plan: wire recordTaskFailureSalvage auto-commit when autoCommitOnStall and salvageable, scoped git add on lane branch, lane.salvage_commit journal with stable refusal reasons, evidence salvageCommit, return fields for task.failed; journal.mjs summarize/diagnosis for salvage_commit. Step 2 commit = journal polish only if integration is already in 1ce09dc; defer tests/batch/salvage-auto-commit.test.mjs to Step 4 and PRD/runbook to Step 3. No worker-host change. Proceed with Step 2 journal commit after plan review; then Step 3 docs and Step 4 tests."
}
```
