## Plan Review: Discovery engine

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` is sound: recursive scan of `.cursor/rules/**/*.{mdc,md}` with hard caps (200 files, 512 KiB per file), posix `relPath` under the rules root, profile-driven exclusions via `micromatch` (`excludePatterns`) and exact `excludeRelPaths`, `spineClass` derived from parsed frontmatter (`alwaysApply` → `always`; non-empty `globs` → `glob`; else `manual`; excluded files land in `excluded[]` with `spineClass: "excluded"`), and atomic JSON write to `.spine/rules-manifest.json` (`generatedAt`, `rulesRoot`, `rules`, `excluded`, optional top-level `warnings`). The plan correctly consumes SP-089’s `parseCursorRuleFrontmatter` and `loadRulesProfile` / `DEFAULT_RULES_PROFILE`, keeps `micromatch` limited to discovery exclusions (not PROMPT File Scope), and defers `templates/rules-profile.json`, committed pi-spine manifest, and `.gitignore` confirmation to Step 2 per file scope and Do NOT boundaries.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| `discoverCursorRules({ projectRoot, profile, writeManifest })` | Optional injected `profile`; otherwise `loadRulesProfile`; `writeManifest` gates atomic write |
| Caps + posix paths | `DISCOVER_MAX_FILES` / `DISCOVER_MAX_FILE_BYTES`; sorted recursive collect; `toPosixRelPath` on rule `relPath` |
| Profile exclusions | `getCursorRuleExclusionReason`: `excludeRelPaths` set first, then `micromatch.isMatch` on relPath, basename, and stem |
| `spineClass` | `classifyCursorRuleSpineClass` with always-over-globs precedence; excluded entries never parsed into `rules[]` |
| Manifest shape | Atomic tmp+rename write; `parseStatus` / per-entry `warnings` preserved for SP-091 consumers |
| Do NOT | No `ruleGlobsMatchFileScope`, `selectRulesForWorker`, or worker-runner wiring |
| Step boundaries | Step 1 = engine + exports; Step 2 = template + committed manifest; Step 3 = full suite + coverage gate |

### Suggestions (non-blocking)

1. **`STATUS.md`** — After code review, tick Step 1 checkboxes and note plan review APPROVE; reserve Step 3 for full-suite + `coverage:check` even though discover unit tests already exist.
2. **Invalid on-disk profile** — When `profile` is omitted and `loadRulesProfile` returns `RULES_PROFILE_INVALID`, document whether discover should fail closed or fall back to `DEFAULT_RULES_PROFILE` (current implementation falls back); log the choice in Step 4 Discoveries for operators.
3. **Step 4 Discoveries** — Publish manifest entry fields (`relPath`, `spineClass`, `alwaysApply`, `description`, `globs`, `parseStatus`, `warnings?`) and `excluded[]` `{ relPath, reason, spineClass }` for SP-091/093.
4. **Step 3 tests** — Add an integration case for `excludeRelPaths` (e.g. `audit-workflow.mdc`) and nested `taskplane/*.md` paths if not already covered at discover level (helper test exists for `getCursorRuleExclusionReason`).

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan matches PROMPT outcomes, SP-089 parser/profile contracts, and CONTEXT product decisions (committed manifest deferred to Step 2). Exclusions and classification stay read-only and out of SP-091/092 scope. Proceed to Step 2 template + manifest commit after code review."
}
```
