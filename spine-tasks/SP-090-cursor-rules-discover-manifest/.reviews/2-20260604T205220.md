# Code Review — Step 2: Init template + pi-spine manifest

**Task:** SP-090 — Cursor rules discover + manifest  
**Baseline:** `801715b..8c7a7f5` (`feat(SP-090): complete Step 2 — init template and manifest`)  
**Worktree:** `/Users/cdelgado/Documents/github.com/pi-spine/.worktrees/spine-20260604T204341/lane-1`  
**Files reviewed:** `templates/rules-profile.json`, `.spine/rules-manifest.json`, `spine-tasks/SP-090-cursor-rules-discover-manifest/STATUS.md` (`.gitignore` checked for manifest tracking)

### Verdict: APPROVE

### Summary

Step 2 delivers the init template and a committed pi-spine manifest within PROMPT file scope. `templates/rules-profile.json` mirrors `DEFAULT_RULES_PROFILE` from SP-089 (`taskplane-worker-cursor.mdc` in `alwaysInclude`, `globMatch: true`, brutal-audit and template exclusions). `.spine/rules-manifest.json` is tracked in git, not listed in `.gitignore`, and enumerates 27 active rules plus 11 excluded entries (38 files under `.cursor/rules/`), consistent with a rediscovery pass using the default profile.

Working tree had no unstaged diff at review time; changes were reviewed via `git diff 801715b..8c7a7f5` (Step 2 commit only).

### Requirements coverage

| PROMPT Step 2 item | Status |
|--------------------|--------|
| `templates/rules-profile.json` with worker + discovery fields | ✅ Matches built-in defaults |
| `taskplane-worker-cursor.mdc` in `alwaysInclude` | ✅ |
| `globMatch: true` | ✅ |
| Run discover on pi-spine; commit manifest | ✅ `generatedAt`, `rulesRoot`, `rules[]`, `excluded[]` |
| Manifest tracked (not gitignored) | ✅ `git ls-files`; `.gitignore` only ignores runtime/batch state |

### Strengths

- **Profile/template parity** — Template JSON is byte-for-byte aligned with `DEFAULT_RULES_PROFILE` in `profile.mjs`, so `spine init` copy (SP-093) will not surprise consumers relative to current dogfood behavior.
- **Exclusion fidelity** — Manifest `excluded[]` reasons (`excludePattern` vs `excludeRelPath`) match profile rules; all `*-brutal-audit` and named paths from PROMPT/SP-089 are absent from `rules[]`.
- **Worker rule placement** — `taskplane-worker-cursor.mdc` appears in `rules[]` as `spineClass: "manual"` (frontmatter-driven), while profile `alwaysInclude` reserves injection behavior for SP-092 — correct separation.
- **Reproducibility** — Rediscovery with `DEFAULT_RULES_PROFILE` yields identical `rules` and `excluded` arrays to the committed manifest (only `generatedAt` differs).

### Non-blocking suggestions

1. **`STATUS.md`** — After this review, mark Step 2 complete and record code review APPROVE (mirror Step 1 plan review line); add a short note that pi-spine used built-in defaults (no `.spine/rules-profile.json` yet) and that the template is the init source of truth.
2. **Step 3** — Add a discover integration test that loads `templates/rules-profile.json` (or copies it into a temp `.spine/`) and asserts manifest shape/counts, so committed manifest drift is caught in CI.
3. **`generatedAt`** — Expect timestamp churn on re-run; Step 3 round-trip tests should compare semantic fields, not the ISO timestamp.

### Verification performed

```text
git ls-files .spine/rules-manifest.json templates/rules-profile.json  → both tracked
find .cursor/rules → 38 files; manifest rules 27 + excluded 11 = 38
node rediscover (DEFAULT_RULES_PROFILE) vs committed rules+excluded      → match
```

```json
{"verdict":"APPROVE","feedback":"Step 2 adds templates/rules-profile.json aligned with DEFAULT_RULES_PROFILE, commits reproducible .spine/rules-manifest.json (27 rules, 11 excluded), and keeps the manifest out of .gitignore. Scope stays read-only; worker injection and init wiring remain deferred to SP-092/093. Record this review in STATUS before advancing to Step 3 tests and coverage."}
```
