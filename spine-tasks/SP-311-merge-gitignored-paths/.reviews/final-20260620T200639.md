### Verdict: PASS

### Summary

SP-311 meets all completion criteria and contract checks. Commit `5960d03` implements `filterGitignoredPaths` / `gitAddFilteredPaths`, merge conflict auto-resolution skips gitignored paths (with `git rm --cached` for force-added ignored entries), `commitLaneWorktree` refuses gitignored-only dirty worktrees, and `merge_failed_gitignored` diagnosis with repair `suggestedCommand` is wired through reconcile and the operator runbook. Machine verifier passed; contract test command succeeded (980 tests); issue #15 is closed; `.DONE` exists.

The only working-tree delta outside the task commit is an unstaged `.spine/rules-manifest.json` regeneration—outside File Scope and not a functional blocker. Optional follow-up: document `merge_failed_gitignored` in `docs/design/integrate-conflict-recovery.md` if that taxonomy doc is maintained in sync with diagnosis classes.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria satisfied: gitignored paths filtered before merge/lane-commit staging, lane commit surfaces GitignoredDirtyWorktree, diagnosis distinguishes merge_failed_gitignored with git rm --cached + resume --force repair, regression tests and coverage gate pass (87.26% >= 77%), issue #15 closed, .DONE present. Contract verifier PASS; npm run typecheck && SPINE_WORKER_STUB=1 npm test -- tests/batch/merge-gitignored-paths.test.mjs verified (980 pass). Non-blocking: unstaged rules-manifest.json outside file scope."}
```
