## Plan Review: Step 1 — Extract IO module

### Verdict: APPROVE

### Summary

Step 1 correctly targets SP-106 Finding #4: extract `resolveBatchStatePath`, `loadBatchStateFile`, and `parseBatchState` into a leaf `src/batch/batch-state-io.mjs` that imports only `fs`, `path`, and the existing spine/taskplane readers — breaking the `state.mjs` → `reconcile.mjs` edge of the circular dependency while leaving `reconcile.mjs` → `orphan-detect.mjs` → `state.mjs` as a one-way chain. Updating `state.mjs` to import `loadBatchStateFile` from the new module (instead of `reconcile.mjs`) satisfies the Step 1 completion criterion ("no circular import state↔reconcile") without touching batch-state schema or git inspection (Step 2). Re-exporting the three symbols from `reconcile.mjs` preserves backward compatibility for `abort.mjs`, `lifecycle.mjs`, `integrate.mjs`, `dashboard/snapshot.mjs`, and CLI entrypoints that still import via reconcile — appropriate minimal blast radius for a pure refactor step.

### Issues Found

None blocking.

### Missing Items

- **Re-export in Step 1 checklist** — PROMPT bullets only say "update imports in state + reconcile." Add an explicit artifact: `reconcile.mjs` re-exports `loadBatchStateFile`, `parseBatchState`, and `resolveBatchStatePath` from `batch-state-io.mjs` so downstream importers need not change in this step.
- **Step 1 verification gate** — Step 3 owns full suite + coverage; Step 1 should still record a lightweight gate before commit: `npm run typecheck` and existing `tests/batch/reconcile.test.mjs` (exercises load/parse via `reconcileBatch` on `.pi` fixtures). No new test file required in Step 1.
- **STATUS plan blurb** — `STATUS.md` has no Step 1 plan subsection yet; add module exports, import edges, and re-export strategy before marking the step complete (Review Level 2 hygiene).
- **Scope guard for Step 2** — Confirm Step 1 commit excludes git empty-catch changes in `inspectGitState` (`reconcile.mjs` ~129–266); those belong to Step 2 per PROMPT.

### Suggestions

- **Leaf module contract** — Document in the module header that `batch-state-io.mjs` must not import `state.mjs`, `reconcile.mjs`, or any module that transitively imports them (matches the partial implementation comment).
- **Behavior parity** — Keep `loadBatchStateFile` return shape `{ path, raw, parseError }` unchanged; the `parseError` normalization (`err instanceof Error ? err.message : String(err)`) is a safe micro-hardening if applied consistently.
- **Do not conflate loaders** — Leave `loadSpineBatchState` / `spineBatchStatePath` in `state.mjs` (spine-only active file); only the cross-format load/parse trio moves to the IO module.
- **Plan-before-implementation ordering** — Uncommitted worktree changes already mirror this plan (good alignment); going forward, land plan review APPROVE in STATUS before the Step 1 commit per Review Level 2.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-116",
  "feedback": "Step 1 plan: extract resolveBatchStatePath/loadBatchStateFile/parseBatchState to leaf batch-state-io.mjs; state.mjs imports IO module (breaks state→reconcile cycle); reconcile.mjs imports and re-exports for downstream compat. Defer git error surfacing to Step 2. Verify with typecheck + reconcile.test.mjs; record plan in STATUS before commit."
}
```
