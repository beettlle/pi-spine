## Code Review: Step 2 — Git error surfacing

### Verdict: APPROVE

### Summary

Step 2 replaces the two previously silent `inspectGitState` catch blocks with `recordGitInspectionError`, threads `gitInspectionError` through `deriveDiagnosis` as `git_unavailable` (correctly after `orphanRunning`, before limbo/integrate branches), and overrides operator messaging in `reconcileBatch` with `spine doctor`. The new `tests/batch/reconcile-git-error.test.mjs` covers real git failures (`missing-base-branch`), the env test hook, and the limbo-stale misdiagnosis regression. Typecheck passes; targeted reconcile tests pass (11/11).

### Build / typecheck

| Gate | Command | Result |
|------|---------|--------|
| Typecheck | `npm run typecheck` | Pass |
| Targeted tests | `node --test tests/batch/reconcile-git-error.test.mjs tests/batch/reconcile.test.mjs` | Pass (11/11) |
| Coverage (changed module) | `npm run coverage:check` (partial — aborted on unrelated failure) | `reconcile.mjs` **94.28%** line coverage; new paths exercised by 4 tests |

Full `npm run coverage:check` aborted on an unrelated flaky test (`tests/dashboard/cli-startup.test.mjs` — startup banner timeout). That failure is out of Step 2 scope; defer full-suite gate to Step 3 per PROMPT.

### Issues Found

None blocking.

### Suggestions (non-blocking)

1. **`src/batch/diagnosis.mjs`** — `git_unavailable` is not in `DIAGNOSIS_TAXONOMY` and messaging is patched inline in `reconcile.mjs:571-575` after `buildDiagnosisOutput`. Works for the reconcile path today, but consolidating headline/command/alternatives into `diagnosis.mjs` would match the approved plan and avoid the default `"requires attention (phase: …)"` fallback if another caller uses `buildDiagnosisOutput("git_unavailable", …)` directly.
2. **`src/batch/reconcile.mjs` helper catches** — `isInsideGitRepo`, `listOrchBranches`, `gitRefExists`, and `gitIsAncestor` still swallow errors as `false`/`[]`. The audit-highlighted catches are fixed; broader helper surfacing (e.g. rev-parse failure → `git_unavailable` instead of `inGitRepo: false`) can be a follow-up if operators still hit silent limbo paths.
3. **Commit hygiene** — `tests/batch/reconcile-git-error.test.mjs` is present and passing but still untracked; include it in the Step 2 commit alongside `reconcile.mjs`.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-116",
  "feedback": "Git error surfacing meets Step 2 outcomes: silent catches replaced with gitInspectionError, deriveDiagnosis returns git_unavailable before limbo_stale, reconcileBatch surfaces spine doctor, and reconcile-git-error tests pass with reconcile.mjs at 94% line coverage. Non-blocking: wire git_unavailable into diagnosis.mjs taxonomy for consistency; stage the new test file on commit; defer full coverage:check to Step 3 (unrelated dashboard flake)."
}
```
