### Verdict: PASS

### Summary

SP-583 completes the first-half strangler split of `lane-dirty-check.mjs` per FR-SHIP-02. Porcelain parsing, symlink drift, and path-in-scope helpers live in `lane-dirty-check-git.mjs` (298 LOC); the parent re-exports all public symbols for API stability while gitignored remediation and `resolvePostLaneCommitPorcelain` remain deferred to SP-601. Machine contract verification passed; independent re-run of `testCommand` (12/12) and lane-dirty regression tests (28/28 on coverage/symlink/gitignored/graphify suites) confirm no behavior regression.

```json
{"verdict":"PASS","feedback":"All steps complete (Steps 0–3, .DONE present, STATUS marked done). File scope honored: src/batch/lane-dirty-check-git.mjs (new, 298 LOC ≤500), src/batch/lane-dirty-check.mjs (484 LOC, re-exports preserved). Grandfather list honored — bin/spine-cli/verify.mjs untouched. Commit message matches convention (refactor(SP-583): extract lane-dirty-check-git.mjs). Completion criterion met: first-half extract done, second half explicitly deferred to SP-601. Non-blocking: full npm test suite still has pre-existing worker-env failures unrelated to this refactor."}
```
