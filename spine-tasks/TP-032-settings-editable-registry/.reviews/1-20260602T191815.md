## Plan Review: Step 1 — Registry + validators

### Verdict: APPROVE

### Summary

The Step 1 plan matches PROMPT deliverables: a pure `src/config/settings-fields.mjs` module with a frozen `SETTINGS_FIELDS` catalog for the five FR-CFG-03 v1.1 paths documented in Step 0, `parseSettingPath` / `validateSettingValue` returning `{ ok, error?, normalizedValue? }`, `listEditableFields()` for TP-033 show, and CLI-safe coercion for booleans, integers, strings, and enums. Per-field tests plus unknown-path rejection satisfy the checkpoint test matrix; wiring `tests/config/*.test.mjs` into `package.json` is required and already planned. Keeping the module filesystem-free and deferring merge/persist to TP-034 is the correct boundary.

### Issues Found

None blocking.

### Missing Items

- **Step 1 plan in STATUS** — Add a short “Step 1 plan” subsection (module exports, coercion rules, bounds table) before marking the step done; helps later code reviews and TP-033 consumers.
- **`parseSettingPath` in Step 1 checklist** — PROMPT Mission lists `parseSettingPath`; Step 1 bullets only name `validateSettingValue` / `listEditableFields`. Implementation correctly includes `parseSettingPath`; no functional gap.

### Suggestions

- **Bounds rationale** — Document why `lanes.maxParallel` caps at `32` (scheduler sanity) while `validateSpineConfig` only enforces `>= 1`; doctor heuristics remain advisory (`suggestMaxParallel` max 4). This avoids operators assuming registry max equals doctor suggestion.
- **`dashboard.port` range** — `min: 1024` aligns with non-privileged ports and `DEFAULT_DASHBOARD_PORT` (8109); note in a one-line comment that `validateSpineConfig` does not yet validate `dashboard`, so this registry is the authoritative gate for settings UX.
- **Commit scope** — Step 1 commit should include only `src/config/settings-fields.mjs`, `tests/config/settings-fields.test.mjs`, and the `package.json` test glob line. Do not bundle unrelated worktree changes (e.g. other staged tasks’ `src/cli/`, `bin/spine-deps.mjs`) into the TP-032 step commit.
- **TP-034 contract** — Downstream `spine settings set` must still run `validateSpineConfig` on the merged document; registry validation alone is necessary but not sufficient for write safety.
- **Error messages** — Keep enum/boolean error strings stable; TP-033/034 will surface them to operators.

```json
{
  "verdict": "APPROVE",
  "feedback": "Plan correctly delivers a pure SETTINGS_FIELDS registry with parseSettingPath, validateSettingValue, listEditableFields, CLI coercion, and per-field tests for all five Step 0 paths. Proceed with settings-fields module and tests only in Step 1 commit; document maxParallel upper bound vs doctor heuristic; TP-034 must still call validateSpineConfig after merge."
}
```
