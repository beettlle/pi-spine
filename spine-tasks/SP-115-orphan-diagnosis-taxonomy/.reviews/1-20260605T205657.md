## Plan Review: Taxonomy

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` and the current worktree diff is sound: introduce `worker_orphaned` in `DIAGNOSIS_TAXONOMY`, remap lane-scoped orphan detection in `deriveDiagnosis` (via SP-111 `detectOrphanRunning`) from generic `needs_retry` to the new kind, and wire `buildSuggestedCommand` / `buildHeadline` / `buildAlternatives` with a `ghostRunningCluster` signal so multiple ghost-running tasks on one lane prefer `spine batch abort`. Orphan checks remain ahead of segment-drift handling, preserving `needs_retry` for drift-only failures. Focused tests in `tests/batch/diagnosis-orphan-taxonomy.test.mjs` plus updated expectations in `tests/batch/orphan-reconcile.test.mjs` match the Step 1 outcomes and the "Do NOT break segment drift" constraint.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Add `worker_orphaned` to `DIAGNOSIS_TAXONOMY` | New entry in `src/batch/diagnosis.mjs` between `needs_retry` and `engine_orphaned` |
| Map lane orphan in `deriveDiagnosis` | `orphanRunning.kind === "lane" && orphanRunning.taskId` → `worker_orphaned`; engine-only path stays `engine_orphaned` |
| `buildSuggestedCommand` prefers abort for ghost cluster | `hasGhostRunningCluster()` counts `classification === "running"` on the orphan lane; `ghostRunningCluster: true` → `spine batch abort` |
| File Scope | `diagnosis.mjs`, `reconcile.mjs`, new taxonomy test file — no dashboard/docs in Step 1 |
| Do NOT break segment drift | `hasSegmentDrift` still maps to `needs_retry` after orphan branch; explicit regression test planned |
| Dependency SP-111 | Reuses existing `detectOrphanRunning` / `journalEventsSinceResume` — no duplicate orphan logic |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-115-orphan-diagnosis-taxonomy/STATUS.md`** — Add a short Step 1 plan subsection (taxonomy entry, `deriveDiagnosis` remap, ghost-cluster abort signal, test files) and record this review artifact before marking Step 1 complete.
2. **Step 1 vs Step 2 commit boundary** — The worktree also includes Step 2 headline enrichment (`classifyLaunchFailureHaystack`, `inferLaunchFailureFromWorkerOutputTail`, `enrichLaunchFailureFromWorkerOutput`, log-tail integration tests). Keep the Step 1 commit scoped to taxonomy + lane orphan mapping + ghost-cluster abort + drift regression; defer log-tail enrichment to Step 2 per PROMPT.
3. **Plan-before-implementation ordering** — Implementation appears ahead of this plan review. Acceptable if the Step 1 diff stays within File Scope; going forward, land Step 1 commit after plan APPROVE.
4. **Dashboard / runbook** — Task completion criteria mention dashboard/diagnose parity; Step 1 file scope does not require `src/dashboard/view.mjs` or operator docs. `spine status --diagnose` will surface the distinct kind via reconcile; optional dashboard badge/label mapping can follow in a later step or doc pass if product wants explicit UI labels.
5. **Lane orphan without `taskId`** — Current plan falls through to `engine_orphaned` when `kind === "lane"` but `taskId` is missing. Document as intentional fallback or add a narrow test if SP-111 can emit that shape.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan: add worker_orphaned to DIAGNOSIS_TAXONOMY; remap lane orphanRunning in deriveDiagnosis; ghostRunningCluster → buildSuggestedCommand prefers spine batch abort; worker_orphaned headlines/alternatives in diagnosis.mjs; update orphan-reconcile + new diagnosis-orphan-taxonomy tests including segment-drift needs_retry regression. Split Step 2 log-tail enrichment to its own commit; record plan review in STATUS before closing Step 1."
}
```
