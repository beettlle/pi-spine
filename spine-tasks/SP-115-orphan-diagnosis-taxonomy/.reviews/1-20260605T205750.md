## Code Review: Taxonomy

### Verdict: APPROVE

### Summary

Step 1 delivers the promised taxonomy split: `worker_orphaned` is added to `DIAGNOSIS_TAXONOMY`, lane-scoped orphan detection in `deriveDiagnosis` maps to the new kind (engine-only orphans remain `engine_orphaned`), and `buildSuggestedCommand` / `buildHeadline` / `buildAlternatives` handle the new diagnosis with a `ghostRunningCluster` signal that correctly prefers `spine batch abort` when multiple ghost-running tasks share a lane. Orphan precedence is unchanged ahead of segment drift, and the explicit segment-drift regression test confirms `needs_retry` is preserved for non-orphan failures.

Focused tests pass locally: `tests/batch/diagnosis-orphan-taxonomy.test.mjs` (9) and updated `tests/batch/orphan-reconcile.test.mjs` (9), plus `tests/batch/diagnosis-launch-failed.test.mjs` (8) for `needs_retry` launch-failure paths. The `classifyLaunchFailureHaystack` refactor is clean and shared between journal and log-tail inference.

### Issues Found

None blocking.

### Missing Items

- **Untracked test file** — `tests/batch/diagnosis-orphan-taxonomy.test.mjs` is `??` in git status and must be staged with the Step 1 commit; without it the new coverage is lost.
- **Commit noise** — `.spine/rules-manifest.json` timestamp-only change should be excluded from the feature commit.
- **Step 1 / Step 2 boundary** — The diff also lands Step 2 headline enrichment (`inferLaunchFailureFromWorkerOutputTail`, `enrichLaunchFailureFromWorkerOutput`, log-tail integration test). Code is correct and tested; either accept as one commit or split per PROMPT step boundaries before marking Step 2 complete.

### Suggestions

1. **`buildSuggestedCommand("worker_orphaned")`** — The `pi_spine_root`/`launch_failed` and `worktree_unhealthy` branches both return `spine doctor`; could merge into one condition (style only).
2. **Dashboard parity** — `src/dashboard/view.mjs` has no badge/label entry for `worker_orphaned` (falls through to `badge-action` / `"Diagnose"`). Out of Step 1 file scope; address when closing task completion criteria or in a follow-up step.
3. **Lane orphan without `taskId`** — `orphanRunning.kind === "lane"` without `taskId` still falls through to `engine_orphaned`; matches plan-review note; add a narrow test only if SP-111 can emit that shape.
4. **STATUS closure** — Record this review artifact and check Step 1 boxes in `STATUS.md` before advancing to Step 2 verification / full suite.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-115",
  "feedback": "worker_orphaned taxonomy, deriveDiagnosis lane remap, ghostRunningCluster abort preference, headlines/alternatives, and segment-drift needs_retry regression are correct; 18 focused orphan tests pass. Stage diagnosis-orphan-taxonomy.test.mjs; drop rules-manifest noise from commit. Step 2 log-tail enrichment is bundled early but sound—split commits optional. Proceed to Step 2 or full-suite gate per PROMPT."
}
```
