## Plan Review: Headline enrichment (Step 2)

### Verdict: APPROVE

### Summary

Step 2 closes SP-106 Finding #11 / the mission's FR-STALL-01 gap: when a lane orphan is diagnosed as `worker_orphaned` but journal inference yields no `launchFailureKind` (no `task.failed` yet), reconcile should read the persisted worker output log tail and feed it through the same launch-failure classifier SP-105 uses for `needs_retry`. The planned shape is minimal and compositional — refactor `classifyLaunchFailureHaystack` as the shared haystack classifier, export `inferLaunchFailureFromWorkerOutputTail` from `diagnosis.mjs`, add `readWorkerOutputLogTail` + `enrichLaunchFailureFromWorkerOutput` in `reconcile.mjs` (via existing `workerOutputLogPath`), and apply enrichment only on the `worker_orphaned` path before `buildDiagnosisOutput`. Step 1 already wired `buildHeadline` / `buildSuggestedCommand` launch variants for `worker_orphaned`; Step 2 supplies the missing signal when journal events are absent.

The worktree diff matches this plan: enrichment runs when `launchFailureKind` is null, reads the last 20 lines from `.spine/runtime/<batchId>/lanes/lane-<N>/worker-output-<taskId>.log`, and maps `CONFIG_PI_SPINE_ROOT` / worktree hints to the same `pi_spine_root` / `worktree_unhealthy` headlines and `spine doctor` commands. Focused coverage — unit test on `inferLaunchFailureFromWorkerOutputTail`, integration test `worker_orphaned enriches launch headline from worker output log without task.failed` — exercises the SAT-048 orphan-without-journal scenario. All 9 tests in `tests/batch/diagnosis-orphan-taxonomy.test.mjs` pass locally.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Inspect worker output log tail when launching-phase orphan | `enrichLaunchFailureFromWorkerOutput` reads log via `workerOutputLogPath`; triggers on `worker_orphaned` + null `launchFailureKind` |
| Enrich headlines when no `task.failed` journal | Enrichment skipped when journal/`inferLaunchFailureKind` already resolved a kind; log tail is fallback only |
| FR-STALL-01 artifact reuse | Uses SP-056 persisted log path helper; no new capture/redaction logic |
| File scope | `diagnosis.mjs`, `reconcile.mjs`, `tests/batch/diagnosis-orphan-taxonomy.test.mjs` — within PROMPT list |
| Do NOT break segment drift / `needs_retry` | Enrichment gated to `diagnosis === "worker_orphaned"`; `needs_retry` journal path unchanged |
| Step 1 dependency | Reuses Step 1 `worker_orphaned` headlines/commands and `classifyLaunchFailureHaystack` refactor |

### Issues Found

None blocking.

### Missing Items

- **Launching-phase gate (PROMPT nuance)** — Step 2 checkbox says "launching-phase orphan," but the plan enriches for all `worker_orphaned` rows with null `launchFailureKind`, without checking `lanes[].workerPhase === "launching"`. Keyword-only classification limits misclassification risk today; explicit phase gating would tighten PROMPT alignment.
- **Generic launch fallback** — `inferLaunchFailureFromWorkerOutputTail` does not pass `launchingPhase: true`, so log tails without `pi_spine_root` / worktree keywords will not resolve to `launch_failed` (journal path with `workerPhase: launching` does). Acceptable for the primary PI_SPINE_ROOT incident; optional follow-up if operators need "during launch" headline on opaque boot text.
- **Worktree log-tail test** — Plan covers `pi_spine_root` integration; no parallel test for worktree-unhealthy log tail on orphan-without-`task.failed`.
- **STATUS / commit hygiene** — Record this review in `STATUS.md`; keep Step 2 diff in its own commit if Step 1 was split per prior plan review.

### Suggestions (non-blocking)

1. **Phase gate** — Before reading the log, resolve the orphan lane from `signals.lanes` and require `workerPhase === "launching"` (or last journal heartbeat with `workerPhase: launching`) so pi-phase orphans keep the generic "lane worker orphaned while task … was running" headline unless journal already classified the failure.
2. **Launching hint on log path** — When phase gate passes, call `classifyLaunchFailureHaystack(tail, { launchingPhase: true })` so any non-empty launching log yields `launch_failed` and the "during launch" headline even without keyword matches.
3. **Negative test** — pi-phase orphan with a log containing unrelated text should keep the generic orphan headline (especially if phase gating is added).
4. **Log availability note** — Document that enrichment depends on SP-056 persistence; orphans that die before `finalizeWorkerOutput` may still lack a log — enrichment degrades gracefully to generic headline.
5. **Step 3 gate** — Proceed to FULL suite + coverage after Step 2 code review; no dashboard/docs required in Step 2 file scope.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-115",
  "feedback": "Step 2 plan: shared classifyLaunchFailureHaystack + inferLaunchFailureFromWorkerOutputTail; reconcile enrichLaunchFailureFromWorkerOutput reads FR-STALL-01 log tail for worker_orphaned when launchFailureKind is null; Step 1 headlines/commands consume enriched kind. Tests cover PI_SPINE_ROOT orphan without task.failed. Optional: gate on lane workerPhase launching and pass launchingPhase hint for generic launch_failed fallback; add worktree log-tail test. Record review in STATUS; proceed to Step 2 code review then Step 3 FULL suite."
}
```
