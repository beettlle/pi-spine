## Code Review: Headline enrichment (Step 2)

### Verdict: APPROVE

### Summary

Step 2 closes the FR-STALL-01 / SP-106 Finding #11 gap: when reconcile diagnoses `worker_orphaned` but journal inference leaves `launchFailureKind` null (no `task.failed` yet), the code reads the persisted worker output log tail and feeds it through the same launch-failure classifier SP-105 uses for `needs_retry`. The implementation is minimal and compositional — `classifyLaunchFailureHaystack` is extracted as the shared haystack classifier, `inferLaunchFailureFromWorkerOutputTail` is exported from `diagnosis.mjs`, and `readWorkerOutputLogTail` + `enrichLaunchFailureFromWorkerOutput` in `reconcile.mjs` apply enrichment only on the `worker_orphaned` path before `buildDiagnosisOutput`. Step 1 headlines/commands already consume `launchFailureKind`; Step 2 supplies the missing signal when journal events are absent.

The worktree diff matches the approved Step 2 plan: enrichment runs when `launchFailureKind` is null, reads the last 20 lines from `.spine/runtime/<batchId>/lanes/lane-<N>/worker-output-<taskId>.log` via existing `workerOutputLogPath`, and maps `CONFIG_PI_SPINE_ROOT` / worktree hints to the same `pi_spine_root` / `worktree_unhealthy` headlines and `spine doctor` commands. Focused coverage — unit test on `inferLaunchFailureFromWorkerOutputTail`, integration test `worker_orphaned enriches launch headline from worker output log without task.failed` — exercises the SAT-048 orphan-without-journal scenario. Updated `orphan-reconcile.test.mjs` expectations align with Step 1 `worker_orphaned` headlines. All 18 tests in `tests/batch/diagnosis-orphan-taxonomy.test.mjs` and `tests/batch/orphan-reconcile.test.mjs`, plus 8 in `tests/batch/diagnosis-launch-failed.test.mjs`, pass locally.

### Alignment checks

| PROMPT outcome | Implementation |
|----------------|----------------|
| Inspect worker output log tail when launching-phase orphan | `enrichLaunchFailureFromWorkerOutput` reads log via `workerOutputLogPath`; triggers on `worker_orphaned` + null `launchFailureKind` |
| Enrich headlines when no `task.failed` journal | Enrichment skipped when journal/`inferLaunchFailureKind` already resolved a kind; log tail is fallback only |
| FR-STALL-01 artifact reuse | Uses SP-056 persisted log path helper; no new capture/redaction logic |
| File scope | `diagnosis.mjs`, `reconcile.mjs`, `tests/batch/diagnosis-orphan-taxonomy.test.mjs` — within PROMPT list |
| Do NOT break segment drift / `needs_retry` | Enrichment gated to `diagnosis === "worker_orphaned"`; `needs_retry` journal path unchanged |

### Issues Found

None blocking.

### Missing Items

- **Untracked test file** — `tests/batch/diagnosis-orphan-taxonomy.test.mjs` remains `??` in git status and must be staged with the Step 2 commit; without it the log-tail integration coverage is lost.
- **Commit noise** — `.spine/rules-manifest.json` timestamp-only change should be excluded from the feature commit.
- **Launching-phase gate (PROMPT nuance)** — Step 2 checkbox says "launching-phase orphan," but enrichment runs for all `worker_orphaned` rows with null `launchFailureKind`, without checking `lanes[].workerPhase === "launching"`. Keyword-only classification limits misclassification risk today; explicit phase gating would tighten PROMPT alignment (optional follow-up, not blocking).
- **Worktree log-tail test** — Coverage includes `pi_spine_root` integration; no parallel test for worktree-unhealthy log tail on orphan-without-`task.failed`.
- **STATUS closure** — Record Step 1 and Step 2 review artifacts in `STATUS.md` and advance **Current Step** to Step 3 before full-suite verification.

### Suggestions

1. **Phase gate** — Before reading the log, resolve the orphan lane from `signals.lanes` and require `workerPhase === "launching"` so pi-phase orphans keep the generic "lane worker orphaned while task … was running" headline unless journal already classified the failure.
2. **Launching hint on log path** — When phase gate passes, call `classifyLaunchFailureHaystack(tail, { launchingPhase: true })` so any non-empty launching log yields `launch_failed` and the "during launch" headline even without keyword matches.
3. **Negative test** — pi-phase orphan with a log containing unrelated text should keep the generic orphan headline (especially if phase gating is added).
4. **Log availability note** — Enrichment depends on SP-056 persistence; orphans that die before `finalizeWorkerOutput` may still lack a log — enrichment degrades gracefully to generic headline.
5. **Step 3 gate** — Proceed to FULL suite + coverage; no dashboard/docs required in Step 2 file scope.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-115",
  "feedback": "Step 2 code: classifyLaunchFailureHaystack refactor + inferLaunchFailureFromWorkerOutputTail; reconcile enrichLaunchFailureFromWorkerOutput reads FR-STALL-01 log tail for worker_orphaned when launchFailureKind is null; Step 1 headlines/commands consume enriched kind. PI_SPINE_ROOT orphan-without-task.failed integration test passes; needs_retry launch paths unchanged. Stage diagnosis-orphan-taxonomy.test.mjs; drop rules-manifest noise. Optional: gate on lane workerPhase launching and pass launchingPhase hint for generic launch_failed fallback. Record reviews in STATUS; proceed to Step 3 FULL suite."
}
```
