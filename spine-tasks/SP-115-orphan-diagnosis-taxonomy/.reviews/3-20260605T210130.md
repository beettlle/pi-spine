# Code Review — SP-115 Step 3: Testing & Verification

**Reviewer:** Code review agent  
**Step:** 3 — Testing & Verification  
**Worktree:** `lane-1` (`task/spine-lane-1-20260605T191325`)  
**Date:** 2026-06-05

## Scope

Step 3 per `PROMPT.md`: run the full test suite and coverage gate after Steps 1–2 (taxonomy + headline enrichment).

Changed files (`git diff --name-only`):

| File | Role |
|------|------|
| `src/batch/diagnosis.mjs` | `worker_orphaned` taxonomy, headlines, commands, launch-failure haystack refactor |
| `src/batch/reconcile.mjs` | Lane orphan mapping, worker-output enrichment, ghost-cluster detection |
| `tests/batch/orphan-reconcile.test.mjs` | Updated expectations for `worker_orphaned` |
| `tests/batch/diagnosis-orphan-taxonomy.test.mjs` | **New, untracked** — dedicated SP-115 test module |
| `spine-tasks/SP-115-orphan-diagnosis-taxonomy/STATUS.md` | Status bookkeeping |
| `.spine/rules-manifest.json` | Timestamp-only manifest regen (noise) |

## Summary

The implementation cleanly separates lane orphan (`worker_orphaned`) from generic task failure (`needs_retry`) and enriches launch-phase orphans from worker output logs when no `task.failed` journal exists.

**Verification run:**

| Check | Result |
|-------|--------|
| `npm test` | **579 pass**, 0 fail |
| SP-115 targeted tests (`diagnosis-orphan-taxonomy` + `orphan-reconcile`) | **18 pass**, 0 fail |
| `npm run coverage:check` | **Aborted** — 1 unrelated failure in `tests/dashboard/cli-startup.test.mjs` (`spine dashboard CLI prints URL before process exits`, 3s timeout flake). Aggregate in-scope line coverage reported **83.27%** (threshold 77%) before abort. |

## Strengths

- **Taxonomy separation:** `deriveDiagnosis` maps lane orphan to `worker_orphaned`; segment drift still resolves to `needs_retry` (explicit regression test).
- **Shared launch classifier:** `classifyLaunchFailureHaystack` refactor avoids duplicating PI_SPINE_ROOT / worktree heuristics between journal and worker-output paths.
- **Headline enrichment:** `enrichLaunchFailureFromWorkerOutput` reads the last 20 lines of the worker log only when journal inference is empty — bounded I/O, correct guard on diagnosis kind.
- **Ghost cluster UX:** `hasGhostRunningCluster` + `buildSuggestedCommand` prefer `spine batch abort` when multiple ghost running tasks share a lane; covered by fixture replay.
- **Test depth:** New module exercises unit helpers, reconcile integration, incident fixture replay, and the "do not break needs_retry" constraint from the PROMPT.

## Findings

### Minor (non-blocking)

1. **Untracked test file** — `tests/batch/diagnosis-orphan-taxonomy.test.mjs` is not in `git diff --name-only` (still `??`). Stage before commit so Step 3 deliverables ship with the feature.

2. **Coverage gate blocked by pre-existing flake** — `npm run coverage:check` failed twice on the dashboard CLI startup timeout test, not on any SP-115 test. `npm test` (default script) passes fully. The flake is environmental/timing-related and outside this task's file scope; retry or stabilize separately if CI requires a green coverage run.

3. **Dashboard badge mapping** — `src/dashboard/view.mjs` has no explicit entries for `worker_orphaned` or `engine_orphaned` (falls back to `badge-action` / `Diagnose`). Diagnosis JSON and headlines are distinct, satisfying completion criteria; optional follow-up for operator-facing badge labels.

4. **Post-mortem failure set** — `postmortem.mjs` `FAILURE_DIAGNOSES` still lists only `failed` and `needs_retry`. Orphan diagnoses may not trigger post-mortem "failure" styling; same gap existed for `engine_orphaned` before SP-115.

5. **Worker-output launch classification without `launchingPhase`** — `inferLaunchFailureFromWorkerOutputTail` only matches explicit log keywords (PI_SPINE_ROOT, worktree). Generic launch errors in the log tail will not enrich to `launch_failed`; headline falls back to the generic orphan message. Acceptable for FR-STALL-01 artifact scope.

6. **STATUS.md stale** — Still shows Step 1 in progress; update when the task completes.

## Completion Criteria

| Criterion | Met |
|-----------|-----|
| Orphan lane diagnosis ≠ `needs_retry` | Yes — `worker_orphaned` with regression tests |
| Dashboard/diagnose shows distinct kind | Yes — `diagnosis: "worker_orphaned"` in reconcile output |
| Do not break `needs_retry` for segment drift | Yes — dedicated test |
| Full suite | Yes — 579/579 via `npm test` |
| Coverage gate | Partial — threshold would pass; run aborted on unrelated dashboard flake |

## Recommendation

**Approve Step 3.** SP-115 behavior is well-tested, the full suite is green, and coverage on touched modules is strong (`diagnosis.mjs` ~92%, `reconcile.mjs` ~94%). The coverage-gate failure is a pre-existing flaky dashboard test, not a regression from this change. Stage the new test file before commit.

### Verdict: APPROVE

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "SP-115",
  "feedback": "worker_orphaned taxonomy, reconcile enrichment, and ghost-cluster abort logic are covered by 9 new tests plus updated orphan-reconcile expectations; npm test 579/579 pass. Stage tests/batch/diagnosis-orphan-taxonomy.test.mjs before commit. npm run coverage:check aborts on unrelated flaky dashboard CLI startup test despite 83.27% line coverage; not introduced by SP-115."
}
```
