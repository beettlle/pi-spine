### Verdict: PASS

### Summary
The requested changes correctly extend the `POOL_PREFIXES` array to include both `anthropic` and `github-copilot` prefixes. The corresponding tests were updated to verify these specific mappings while asserting that unmapped providers (like `copilot/`) resolve correctly to `unknown`. All tests pass and no unintended modifications (like probe adapters) were introduced.

```json
{
  "verdict": "PASS",
  "feedback": "Great job. The implementation perfectly satisfies the completion criteria and respects the boundary constraints (no probe code changes)."
}
```