### Verdict: APPROVE

### Summary
The implementation plan for Step 0 and subsequent steps thoroughly addresses the core requirements of SP-742 (#232). Using whole-document `applyMatrixRowToPrompt` on top of the existing `substituteMatrixVariables` engine cleanly substitutes steps, contract, and file-scope paths while preserving fail-loud unknown-ref semantics. The design specifically anticipates and prevents row-lane merge conflicts by restoring the authored PROMPT.md and cleaning row `.DONE` before commit, while providing robust diagnostic journaling and test coverage.

```json
{"verdict":"APPROVE","feedback":"Plan covers all requirements of SP-742 and #232 within File Scope. The whole-document prompt substitution approach, fail-loud behavior, merge conflict prevention via authored prompt restoration, and test plan with runbook §2.4 updates are sound."}
```
