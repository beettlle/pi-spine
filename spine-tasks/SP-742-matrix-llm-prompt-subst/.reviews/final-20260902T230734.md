### Verdict: PASS

### Summary
The task implementation correctly wires the `applyMatrixRowToPrompt` helper into the LLM matrix execution branch (`runMatrixSubLane`). It fully substitutes `{matrix.*}` references in `PROMPT.md` for each row, correctly restores the original `PROMPT.md` and deletes the `.DONE` marker to prevent lane-merge conflicts, and fails fast on unknown variables. All tests run cleanly and documentation constraints (Runbook caveats) have been perfectly addressed.

```json
{"verdict":"PASS","feedback":"Excellent implementation. The strategy to swap PROMPT.md in place and restore it before the row commit is clean, and test coverage is solid."}
```