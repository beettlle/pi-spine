### Verdict: PASS

### Summary
The peer dependency `@earendil-works/pi-coding-agent` was correctly bumped to `^0.87.0` in `package.json` and the lockfile was refreshed. The contract test command passes successfully (typecheck, lint, audit, version check), and related documentation mentioning the old `0.85.1` version was updated accordingly.

```json
{"verdict":"PASS","feedback":"All completion criteria and contract checks are satisfied."}
```