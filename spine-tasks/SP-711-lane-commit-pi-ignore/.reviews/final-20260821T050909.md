### Verdict: PASS

### Summary
The task successfully adds `.pi` and `.pi-smart-router` to `DEFAULT_WORKTREE_SETUP_IGNORE_PATHS` in `src/config/spine-config-load.mjs`, extending the previous `.venv` pattern. It handles legacy repositories gracefully, verifies correctly via comprehensive scoping tests, and documents the change in the operator runbook as requested.

```json
{"verdict":"PASS","feedback":"All completion criteria met. The implementation correctly excludes the specified directories by default and properly respects opt-in overrides."}
```