### Verdict: PASS

### Summary

SP-406 delivers the Tier 3 tail-activity subline for GitHub #68: when all lanes are idle but the batch remains non-terminal, `resolveTailActivityLabel` derives a label from journal tail events (preferred) or macro phase, exposes it on the snapshot, and renders it as a lanes-table footer row in `dashboard.js`. UI contract tests cover resolution, view-model wiring, active-lane suppression, and DOM contract; machine verifier gates pass and the full suite (1411/1411) passes in this worktree. Issue #68 is closed and `.DONE` is present.

```json
{"verdict":"PASS","feedback":"Implementation matches PROMPT.md mission and completion criteria. snapshot.mjs adds resolveTailActivityLabel with journal-over-macro precedence and reuses isRunningWithoutActiveWorkers (no second reconcile path). view.mjs exposes buildTailActivityModel; dashboard.js renders the footer row with accessible aria-label. Four ui-contract tests added; fileScopeMustChange (view.mjs), testCommand, and minLineCoverage (88.06%) all pass. Operator runbook unchanged is appropriate for this display-only UX. No blocking scope creep or regressions found."}
```
