### Verdict: PASS

### Summary

SP-611 delivers the LOC-capstone readiness gate: pending empty-grandfather missions fail preflight/plan with an actionable over-limit module list, while ready trees and non-capstone scopes still pass. Shared policy evaluation lives in `src/config/preflight/loc-capstone.mjs`, wired into `runBatchPreflight` and `buildPlan`; tests cover blocked vs allowed cases; `PHASE23_GRANDFATHERED_OVER_500` remains `[]`; machine contract checks and `.DONE` are present. Minor note: `src/config/spine-preflight-lib.mjs` is adjacent to listed File Scope but is the necessary hook for the preflight check—not blocking.

```json
{"verdict":"PASS","feedback":"LOC-capstone readiness gate complete: blocks premature empty-grandfather with module list, allows ready trees, grandfather stays [], tests + contract PASS, .DONE present. Acceptable to close #192 / land SP-611."}
```
