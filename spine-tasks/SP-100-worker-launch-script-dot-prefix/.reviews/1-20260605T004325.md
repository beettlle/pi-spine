## Plan Review: Normalize relative prefix (Step 1)

### Verdict: APPROVE

### Summary

The Step 1 plan is sound and minimal: after trim and slash normalization, apply `path.posix.normalize` to collapse `./` segments before the existing `scripts/` prefix gate, while keeping the pre-normalize `..` and absolute-path rejections unchanged. That ordering preserves traversal safety (`./scripts/../outside.sh` still fails on the literal `..` check) and makes `./scripts/...` equivalent to `scripts/...` for valid paths. The `worktreeSetupHook` sweep correctly concludes there is no parallel prefix validator in `src/`, so Step 1 scope stays confined to `validateWorkerLaunchScriptPath`.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Strip leading `./` before `startsWith("scripts/")` | `path.posix.normalize(trimmed)` inserted after trim/backslash normalize, before prefix check |
| Keep `..`, absolute, symlink-escape rejections | `..` and absolute checks remain on `trimmed` (pre-normalize); downstream resolve/realpath guards unchanged |
| `worktreeSetupHook` parity (if present) | Grep confirms no hook path-prefix validation in `src/` — no extra work needed |
| File scope | Single-function change in `src/config/worker-launch-script.mjs`; no CLI/symlink scope creep |
| Do NOT | Normalization cannot widen allowed directories; traversal paths still rejected |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-100-worker-launch-script-dot-prefix/STATUS.md`** — After this review, record the review artifact in the Reviews table and mark the Step 1 plan-review checkbox complete before advancing to Step 2.
2. **Step boundary** — `tests/config/worker-launch-script.test.mjs` already covers dot-prefix acceptance and traversal; keep Step 2 as the formal gate for full suite, existing `worker-host.test.mjs` regression, and `npm run coverage:check` ≥77%.
3. **Plan-before-code ordering** — Implementation landed ahead of this plan review (same pattern as other lane tasks). Acceptable here because the diff matches the approved approach; defer any further edits until Step 2 verification.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan correctly normalizes ./scripts/ via path.posix.normalize after trim while preserving pre-normalize .. and absolute guards; worktreeSetupHook has no src/ counterpart. Proceed to Step 2 full-suite and coverage verification."
}
```
