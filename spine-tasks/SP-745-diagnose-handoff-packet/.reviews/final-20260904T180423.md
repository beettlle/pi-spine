### Verdict: PASS

### Summary
The task successfully adds `background` and `assessmentReason` to `buildDiagnosisOutput` without modifying legacy fields, adhering perfectly to the requirement of backward compatibility. The CLI has been updated to render the SBAR structure cleanly when `--diagnose` is requested. Contract constraints were respected, and thorough tests were added for the required diagnoses. 

```json
{"verdict":"PASS","feedback":"All PROMPT goals are met securely and completely. SBAR structure works as requested while keeping the diagnosis API clean."}
```