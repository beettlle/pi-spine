### Verdict: PASS

### Summary

SP-405 correctly fixes GitHub issue #68 by deriving wave `completed` status from classified task terminal-success state, not only from `index < currentWaveIndex`. The implementation stays within file scope, wires `classifiedTasks` through `buildDashboardSnapshot`, and adds focused regression tests for both the last-wave completion case and in-flight wave behavior.

Contract verification passed (testCommand, fileScopeMustChange on `src/dashboard/snapshot.mjs`, minLineCoverage 87.94% ≥ 77%). Independent snapshot test run: 11/11 pass. No blocking scope creep, schema changes, or missing acceptance coverage identified.

```json
{"verdict":"PASS","feedback":"buildWaveProgress now marks waves completed when every wave task ID is terminal-success via resolveWaveStatus/waveTasksAllTerminalSuccess, with classifiedTasks passed from buildDashboardSnapshot. Tests cover last-wave completion at currentWaveIndex and preserve active status for in-flight waves. Contract gates pass; file scope and Do NOT constraints honored."}
```
