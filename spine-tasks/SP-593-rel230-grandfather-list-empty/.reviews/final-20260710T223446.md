### Verdict: PASS

### Summary

Completion criteria are met: `PHASE23_GRANDFATHERED_OVER_500` is `[]` in `bin/spine-cli/verify.mjs`, and all `src/batch/*.mjs` modules are ≤500 LOC (max: lifecycle 498, engine 497, integrate 496). Machine contract checks pass (`testCommand` 5/5; `fileScopeMustChange` matched); `phase23-exit --skip-test` exits 0; `.DONE` and STATUS Step 0–3 are present. LOC policy remains enforced — not removed.

```json
{"verdict":"PASS","feedback":"Grandfather list emptied; all batch modules ≤500 LOC; batch-loc-policy green; contract tests 5/5 pass; .DONE and STATUS complete. Acceptable to close SP-593 / #117."}
```
