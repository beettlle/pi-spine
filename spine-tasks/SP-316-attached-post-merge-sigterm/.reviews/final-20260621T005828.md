### Verdict: PASS

### Summary

SP-316 fully addresses GitHub #21: attached engines install SIGTERM/SIGINT shutdown handlers (`installAttachedEngineShutdownHandlers` in `src/batch/attached-engine-handoff.mjs`, wired from `src/batch/engine.mjs` and `src/batch/resume.mjs`) that finalize post-merge limbo in-process via `tryFinalizePostMergeLimbo` or spawn a detached resume engine as fallback. Regression tests in `tests/batch/attached-post-merge-sigterm.test.mjs` cover the batch `20260620T194352` journal pattern (merge → orphan SIGTERM → gate open), idempotent finalize, SIGTERM handler exit, and resume recovery. Contract verification passes (testCommand, fileScopeMustChange, 86.97% line coverage ≥77%, required artifact). Delivery complete: operator-runbook updated, issue #21 closed, `.DONE` committed.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met. Implementation is scoped to post-merge limbo handoff with solid regression coverage. Detached-resume fallback when in-process finalize fails is implemented but not explicitly unit-tested; acceptable given primary fix path and resume tests. No blocking scope creep or regressions observed."}
```
