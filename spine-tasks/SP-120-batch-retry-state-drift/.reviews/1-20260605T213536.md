## Plan Review: Step 1 — Reproduce + fix state transition

### Verdict: APPROVE

### Summary

Step 1 correctly targets the batch `20260605T191325` drift class: stale `classification` / `exitReason` and counter skew after `spine batch retry` when the worker later emits `task.completed`. Centralizing `resetTaskForRetry`, `recordTaskSucceeded`, and `recomputeTaskCounters` in `state.mjs` — with `retry.mjs` clearing failure metadata on reset and `engine-lanes.mjs` routing success paths through `recordTaskSucceeded` — addresses both PROMPT bullets (retry reset + post-retry success transition) without changing unrelated segment-drift detection semantics. The proposed fixture and `retry-state-drift.test.mjs` cover the incident pattern and reconcile diagnosis (`needs_retry` / `prompt_parse_failed` suppression), aligned with the completion criterion.

### Issues Found

None blocking.

### Missing Items

- **Step 1 commit boundary** — Land Step 1 as `fix(SP-120): clear failed task state on successful retry` with scoped files: `src/batch/state.mjs`, `src/batch/retry.mjs`, `src/batch/engine-lanes.mjs`, `src/batch/resume-common.mjs` (dedup re-export only), `tests/batch/retry-state-drift.test.mjs`, `tests/fixtures/incidents/retry-clears-failed-classification.json`, and task `STATUS.md`. Defer `docs/incidents/` note and `.DONE` to Step 3.
- **STATUS hydration** — Add a Step 1 plan subsection (new exports, retry/success transition summary, commit file list) and log this review under a Reviews table before Step 2 proceeds (Review Level 2 hygiene).
- **Step 2 verification gate** — Step 1 tests pass locally (`node --test tests/batch/retry-state-drift.test.mjs tests/batch/retry.test.mjs`); Step 2 owns `npm run typecheck`, full suite, and coverage gate per PROMPT.

### Suggestions (non-blocking)

- **Root-cause alignment** — Prior `resetTaskAndSegments` cleared `exitReason` but left persisted `classification` on task/segment rows; `recordTaskSucceeded` closes the second half of the bug where success updated `status` without deleting stale failure metadata. Good split of responsibilities.
- **`resume-common.mjs` dedup** — Re-exporting `recomputeTaskCounters` from `state.mjs` is slightly outside File Scope but justified to eliminate three duplicate counter implementations; keep the diff to a re-export only (no behavior change).
- **`resume.mjs` parity** — Single-lane resume success paths still inline `task.status = "succeeded"` without `recordTaskSucceeded`; acceptable for this incident (engine-lanes path) because retry reset already clears classification. Consider adopting `recordTaskSucceeded` in a follow-up if resume-after-retry drift appears elsewhere — not blocking.
- **Plan-before-implementation ordering** — Implementation and tests are present in the worktree before this plan review (same pattern as SP-111/SP-116). Going forward, record plan APPROVE in STATUS before marking Step 1 complete; Step 2 code review should follow the same gate.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-120",
  "feedback": "Step 1 plan: centralize resetTaskForRetry/recordTaskSucceeded/recomputeTaskCounters in state.mjs; retry.mjs clears exitReason+classification on reset; engine-lanes.mjs routes success through recordTaskSucceeded. Add retry-clears-failed-classification incident fixture + retry-state-drift.test.mjs (retry reset, retry→completed→reconcile, fixture reconcile). Commit scoped src + tests + STATUS; defer docs/incidents to Step 3. Step 2 owns full suite + coverage gate. Record plan review in STATUS."
}
```
