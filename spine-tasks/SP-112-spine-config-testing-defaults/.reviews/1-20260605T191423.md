## Plan Review: Step 1 — Defaults and dogfood config

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` is sound and correctly scoped to config defaults only (no gate FSM or doctor wiring). It closes SP-107 Finding #3 / SP-108 F2 by ensuring `testing.build`, `testing.test`, and `testing.testWithCoverage` are non-empty wherever evidence gates are enabled.

The plan has three concrete deliverables:

1. **`templates/spine-config.json`** — Populate `testing.build` and `testing.test` with the existing init constant `SPINE_INIT_TESTING_COMMAND` (`npm run typecheck && npm test`), matching `package.json` `prepublishOnly` and reviewer-template guidance. `testWithCoverage` already defaults to `npm run coverage:check` in the template; no change needed there beyond consistency checks.

2. **`bin/spine-init.mjs`** — Export `SPINE_INIT_COVERAGE_COMMAND` (`npm run coverage:check`) and set `config.testing.testWithCoverage` inside `applySpineInitDefaults`, closing the gap where init overlay set build/test but left coverage to the raw template only.

3. **`.spine/spine-config.json` (dogfood)** — Replace all-empty `testing.*` with `package.json` script invocations: `build` → `npm run typecheck`, `test` → `npm test`, `testWithCoverage` → `npm run coverage:check`. Split build/test is intentional for pi-spine dogfood (faster reviewer build gate, separate test evidence) and still maps to the same scripts greenfield init combines.

Step boundaries are respected: doctor warnings (`buildTestingEvidenceDoctorChecks`, `bin/spine-doctor.mjs` wiring) and `tests/config/spine-config-testing.test.mjs` belong in Step 2 / Step 3 per PROMPT checkboxes, not Step 1.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Template defaults from `package.json` patterns | `SPINE_INIT_TESTING_COMMAND` + `SPINE_INIT_COVERAGE_COMMAND` sourced from `npm run typecheck`, `npm test`, `coverage:check` scripts |
| Refresh pi-spine dogfood config | `.spine/spine-config.json` non-empty `testing.*` aligned to same scripts (split build/test) |
| Doctor surfaces misconfiguration | Deferred to Step 2 (correct — empty testing + evidence gates) |
| Do NOT change gate FSM | Config/init/template only; no `evidence.mjs` / `gate.mjs` changes |
| File scope | Step 1 touches `templates/spine-config.json`, `.spine/spine-config.json`, `bin/spine-init.mjs` only |
| Completion: dogfood non-empty testing fields | Satisfied by dogfood refresh |

### Suggestions (non-blocking)

1. **`STATUS.md`** — After this review, record Step 1 plan review APPROVE; note dogfood uses split `build`/`test` while init template uses combined `SPINE_INIT_TESTING_COMMAND` for both fields (document in Discoveries if operators ask).
2. **Step 1 commit boundary** — Land config files only (`templates/spine-config.json`, `.spine/spine-config.json`, `bin/spine-init.mjs`). Keep doctor module + test file for Step 2 even though template/init/dogfood unit tests logically belong to Step 1 outcomes.
3. **Step 2 tests** — When adding `tests/config/spine-config-testing.test.mjs`, include the three Step 1 assertions (template mirrors constants, `runInit` dry-run overlay, dogfood non-empty) alongside doctor-check cases; register the file under `tests/config/*.test.mjs` in `package.json` if not already present.
4. **Step 3** — Reserve full `npm test` + `npm run coverage:check` for Step 3 verification; Step 1 plan does not require running the suite.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan correctly aligns template and init defaults with package.json script patterns (including testWithCoverage in applySpineInitDefaults), refreshes dogfood with non-empty split build/test commands, and defers doctor warnings and tests to later steps per PROMPT boundaries. Document intentional dogfood split vs init combined commands in STATUS Discoveries. Proceed to Step 2 doctor warning after code review."
}
```
