## Plan Review: Step 2 — Bootstrap doc link + example

### Verdict: APPROVE

### Summary

Step 2 correctly limits adoption surfacing to **`README.md`** and **`docs/adoption/bootstrap-checklist.md`**: a feature-summary bullet, an Adoption subsection with a copy-paste pi prompt, and bootstrap step 3 **Option A** (skill) vs **Option B** (manual packet), plus a quick-reference row. Examples use greenfield `spine-tasks/`, `pi install /path/to/pi-spine -l`, and CONTEXT/deps updates — matching PROMPT mission and the “no npm publish” constraint. Carrying **prompt-template hygiene** (em dash heading, `.spine/agents/worker.md` naming, strip `#### Segment:` polyrepo blocks) into Step 2 satisfies Step 1 review’s “authoritative before doc links” gate without touching execution code or tests. Step 1 skill artifacts remain the source of truth; Step 2 only wires consumers to them.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Plan-before-implementation ordering** — Commit `10b8390` landed Step 2 docs (and template cleanup) before this plan review. Going forward, land the Step 2 commit after plan APPROVE; if already implemented, treat any follow-up as doc/example polish only.
- **Step 2 plan review closure** — After this review file is written, record `.reviews/2-20260603T171733.md` under Step 2 in `STATUS.md` (mirror Step 1) before task completion / `.DONE`.
- **Example path consistency** — README example says `Update CONTEXT.md` while bootstrap uses `spine-tasks/CONTEXT.md`. Align README to the configured tasks root (or `{tasksRoot}/CONTEXT.md`) so greenfield copy-paste matches bootstrap-checklist.
- **Commit scope clarity** — Step 2 commit message cites template stripping; keep the plan explicit that `skills/create-spine-tasks/references/prompt-template.md` edits are **Step 1 review carryover**, not new skill behavior, so reviewers do not expect SKILL.md changes in Step 2.

### Suggestions

- **Cross-links** — README ↔ `SKILL.md` ↔ bootstrap-checklist reciprocal links are sufficient for greenfield adoption; migrate section can stay manual-first (optional one-line pointer to the skill after `spine migrate-from-taskplane`).
- **Review level in examples** — Bootstrap’s “Level 0 smoke, then M-sized implementation tasks” matches skill guidance; no change needed.
- **Out of PROMPT File Scope** — `docs/adoption/real-project-readiness.md` still lists the skill as future work; optional follow-up when SP-054 closes (not required for Step 2).
- **Verification** — No test gate for Step 2; optional smoke: after `pi install . -l`, confirm pi lists `create-spine-tasks` via package.json `"skills": ["./skills"]`.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-054",
  "feedback": "Step 2 plan wires README and bootstrap-checklist with create-spine-tasks example prompts, local pi install, and cross-links to SKILL.md; includes Step 1 template hygiene before docs cite the skill. No npm publish, no engine changes, no tests. Record this review in STATUS; align README CONTEXT path with bootstrap. Proceed to task completion after plan review is logged."
}
```
