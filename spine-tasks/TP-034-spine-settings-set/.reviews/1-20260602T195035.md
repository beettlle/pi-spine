## Plan Review: Step 1 — Set implementation

### Verdict: APPROVE

### Summary

Step 1 correctly delivers FR-CFG-03's write path as the mirror of TP-033 show: a pure `applySetting(config, path, value)` merge in `src/cli/settings-set.mjs`, a dedicated `writeSpineConfigAtomic(projectRoot, config)` helper (no reusable atomic writer exists in init/migrate — tmp + rename is the right choice), and a testable `runSettingsSetOperation` that chains TP-032 `parseSettingPath` / `validateSettingValue`, post-merge `validateSpineConfig`, and optional persistence. Wiring `set` through `bin/spine-settings.mjs` (load config → operate → write unless `--dry-run`) matches the established `src/cli/` logic + `bin/` entry split. The planned test matrix — unit merge, unknown path, invalid value, atomic write, CLI set→show round-trip, dry-run no-op, and `--json` metadata — satisfies PROMPT deliverables and the success criterion (`spine settings set lanes.maxParallel 2` then `show` reflects change).

### Issues Found

None blocking.

### Missing Items

- **`validateSpineConfig` in Step 1 checklist** — PROMPT deliverable #1 requires full-document validation after merge; Step 1 bullets only name `applySetting` and atomic write. Ensure implementation always runs `validateSpineConfig(nextConfig)` before write and surfaces `suggestedCommand` on failure (TP-032 registry alone is insufficient per TP-032 review notes).
- **`bin/spine-settings.mjs` in artifacts list** — File Scope requires adding the `set` subcommand; include it explicitly in Step 1 artifacts alongside `src/cli/settings-set.mjs` and tests so verification does not miss CLI wiring.
- **`--json` in Step 1 checklist** — Deliverable specifies JSON output shape `{ path, previousValue, newValue, dryRun, wrote }` and JSON errors; add an explicit bullet so human/JSON parity is not deferred accidentally.
- **Missing-config CLI tests** — Plan covers validation rejection and dry-run; add one integration test that `set` on a project without config exits 1 with `suggestedCommand: spine init` (human + JSON), mirroring TP-033 show behavior.

### Suggestions

- **Module layout** — Export `applySetting`, `writeSpineConfigAtomic`, and `runSettingsSetOperation` for unit tests; keep `bin/spine-settings.mjs` as a thin loader/writer wrapper (same `{ exitCode, output, error?, suggestedCommand? }` contract as show).
- **Merge semantics** — `applySetting` should clone input (`structuredClone`) and only create intermediate objects for registered dotted paths; do not mutate the loaded config in place. Reuse `getValueAtPath` from `settings-show.mjs` for `previousValue`.
- **Argv parsing** — Treat `--dry-run` and `--json` as flags; join remaining positional args after `<path>` into the raw value so string settings with spaces work. Reject missing path/value with usage error.
- **Atomic write details** — Use a pid/random tmp suffix under `.spine/spine-config.json.*.tmp`, write pretty JSON with trailing newline, rename, and best-effort tmp cleanup on failure.
- **Scope boundary** — Step 1 commit should include only TP-034 files; defer README/`bin/spine.mjs` help updates to Step 2 verification per PROMPT "Must Update" unless already present from parallel work.
- **Do NOT guardrails** — Never silently modify non-editable keys; only paths from TP-032 registry are accepted via `parseSettingPath`.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan correctly implements spine settings set via applySetting merge, validateSpineConfig gate, writeSpineConfigAtomic persistence, bin/spine-settings.mjs wiring, and temp-fixture tests including dry-run and set→show round-trip. Add explicit checklist items for validateSpineConfig, --json, bin wiring, and missing-config errors. Proceed without TUI/slash scope (TP-035)."
}
```
