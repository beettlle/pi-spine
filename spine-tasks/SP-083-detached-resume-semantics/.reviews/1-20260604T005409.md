## Plan Review: Wait semantics (Step 1)

### Verdict: APPROVE

### Summary

The revised Step 1 plan closes the prior REVISE feedback: default detached start/resume returns `{ ok: true, status: "engine_started" }` on a fresh `running` transition, `--wait-terminal` blocks until batch terminal/`paused` or (on resume) the resumed task reaches a terminal task status via `evaluateDetachedResumeWait`, and `waitTerminal` is wired through `parseBatchArgs`, `startBatchDetached`, and `resumeBatchDetached`. Runbook §3 Monitor and §6 Resume now document the return contract, status values, and `--wait-terminal`; the diff no longer bundles Step 2 failure diagnostics or unrelated SP-072 prose.

### Issues Found

None blocking.

### Missing Items

- **Step 3 tests deferred** — `engine_started` vs `resume_completed` / `--wait-terminal` cases belong in Step 3 per PROMPT; Step 1 should not claim test coverage complete.
- **STATUS closure** — After this review lands, record `.reviews/1-20260604T005409.md` in `STATUS.md` Step 1 before marking the step complete.
- **Step 2 boundary** — Keep timeout failure messaging, journal `task.failed` excerpts, and worker-log tails out of the Step 1 commit (Step 2 scope).

### Suggestions

- **Symmetry for start** — `waitForDetachedBatchStart` mirrors resume (`engine_started` vs `start_completed` with `--wait-terminal`); shared operator contract is correct.
- **`evaluateDetachedResumeWait`** — Requiring `updatedAt > updatedAtBefore` before completion avoids stale-state false positives; terminal batch, `paused`, and per-task terminal via `resumedTaskReachedTerminal` are the right `--wait-terminal` signals.
- **Runbook §5 Start examples** — Optional polish: add `spine batch start --wait-terminal` beside the resume examples in §6; §3 Monitor contract already covers both commands.
- **Plan-before-implementation ordering** — Uncommitted implementation precedes this re-review (same pattern as SP-072/SP-079). Treat this APPROVE as the gate before the Step 1 commit; proceed to Step 2 failure surfacing after Step 1 lands.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 wait-semantics plan (revised): engine_started on default detached return; resume_completed/start_completed with --wait-terminal via evaluateDetachedResumeWait + taskId terminal check; waitTerminal wired through spine-batch.mjs; success CLI headlines + runbook §3/§6 contract docs. Prior REVISE blockers resolved (runbook added, Step 2 diagnostics excluded). Defer tests to Step 3. Record plan review in STATUS; land Step 1 commit then proceed to Step 2."
}
```
