## Plan Review: Step 1 — Local install doc

### Verdict: APPROVE

### Summary

Step 1 correctly scopes documentation-only work: a new `docs/adoption/local-install.md` covering git clone, `pi install … -l`, optional `npm link`, consumer `file:` dependency, bootstrap (`spine init` / `doctor` / `plan` / `preflight`), slash-command verification, and PATH troubleshooting (empty `spine` on PATH). README "Adoption (pre-publish)" plus `real-project-readiness.md` cross-links satisfy PROMPT deliverables and the Must Update list without pulling Step 2 doctor logic or Step 3 tests into this commit. The five-section doc structure (prerequisites → clone → pi install → CLI/file dep → consumer bootstrap → verify → troubleshooting) matches Phase 9 adoption tiers and TP-030 baseline.

### Issues Found

None blocking.

### Missing Items

- **Verify slash-command name** — Use `pi list` (or an existing slash command such as `/spine-status`) for install verification. There is no `/spine-help` in `extensions/spine/slash-commands.ts`; referencing it will confuse operators.
- **Stale-PATH doc vs Step 2 sequencing** — Troubleshooting and README already describe doctor **"spine on PATH (stale)"** warnings. Either land Step 2 before treating the doc as final, or phrase stale-PATH guidance as expected post-Step-2 behavior so pilots are not misled mid-task.
- **`real-project-readiness.md` in Step 1 checklist** — PROMPT Must Update requires the adoption-plan link; add an explicit Step 1 bullet so verification does not skip it (commit scope should include all three doc files).

### Suggestions

- **Commit boundary** — Step 1 commit: `docs/adoption/local-install.md`, `README.md`, `docs/adoption/real-project-readiness.md` only. Defer `bin/spine.mjs`, `src/doctor/stale-path.mjs`, and tests to Steps 2–3 per File Scope.
- **Command examples** — Prefer absolute paths in consumer examples (`pi install /absolute/path/to/pi-spine -l`) as PROMPT specifies; keep relative `file:../pi-spine` for monorepo JSON only.
- **Doctor advisory tone** — When documenting missing PATH, align with doctor's advisory (warning, not fail) semantics so operators know slash commands can work without global `spine`.
- **Related docs table** — Link to `real-project-readiness.md` and README is sufficient; `v1.0-checklist.md` is optional context for post-publish.
- **No npm publish** — Do not add publish/version steps; Quick start already notes "(when published)" for `pi install npm:pi-spine`.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "TP-043",
  "feedback": "Step 1 plan correctly delivers pre-publish local install documentation (git clone, pi install -l, npm link, file: dependency, slash verification, PATH troubleshooting) plus README and real-project-readiness pointers without doctor implementation or tests. Fix /spine-help → pi list; coordinate stale-PATH troubleshooting text with Step 2; include real-project-readiness in Step 1 commit scope. Proceed with docs-only Step 1 commit."
}
```
