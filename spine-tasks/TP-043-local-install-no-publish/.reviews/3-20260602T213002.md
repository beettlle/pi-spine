## Plan Review: Step 3 — Tests + verification

### Verdict: APPROVE

### Summary

Step 3 correctly closes TP-043 with a **tests-only** commit scoped to `tests/doctor/stale-path.test.mjs`, mirroring TP-028's Step 3 pattern (pure unit tests against `src/doctor/stale-path.mjs`, no further `bin/spine.mjs` changes). The plan exercises injectable `which` / `spawn` / `stat` / `realpath` hooks deferred from Step 2 and covers the matrix called out in the Step 2 plan review: PATH absent (warning + `suggestedCommand`), PATH matches package `bin/spine.mjs` (no warning), semver mismatch (stale label + detail), mtime-only mismatch (stale without version drift), and `readSpineCliVersion` against the real checkout binary. Every stale/missing scenario keeps **`ok: true`** (advisory-only contract aligned with TP-028 and Step 2). Verification gate is `npm run typecheck && SPINE_WORKER_STUB=1 npm test` per PROMPT Environment; no npm publish, README, or doctor-module edits belong in this step. Optional doctor CLI integration smoke is correctly omitted (TP-028 treats unit coverage as sufficient).

### Issues Found

None blocking.

### Missing Items

- **Step 3 commit boundary** — Commit should include only `tests/doctor/stale-path.test.mjs` (and task `STATUS.md` / review artifacts if your workflow stages them). Do not re-touch `src/doctor/stale-path.mjs` or `bin/spine.mjs` unless a test reveals a defect.
- **Stat failure path** — Step 2 noted un-stat-able PATH binaries should warn with `npm link`; add one mocked `stat` throw case if not already present (low priority; behavior is in module).
- **Doc label spot-check** — Manually confirm `spine doctor` output label **"spine on PATH (stale)"** matches `docs/adoption/local-install.md` and README adoption text (verification only; no doc commit unless labels drifted).

### Suggestions

- **Advisory assertions** — In stale/missing tests, assert `ok === true` and `issueCount` unaffected (implicit via `checks.push` wiring; unit tests should lock `warning` without `ok: false`).
- **resolveSpineOnPath smoke** — Keep the injectable `which` test; default `which` behavior is covered indirectly via `readSpineCliVersion` on real `bin/spine.mjs`.
- **Full suite record** — After green run, record pass count in `STATUS.md` (e.g. 304 tests) for Step 3 completion audit.
- **No scope expansion** — Skip consumer-repo e2e, publish metadata, or preflight changes; PROMPT completion is satisfied by unit tests + suite green.

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "TP-043",
  "feedback": "Step 3 plan correctly adds tests/doctor/stale-path.test.mjs with mocked PATH scenarios (missing PATH, match, version stale, mtime stale, real --version parse) and runs typecheck + SPINE_WORKER_STUB=1 npm test. Keep commit tests-only; optional stat-failure case and doc label spot-check. Proceed with implementation."
}
```
