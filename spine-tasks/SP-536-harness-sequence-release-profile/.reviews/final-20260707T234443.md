### Verdict: PASS

### Summary

SP-536 implements FR-STA-25 ([#54](https://github.com/beettlle/pi-spine/issues/54) Partial): a release sequence profile with wave caps (max 4 tasks/wave), gate-only dry-run annotations, `--dry-run` wave planning, and stricter `--auto-approve-gate` refusal for release scopes on real pi. Contract verification passed independently (11/11 tests, typecheck clean); changes stay within File Scope; `.DONE` is present.

### Contract verification

| Check | Result |
|-------|--------|
| `testCommand` | PASS — `npm run typecheck && SPINE_WORKER_STUB=1 node --experimental-strip-types --test tests/batch/sequence-release-profile.test.mjs` → 11/11 pass |
| `fileScopeMustChange` (`src/batch/sequence.mjs`) | PASS — release profile constants, resolver, wave-cap validation, dry-run header, `runSequence` integration |

### Completion criteria

- **`spine run sequence` release profile with dry-run and gate-only loop:** PASS — `SEQUENCE_RELEASE_PROFILE`, `resolveSequenceProfile` (comma scope or `--profile release`), `buildSequenceDryRunPlan` with gate-only annotations, wave-cap enforcement before dry-run and execute paths.
- **Auto-approve gate safety for release scope:** PASS — `validateSequenceAutoApproveGate` returns release-specific refusal on real pi; `runSequence` refuses before dry-run/execute; stub mode and `--force` paths preserved for non-release behavior.

### Step plan review

| Step | Status | Notes |
|------|--------|-------|
| 0 Preflight | Complete | SP-390/SP-388 context; SP-535 manifest waves confirmed |
| 1 Release profile | Complete | Constants, dry-run, doctor guardrails in `sequence-safety.mjs`, CLI `--profile` passthrough |
| 2 Tests | Complete | 11 tests cover scope detection, wave caps, dry-run output, auto-approve refusal, `runSequence` integration |
| 3 Testing | Complete | Contract pass verified; full-suite env failures noted as pre-existing (nested batch spawn) |
| 4 Delivery | Complete | Cross-links to `docs/release/manifest-v1.10.0-example.md` in code, doctor, and `bin/spine-run.mjs`; `.DONE` created |

### Scope and regressions

- Changes limited to File Scope: `src/batch/sequence.mjs`, `src/cli/sequence.mjs`, `src/doctor/sequence-safety.mjs`, `bin/spine-run.mjs`, `tests/batch/sequence-release-profile.test.mjs`, plus task artifacts.
- No SP-388 CLI re-implementation; no npm publish automation.
- Existing non-release sequence behavior preserved (profile null unless comma scope or `--profile release`).

### Non-blocking notes

- `buildSequenceReleaseProfileDoctorCheck` is exported and unit-tested but not wired into `run-doctor-checks.mjs`; release safety is enforced at `runSequence` entry (same pattern as SP-390 auto-approve advisory).
- Full `npm test` suite failures are attributed to worker environment, not this diff; contract tests are sufficient for task closure.

```json
{"verdict":"PASS","feedback":"FR-STA-25 complete: release profile with wave caps, dry-run wave plan, gate-only annotations, and release-scoped auto-approve refusal. Contract 11/11 pass; file scope and completion criteria satisfied. Approve for integration."}
```
