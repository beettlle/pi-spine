### Verdict: PASS

### Summary

SP-588 correctly extracts the nested-spawn guard into `src/batch/batch-guards.mjs` and re-exports `detectNestedWorkerContext` from `engine.mjs`, preserving the public API used by contract tests. Both modules are ≤500 LOC (`engine.mjs` 497, `batch-guards.mjs` 69); machine verifier PASS; contract test `node --test tests/batch/nested-spawn-guard.test.mjs` is 7/7 pass; `.DONE` and STATUS completion criteria are present. Full-suite failures noted in STATUS are pre-existing `review-step.mjs` LOC issues outside File Scope and do not block this split.

```json
{"verdict":"PASS","feedback":"Nested-spawn guard split meets PROMPT contract: batch-guards.mjs created, detectNestedWorkerContext re-exported from engine.mjs, rejectNestedBatchStart used by startBatch, both files ≤500 LOC, fileScopeMustChange matched, contract tests 7/7 pass, .DONE present. Proceed."}
```
