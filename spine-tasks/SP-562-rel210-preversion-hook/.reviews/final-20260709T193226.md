### Verdict: PASS

### Summary

SP-562 implements #175 §B: `package.json` adds `"preversion": "npm run release:check"`, blocking `npm version` when release gates fail. `docs/release/npm-publish.md` documents the hook, clarifies that `--no-git-tag-version` still runs `preversion`, and documents `--ignore-scripts` as the emergency bypass. Contract `testCommand` passes (typecheck clean; 2/2 preversion-hook tests). File scope is respected; `.DONE` and STATUS delivery checkboxes are complete.

```json
{"verdict":"PASS","feedback":"Mission criteria met: preversion delegates to release:check; docs cover hook behavior and escape hatches; tests assert script wiring and documentation. Machine verifier PASS (testCommand, fileScopeMustChange on package.json). Completion criterion (npm version fails when release:check fails) follows standard npm lifecycle semantics and is not integration-tested, which is acceptable for this S-sized config task. No scope creep beyond task artifacts (STATUS, .DONE)."}
```
