### Verdict: PASS

### Summary
The task successfully implements optional, fail-closed provider probes for Z.ai, Kimi, and Cursor without requiring a live network for tests or scraping undocumented HTML. The snapshot integration correctly merges live stats while preserving `estimate` and `absent` states, successfully avoiding the exposure of any credentials. All machine verifier constraints and tests passed cleanly.

```json
{
  "verdict": "PASS",
  "feedback": "Great implementation. The probe adapters degrade cleanly on errors, the `hasCursorAdminKey` requirement guards against misusing regular keys, and `fetch` is well mocked in tests ensuring no live network usage. No secrets are leaked."
}
```