### Verdict: PASS

### Summary

SP-362 delivers `spine wait` for issue #46: a blocking poll loop over `reconcileBatch` that exits 0 when `diagnosis` matches the `--until` set and 1 on `--timeout`, with optional `--json` final snapshot output. Contract checks pass (machine verifier PASS; `src/cli/wait.mjs` changed; 87.59% line coverage ≥ 77%; `tests/cli/wait.test.mjs` present), runbook documents CI examples, GitHub #46 is closed, and `.DONE` is present.

Implementation reuses SP-360 watch primitives (`DEFAULT_WATCH_INTERVAL_SEC`, `buildWatchSnapshot`, `reconcileBatch`), stays within File Scope, and includes unit plus integration tests for parsing, polling, exit codes, and CLI wiring.

```json
{"verdict":"PASS","feedback":"All PROMPT.md completion criteria satisfied: spine wait --until DIAG[,DIAG...] [--timeout DURATION] [--json] implemented with reconcile polling, exit 0 on match / exit 1 on timeout, final JSON snapshot on --json; CLI wired in bin/spine.mjs; runbook CI examples added; contract testCommand passes; coverage 87.59% >= 77%; issue #46 closed; .DONE created. No blocking issues."}
```
