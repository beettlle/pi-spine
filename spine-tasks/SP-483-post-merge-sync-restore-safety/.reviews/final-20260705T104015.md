### Verdict: PASS

### Summary

SP-483 is complete. `syncPlumbingMergePathsToWorktree` now pre-filters paths with `git cat-file -e` against the merge commit, skips absent paths with debug logging, and defensively continues on pathspec-mismatch errors during `git restore`. Three targeted tests in `tests/batch/integrate-worktree-sync.test.mjs` cover the coverage-artifact scenario, successful restore of merge-commit paths, and skipping diff-listed paths absent from the merge commit. Machine contract verification passed (88.54% line coverage ≥ 77%), and independent re-run of the contract test command succeeded (typecheck + 1624 tests, 0 failures). All steps and completion criteria are satisfied; no blocking scope creep.

```json
{"verdict":"PASS","feedback":"Implementation matches PROMPT mission: paths absent from the merge commit are skipped before restore, with a secondary pathspec-mismatch guard. Contract checks pass, file scope is respected (integrate-worktree.mjs + new sync test only), and required tests cover both the failure mode (#130 coverage paths) and correct restore behavior. Step 3 documentation review is adequate — no #130 workaround note required removal in operator-runbook.md. Non-blocking note: operator-runbook.md line ~774 still describes pre-fix git show/git add behavior; optional follow-up outside this task's Must Update list."}
```
