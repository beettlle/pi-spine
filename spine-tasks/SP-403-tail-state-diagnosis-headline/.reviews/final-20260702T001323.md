### Verdict: PASS

### Summary

SP-403 satisfies GitHub issue #68 Tier 1: when `phase === "running"` with no active workers, `buildHeadline` now derives operator-readable tail-state text from `macroPhase` and journal signals instead of the generic `{batchLabel} is running`. Implementation is focused (`diagnosis-tail-state.mjs` helper, `macro-phase.mjs` tail derivation, reconcile wiring), tests cover unit and fixture reconciliation paths, and contract verification passes (typecheck, 1358/1358 tests including 9 diagnosis tests, 88.20% line coverage ≥ 77%).

Minor scope beyond declared File Scope (`reconcile.mjs`, `diagnosis-tail-state.mjs`, fixture JSON) is justified and documented in STATUS discoveries; no wave panel or engine behavior changes.

```json
{"verdict":"PASS","feedback":"Tail-state diagnosis headlines are implemented and wired through reconcile → buildHeadline; macroPhase alignment for idle-running batches is covered by unit and fixture tests. Machine contract checks pass. Safe to land."}
```
