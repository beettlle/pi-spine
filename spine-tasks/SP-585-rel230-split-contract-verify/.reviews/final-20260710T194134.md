### Verdict: PASS

### Summary

SP-585 successfully extracts the parse half of `contract-verify.mjs` into `contract-parse.mjs` (244 LOC) while preserving the public API via re-exports from `contract-verify.mjs` (499 LOC). The contract test command passes (2/2), machine verifier reports PASS, and the grandfathered `bin/spine-cli/verify.mjs` was not modified. Execution/npm-guard logic remains in `contract-verify.mjs` for the paired SP-603 task.

```json
{"verdict":"PASS","feedback":"Extract is complete and behavior-preserving. contract-parse.mjs holds gating helpers, listChangedFiles, matchesContractPattern, and verifyStubFileScopeMustChange; contract-verify.mjs re-exports all 11 parse symbols so existing callers (lane-commit.mjs, diagnosis-stub.mjs) remain unchanged. Scope is clean: only src/batch/contract-parse.mjs, src/batch/contract-verify.mjs, and task artifacts (.DONE, STATUS.md). Non-blocking: .DONE marker is empty; full npm test worker-harness failures are pre-existing and outside contract scope."}
```
