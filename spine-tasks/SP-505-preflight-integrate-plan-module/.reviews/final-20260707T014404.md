### Verdict: PASS

### Summary

SP-505 completes the final preflight Strangler slice: integrate/merge/plan checks move into `src/config/preflight/integrate-plan.mjs` (460 LOC) while `spine-preflight-lib.mjs` thins to orchestration and re-exports (388 LOC). Public symbols are preserved via re-exports; `runBatchPreflight` check ordering is unchanged. Contract tests pass independently (1734/1734); machine verifier reports 88.53% in-scope line coverage (≥77%).

### Completion criteria

| Criterion | Status |
|-----------|--------|
| `integrate-plan.mjs` exists and ≤500 LOC | ✅ 460 lines |
| Orch-merge, prelanded, plan checks moved | ✅ `listDivergentOrchMergeRiskPaths`, `predictOrchMergeConflictRisk`, `checkOrchMergeConflictWarn`, `listPrelandedFileScopeStaleTasks`, `checkPrelandedFileScopeWarn`, `runPreflightPlanCheck` |
| `spine-preflight-lib.mjs` ≤500 LOC | ✅ 388 lines |
| Re-exports preserve public API | ✅ All moved symbols re-exported from lib |
| `runBatchPreflight` orchestration retained | ✅ Same check sequence; integrate-plan imported for three checks |
| Regression tests passing | ✅ Contract `testCommand` rerun: 1734/1734 pass |
| `fileScopeMustChange` | ✅ Both scoped source files changed |
| Coverage ≥77% | ✅ Machine verifier: 88.53% |
| `.DONE` present | ✅ |
| Issue #176 closed | ✅ Documented in STATUS |

### Code review notes

- **Extract quality:** Faithful Strangler move; private git-ref helpers stay inside `integrate-plan.mjs`. No logic reordering in `runBatchPreflight`.
- **API surface:** `PRD_REL_PATH`, `ORCH_MULTI_FILE_MERGE_RISK_PATHS`, and all exported check functions remain importable from `spine-preflight-lib.mjs`.
- **Bug fix during refactor:** `checkDoctor` `suggestedCommand` regression caught and fixed before commit (STATUS discovery).
- **Scope:** Test files unchanged (imports still resolve through lib re-exports). No scope creep beyond file scope.
- **Non-blocking:** Full suite in worker sessions may need `SPINE_IS_WORKER` unset; documented in STATUS for engine re-verification.

```json
{"verdict":"PASS","feedback":"Preflight split complete: integrate-plan.mjs (460 LOC) holds orch-merge, prelanded, and plan checks; spine-preflight-lib.mjs (388 LOC) retains orchestration + re-exports with unchanged public API and check ordering. Contract tests pass (1734/1734; coverage 88.53% ≥77%). Closes #176 epic. Proceed."}
```
