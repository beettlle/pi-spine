### Verdict: PASS

### Summary
The implementation correctly flushes pi output for salvage context when `.DONE` is missing, mirroring the non-zero exit path. It correctly uses the existing `truncateLiveLogBytes` helper to cap output and ensures tests verify the behavior without regressions.

```json
{"verdict":"PASS","feedback":"The implementation correctly flushes pi output for salvage context when `.DONE` is missing. The existing truncation function is used to prevent unbound log sizes. The scoped tests are correct and verify the change without regressions."}
```