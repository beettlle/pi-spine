### Verdict: PASS

### Summary

SP-560 completes the remaining #185 scope: detached-first automation guidance (automation rule, orphan recovery recipe, Cursor background-shell anti-pattern) is synced across `agent-orchestrated-waves.md`, `outer-loop.md`, and `SKILL.md`, with operator runbook §4 cross-linking agent-orchestrated-waves on the branch. Diagnosis UX distinguishes parent-shell exit from engine crash via `inferEngineOrphanCause`, headlines link stale `resilience.enginePid` to `detached-engine.log`, and suggested commands prefer detached `resume --force`. Contract `testCommand` passes (typecheck clean; 10/10 diagnosis-parent-exit tests). Delivery artifacts (`.DONE`, #185 comment) are present; default attach mode unchanged.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met. Documentation is discoverable from runbook and orchestrate-waves skill. Diagnosis correctly classifies parent_exit, parent_exit_likely, crash, and unknown (SP-297 detached-resume stall). reconcile.mjs wiring is minimal and necessary. Operator-runbook §4 cross-link landed on branch under SP-555 rather than SP-560 commit — content satisfies PROMPT. No blocking scope creep or regressions."}
```
