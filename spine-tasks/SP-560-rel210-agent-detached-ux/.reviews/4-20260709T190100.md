### Verdict: APPROVE

### Summary

SP-560 delivers the remaining #185 scope: detached-first automation guidance is synced across `agent-orchestrated-waves.md`, `outer-loop.md`, and `SKILL.md` (automation rule, orphan recovery recipe, and Cursor background-shell anti-pattern row). Diagnosis now distinguishes parent-shell exit from engine crash via `inferEngineOrphanCause`, parent-exit headlines link to `detached-engine.log`, and suggested commands prefer detached `resume --force`. Contract `testCommand` passes (10/10 diagnosis-parent-exit tests; typecheck clean). Step 4 delivery artifacts (`.DONE`, #185 comment) are present.

Operator runbook §4 cross-link to `agent-orchestrated-waves.md` is on the branch (landed in SP-555) and satisfies the PROMPT requirement even though it is outside the SP-560 commit diff. `reconcile.mjs` and `diagnosis-parent-exit.mjs` extend file scope minimally for necessary wiring — acceptable.

```json
{"verdict":"APPROVE","feedback":"Documentation, diagnosis UX, tests, and delivery criteria met. No blocking scope creep or regressions. Minor note: operator-runbook §4 cross-link was committed under SP-555 on the same branch rather than SP-560 — content is correct and discoverable."}
```
