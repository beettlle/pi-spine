### Verdict: PASS

### Summary
The changes correctly implement the requested functionality. ETIMEDOUT during `pi --list-models` is now treated as an advisory warning rather than a hard failure that incorrectly suggests `pi login`. The scope constraint was met, and all tests pass with appropriate coverage for both direct errors and exceptions.

```json
{
  "verdict": "PASS",
  "feedback": "Implementation correctly differentiates ETIMEDOUT from other errors, tests provide solid coverage, and all completion criteria are met."
}
```