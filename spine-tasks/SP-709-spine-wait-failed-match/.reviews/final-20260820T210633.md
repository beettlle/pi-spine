### Verdict: PASS

### Summary
The task successfully satisfies all criteria. The implementation cleanly leverages `result.phase === "failed"` to allow the generic `--until failed` to wake on terminal batch failures regardless of the specific failure diagnosis, fixing the orphaned worker/engine issue. Tests are comprehensive, and the decision to skip doc updates is correct since the existing wait loop examples already use `failed`.

```json
{
  "verdict": "PASS",
  "feedback": "Great work. The fix safely adds the `phase === 'failed'` check without disrupting explicit diagnosis matching. The unit tests are highly rigorous and cover all edge cases mentioned in the prompt. Contract tests pass and no out-of-scope files were modified."
}
```
