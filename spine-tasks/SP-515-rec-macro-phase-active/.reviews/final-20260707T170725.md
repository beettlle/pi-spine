### Verdict: PASS

### Summary

SP-515 correctly fixes #165 by gating `FAILED_DIAGNOSES` for recoverable drift/orphan diagnoses when the batch is `running`/`merging` with active workers or all tasks terminal, deferring to gating/merging tail derivation instead of macro `failed`. Eight targeted regression tests cover active-worker execution, terminal-success gating paths, non-recoverable failure, and running-tail headline suppression; all pass independently. Changes stay within File Scope, `.DONE` is present, contract verifier reports PASS (tests, `macro-phase.mjs` change, 88.81% line coverage ≥ 77%).

```json
{"verdict":"PASS","feedback":"Implementation matches PROMPT completion criteria: `isRecoverableDriftOrphanWhileActive` prevents macro `failed` for state_drift/engine_orphaned/worker_orphaned under active running/merging batches with workers or terminal success; `buildRunningTailHeadline` suppresses Failed in running tail. Tests (8/8) and machine contract checks pass. No scope creep beyond listed files. Ready to integrate."}
```
