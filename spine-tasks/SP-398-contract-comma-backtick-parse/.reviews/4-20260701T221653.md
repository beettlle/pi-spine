### Verdict: APPROVE

### Summary

Step 4 delivery is complete. The cumulative branch diff (`566c4fa..HEAD`) implements the issue #61 fix: `parseContractPathList` strips a single outer backtick wrapper before comma-splitting, `validateContract` warns or errors via `detectCommaInSingleBacktickPathLists`, regression tests cover SP-396 paths and stub verification, `contract-template.md` documents per-path backtick guidance, issue #61 is closed, and `.DONE` is present on HEAD. Independent verification: typecheck passed; all SP-398 contract-parse and stub-enforcement tests passed (1353/1354 suite pass; one unrelated flaky failure in `contract-stall-override.test.mjs`).

### Findings

**In scope and correct**
- `parseContractPathList` fix preserves per-path backticks, plain comma lists, and `see File Scope` (outer single-backtick match applies only to fully wrapped cells).
- Authoring guard targets `fileScopeMustChange` and `artifactsMustExist` with optional warn vs required error behavior per PROMPT Step 2.
- `tests/tasks/contract-parse.test.mjs` adds SP-396 reproduction, artifacts parsing, and validate-mode coverage.
- `tests/batch/stub-contract-enforcement.test.mjs` exercises end-to-end `verifyStubFileScopeMustChange` for the comma-in-backtick fixture.

**Non-blocking notes**
- Working-tree `git diff` shows only `.spine/rules-manifest.json` timestamp; substantive task changes are committed on the branch.
- `stub-contract-enforcement.test.mjs` is outside listed File Scope but directly validates the reported batch failure; acceptable.
- Full suite run failed one unrelated test (`runWorker with contract stall override survives beyond global stall budget`); SP-398 tests all passed.

```json
{"verdict":"APPROVE","feedback":"Delivery complete: parser fix, authoring guard, template docs, regression and stub-enforcement tests. Issue #61 closed; .DONE on HEAD. No blocking scope or correctness issues."}
```
