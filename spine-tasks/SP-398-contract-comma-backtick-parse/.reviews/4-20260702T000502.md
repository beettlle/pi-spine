### Verdict: APPROVE

### Summary

Step 4 delivery is complete on branch `566c4fa..HEAD`. The cumulative diff implements issue #61: `parseContractPathList` unwraps a single outer backtick before comma-splitting, `validateContract` warns or errors via `detectCommaInSingleBacktickPathLists`, regression tests cover SP-396 paths and stub verification, `contract-template.md` documents per-path backtick guidance, issue #61 is closed, and `.DONE` is present on HEAD with valid JSON. Independent verification: typecheck passed; full suite 1354/1354 pass including all four new SP-398 tests.

### Findings

**In scope and correct**
- `parseContractPathList` strips only fully wrapped single-backtick cells (`^`([^`]+)`$`), preserving per-path backticks, plain comma lists, and `see File Scope`.
- Authoring guard targets `fileScopeMustChange` and `artifactsMustExist` with optional warn vs required error behavior per PROMPT Step 2.
- `tests/tasks/contract-parse.test.mjs` adds SP-396 reproduction, artifacts parsing, and validate-mode coverage.
- `tests/batch/stub-contract-enforcement.test.mjs` exercises end-to-end `verifyStubFileScopeMustChange` for the comma-in-backtick fixture.

**Non-blocking notes**
- Working-tree `git diff` shows only `.spine/rules-manifest.json` timestamp; substantive task changes are committed on the branch.
- `stub-contract-enforcement.test.mjs` is outside listed File Scope but directly validates the reported batch failure; acceptable.
- Issue #61 was closed before this branch lands on main; acceptable for lane delivery per Step 4 checklist.

```json
{"verdict":"APPROVE","feedback":"Delivery complete: parser fix, authoring guard, template docs, regression and stub-enforcement tests. Issue #61 closed; .DONE on HEAD. Independent verification: typecheck OK, 1354/1354 tests pass. No blocking scope or correctness issues."}
```
