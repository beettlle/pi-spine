### Verdict: PASS

### Summary

SP-398 satisfies all PROMPT.md completion criteria for issue #61. `parseContractPathList` now strips a single outer backtick wrapper before comma-splitting, `validateContract` warns or errors via `detectCommaInSingleBacktickPathLists`, regression tests cover SP-396 paths and authoring-guard modes, and `contract-template.md` documents per-path backtick guidance. Machine contract verification passed; independent runs confirm typecheck OK, 1354/1354 tests pass, and line coverage 88.25% ≥ 77%. Issue #61 is closed and `.DONE` is present on HEAD.

### Findings

**In scope and correct**
- Parser fix preserves per-path backticks, plain comma lists, and `see File Scope` (outer single-backtick match applies only to fully wrapped cells).
- Authoring guard targets `fileScopeMustChange` and `artifactsMustExist` with optional warn vs required error behavior.
- `tests/tasks/contract-parse.test.mjs` adds SP-396 reproduction and validate-mode coverage; `tests/batch/stub-contract-enforcement.test.mjs` exercises end-to-end `verifyStubFileScopeMustChange`.

**Non-blocking notes**
- Unstaged working-tree edits (empty `.DONE`, `.spine/rules-manifest.json` timestamp) are unrelated to committed delivery; HEAD retains valid `.DONE` JSON.
- `stub-contract-enforcement.test.mjs` is outside listed File Scope but directly validates the reported batch failure; acceptable.

```json
{"verdict":"PASS","feedback":"Delivery complete: parser fix, authoring guard, template docs, regression and stub-enforcement tests. Contract verifier PASS (87.85% line coverage). Independent verification: typecheck OK, contract-parse suite and full suite 1354/1354, coverage gate 88.25%. Issue #61 closed; .DONE on HEAD."}
```
