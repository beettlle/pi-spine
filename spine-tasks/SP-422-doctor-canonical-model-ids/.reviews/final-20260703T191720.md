### Verdict: PASS

### Summary

SP-422 delivers a clean model-id validation layer (`src/config/model-id.mjs`) that normalizes pi TUI display labels to canonical `provider/model` format and rejects unrecognized formats. The new doctor check (`src/doctor/agent-models.mjs`) flags misconfigured pins with actionable suggested commands, and `settings-set.mjs` now intercepts display labels at write time. All 27 targeted tests pass, coverage is 88.55% (gate: 77%), and the change is well-scoped to the four declared files plus a minimal 4-line wiring addition in `run-doctor-checks.mjs`.

### Detail

**Contract checks (machine-verified):**
- `testCommand` passed
- `minLineCoverage` met: 88.55% >= 77%

**Code quality:**
- `model-id.mjs` (103 lines): Four focused helpers with clear result types (`{ok, value}` / `{ok, error}`). Regex patterns are readable and anchored. Special values (`inherit`, `""`) handled consistently.
- `agent-models.mjs` (53 lines): Iterates known model paths, delegates validation, aggregates failures with `suggestedCommand` for auto-fix. No business logic leakage.
- `settings-set.mjs` changes: Model field paths defined as a `Set` constant; normalization runs after field validation but before config write — correct ordering.
- `run-doctor-checks.mjs`: +4 lines (import + registration) — standard wiring pattern, not scope creep.

**Test coverage:**
- 27 tests across three layers: unit (model-id helpers), doctor check (pass/fail/edge cases), and integration (settings-set with display labels, canonical ids, invalid formats, JSON mode, special values).
- Positive and negative paths tested. Non-string and null inputs covered.

**File scope compliance:**
- Declared: `src/config/model-id.mjs`, `src/doctor/agent-models.mjs`, `src/cli/settings-set.mjs`, `tests/doctor/model-id-validation.test.mjs` — all present.
- `src/doctor/run-doctor-checks.mjs` is outside declared scope but the change is the minimal wiring required to register the new check (import + 3 lines). Acceptable.

**No issues found:** No ghost layers, no silent failures, no hardcoded secrets, no dead code, no placeholder stubs.

```json
{"verdict":"PASS","feedback":"Clean implementation with good test coverage. Model-id normalization layer is well-factored, doctor check provides actionable fix commands, and settings-set intercepts display labels at write time. All contract gates met."}
```
