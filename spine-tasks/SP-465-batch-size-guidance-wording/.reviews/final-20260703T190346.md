### Verdict: PASS

### Summary

SP-465 correctly makes the batch-size guidance warning size-aware. `countMediumLargeTasks` now returns `mCount`/`lCount` and a new `formatSizeLabel` helper renders M-only, L-only, or mixed labels, replacing the previous generic "M/L" wording. Tests cover all three composition variants (M-only, L-only, mixed) plus below-threshold and stub-mode paths; all 6 tests pass. Changes stay within File Scope (`src/doctor/batch-size-guidance.mjs`, `tests/doctor/batch-size-guidance.test.mjs`).

**Minor note (non-blocking):** There is a stray duplicate JSDoc block above `formatSizeLabel` (lines 50-54 repeat the `@param tasksRoot` / `@returns` block from the deleted original function signature). This is cosmetic and does not affect behavior.

**Contract verification:**
- testCommand: PASS (6/6 tests, typecheck, coverage gate)
- fileScopeMustChange: PASS (`src/doctor/batch-size-guidance.mjs` modified)
- minLineCoverage: PASS (88.51% >= 77%)
- artifactsMustExist: PASS (`tests/doctor/batch-size-guidance.test.mjs` exists)

```json
{"verdict":"PASS","feedback":"Size-aware wording implemented correctly with full test coverage for M-only, L-only, and mixed compositions. Minor cosmetic: duplicate JSDoc block above formatSizeLabel (non-blocking)."}
```
