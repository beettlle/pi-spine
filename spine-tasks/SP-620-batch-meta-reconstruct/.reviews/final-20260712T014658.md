### Verdict: PASS

### Summary

SP-620 meets completion criteria and the machine contract (fileScopeMustChange + testCommand). Force-resume reconstructs from batch-meta when live state is missing/corrupt, fails closed on missing/ambiguous/conflicting meta, and wires through both `resume.mjs` and `resume-multi-validate.mjs`. Contract re-run confirmed: typecheck + 7/7 reconstruct tests pass; `.DONE` is present.

```json
{"verdict":"PASS","feedback":"Completion criteria satisfied: reconstructBatchStateFromRuntime + ensureForceResumeBatchState fail-closed on missing/ambiguous/wave-conflict meta; force-resume wired in resume.mjs and resume-multi-validate.mjs; incident tests cover #126 limbo (missing/corrupt state, missing meta, multi-meta, wave conflict). Machine verifier PASS; contract testCommand re-verified 7/7. .DONE present. New module batch-meta-reconstruct.mjs is a justified LOC split outside the listed File Scope paths but delivers the required helper without Do-NOT violations. Ready to integrate / close #126."}
```
