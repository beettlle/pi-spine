### Verdict: APPROVE

### Summary
The plan for Step 1 precisely targets the required changes in `scripts/coverage-policy.mjs`: adding `tests/arch/*.test.mjs` and `tests/fs/*.test.mjs` to `TEST_GLOBS` while removing `tests/arch` and `tests/fs` from `SUITE_DIR_ALLOWLIST`. This strictly conforms to the file scope and criteria in PROMPT.md without scope creep. All policy discovery and import-cycle tests continue to pass cleanly.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan correctly updates TEST_GLOBS and removes arch/fs entries from SUITE_DIR_ALLOWLIST in scripts/coverage-policy.mjs according to specification."
}
```
