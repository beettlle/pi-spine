### Verdict: PASS

### Summary
The task is complete and successfully meets all criteria defined in PROMPT.md. The CLI properly integrates the `runQuotaProbes` function in an async flow, gracefully fails closed for absent/errored responses, prevents key leakage, and updates the QUICK-REFERENCE documentation as requested.

```json
{
  "verdict": "PASS",
  "feedback": "Great work. The async CLI wiring correctly delegates to `runQuotaProbes` while protecting against network issues and missing credentials by degrading gracefully to estimate/absent. The tests use deterministically safe mocked fetching and verify that no secrets leak. Documentation is properly updated."
}
```