### Verdict: APPROVE

### Summary
The implementation plan for Step 1 (`withBatchStateLock` helper) thoroughly addresses all requirements outlined in `PROMPT.md`. It designs an exclusive `wx` lock file mechanism under `.spine/runtime/batch-state.lock` with robust PID-liveness stale lock breaking, TTL expiry, synchronous wait polling, and same-process re-entrancy support. Lock ordering and relationship with the resume handoff lock are clearly specified to prevent deadlocks.

```json
{
  "verdict": "APPROVE",
  "feedback": "Plan for Step 1 is well-designed with clear lock hierarchy, stale lock breaking via PID liveness/TTL, and re-entrancy support to avoid self-deadlocks."
}
```
