### Verdict: PASS

### Summary

SP-355 delivers the required preflight and plan warnings for predictable multi-file orch merge conflicts when pending merge-origin-main tasks coincide with `docs/PRD.md` and rules-manifest divergence between HEAD and origin/main. Contract verification passes independently (typecheck + 1144/1144 tests), file scope is respected, coverage exceeds the 77% gate, and delivery artifacts (`.DONE`, closed #37) are in place.

Implementation adds `checkOrchMergeConflictWarn` to batch preflight (warning-only, does not block) and surfaces the same risk in `runPreflightPlanCheck`. The regression fixture in `tests/config/spine-preflight-orch-conflict.test.mjs` covers quiet path, warn path, and integration through `runBatchPreflight` / plan output.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met: preflight/plan warn on PRD+manifest divergence with pending merge-origin-main tasks; regression tests pass (4 cases in spine-preflight-orch-conflict.test.mjs); contract verifier PASS (testCommand, fileScopeMustChange, minLineCoverage 87.73%, artifactsMustExist); issue #37 CLOSED; .DONE present. Changes stay within file scope (spine-preflight-lib.mjs + test artifact). No blocking issues."}
```
