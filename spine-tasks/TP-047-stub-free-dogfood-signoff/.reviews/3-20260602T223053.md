## Plan Review: Step 3 — Update Phase 6 report + fix flakes

### Verdict: APPROVE

### Summary

Step 3 correctly closes the documentation and stability gap left after Step 2 stub-free execution: reconcile `docs/compatibility/phase6-dogfood-report.md` with the signed [`stub-free-dogfood-report.md`](../../docs/compatibility/stub-free-dogfood-report.md) (cross-link, manual checklist rows, closed settings/deps deferrals) and run a parallel-load flake triage on `tests/worker-tools/review-step-tool.test.mjs` and `tests/worker-tools/worker-tools-registration.test.mjs`, fixing only if failures block sign-off. The conditional flake scope matches PROMPT ("if blocking sign-off") and File Scope; no stub-free CI or default `npm test` policy changes. Land-loop manual items (gate approve → integrate → complete) correctly remain unchecked in Phase 6 with post-batch pointers to the stub-free report — do not check them during Step 3 while batch `20260602T221506` is still active.

### Issues Found

None blocking for Step 3 plan approval.

### Missing Items

- **Step boundary vs Step 1 commit** — Commit `9309899` already landed Phase 6 cross-links, manual checkboxes (items 1–4, 9), and settings/deps deferral strikethrough (Step 3 scope). Step 3 should **verify alignment** against the Step 2 stub-free report rather than re-edit `phase6-dogfood-report.md` unless a delta is found; record "verified in Step 3, no net doc change" in STATUS if the file is unchanged.
- **Automated regression section** — The stub-free report's `npm run typecheck && SPINE_WORKER_STUB=1 npm test` block and pass count belong to **Step 4 verification**, not Step 3. Do not update that section or mark Step 4 complete in STATUS during Step 3.
- **STATUS discipline** — Hydrate a Step 3 subsection (phase6 verification outcome, flake triage command/result, Step 3 commit file list) but keep Step 3 **in progress** until after plan review approval and a Step 3 commit (or explicit "no-op verification" note). Do not pre-mark Steps 3–4 complete before Step 3 lands.
- **Step 3 commit boundary** — Commit should include only `phase6-dogfood-report.md` deltas (if any), `tests/worker-tools/*.test.mjs` flake fixes (if reproduced), and task `STATUS.md`. Exclude `stub-free-dogfood-report.md` regression notes unless correcting factual errors unrelated to Step 4 counts.

### Suggestions

- **Flake triage** — Run full `SPINE_WORKER_STUB=1 npm test` (default parallel runner) at least once; re-run worker-tools files in isolation if contention is suspected. Current suite is **321/321 pass** — expect no test edits; document "stable under parallel load" in STATUS.
- **Phase 6 land loop** — After batch wave merge, operator runs gate approve → integrate → complete; update stub-free report rows 6–8 and Phase 6 checkboxes 5–6 in a follow-up (post-TP-047 land loop), not as part of flake/doc Step 3 unless explicitly deferred with notes (current state is acceptable).
- **README pointer** — Optional one-line link from README stub section to `scripts/stub-free-dogfood.sh` or phase6 report; non-blocking, docs-only if added.
- **Do NOT** — No stub-free CI gate, no `SPINE_WORKER_STUB=0` in default `npm test`, no README/phase6 churn beyond cross-link verification, no scope expansion into `env-overrides.test.mjs` or other pre-existing parallel flakes outside File Scope.

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "TP-047",
  "feedback": "Step 3 plan: verify phase6-dogfood-report.md cross-links/deferrals against stub-free report (likely no delta after 9309899), conditional parallel flake triage on worker-tools tests with fixes only if blocking, keep land-loop checkboxes deferred post-batch. Do not update stub-free automated regression section or mark Step 4 complete in Step 3; hydrate STATUS after Step 3 commit. Proceed with implementation."
}
```
