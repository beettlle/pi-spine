## Plan Review: Step 2 — Execute manual dogfood

### Verdict: APPROVE

### Summary

Step 2 correctly targets real-pi validation on the pi-spine repo via a single-task self-dogfood batch (`SPINE_WORKER_STUB=0 spine batch start TP-047`), operator CLI checks from project root (preflight, plan, status, gate inspect, dashboard), and a signed `stub-free-dogfood-report.md` with pass/fail rows plus worker evidence (batch id, `--pi` runner, lane heartbeats). Deferring land-loop items 6–8 until post-batch merge is the right sequencing for a meta batch and matches PROMPT success criteria (minimum single-task batch exercised, or blockers documented). The guided script from Step 1 is the right operator entry point; TP-047 over AD-001 is acceptable given TP-044 is optional.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Step boundary vs Step 1 commit** — Commit `9309899` already filled the report, updated `phase6-dogfood-report.md` checkboxes, and struck deferrals (Step 3 scope). Step 2 plan must treat remaining work as **report finalization only** in a dedicated Step 2 commit; do not add further `phase6-dogfood-report.md` edits until Step 3.
- **Automated regression section** — The report's `npm run typecheck && SPINE_WORKER_STUB=1 npm test` block belongs to Step 4 verification, not Step 2 manual dogfood. Remove or stub it in the Step 2 commit; record counts in STATUS during Step 4.
- **Commit SHA in report header** — Header still references pre-run baseline `8a81cbe`; update to the Step 2 commit (or post-batch merge SHA if land loop is run later) before sign-off.
- **STATUS hydration** — Add a Step 2 subsection (batch id, operator commands, checklist row summary, Step 2 commit file list) per Step 1 review feedback; journal shows 1/1 checkboxes but PROMPT lists two Step 2 items.
- **Plan-before-implementation** — Manual execution and report population landed in the Step 1 commit before this review. Going forward, Step 2 commit should capture only refinements (execution log, evidence refresh, sign-off line) without re-running scope from Steps 3–4.

### Suggestions

- **Operator flow** — Start batch from clean project root; use `./scripts/stub-free-dogfood.sh TP-047` in lane worktree for env checks (preflight git-clean warning is expected). Re-run `spine status --diagnose` and `spine gate status` from project root while batch is active for evidence.
- **Land loop** — After batch wave merge, operator runs gate approve → integrate → complete; update report rows 6–8 from **deferred** to **pass** (or document blockers). Self-dogfood cannot complete the land loop inside the same batch — document, do not fail sign-off for deferral.
- **Sign-off criteria** — Items 1–5 and 9 passing with real `--pi` workers plus journal heartbeats satisfies "minimum single-task batch"; partial land-loop deferral is acceptable if explicitly noted.
- **Do NOT** — No stub-free CI changes, no worker-tools flake fixes, no README/phase6 cross-link edits in Step 2 (Step 3).
- **Blockers path** — If real-pi spawn fails, record repro env (`pi --version`, `SPINE_WORKER_STUB`, runner log tail) in Blockers section instead of sign-off.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "TP-047",
  "feedback": "Step 2 plan: SPINE_WORKER_STUB=0 single-task TP-047 self-dogfood on pi-spine, operator CLI checklist via guided script, evidence and pass/fail rows in stub-free-dogfood-report.md, land loop 6–8 deferred post-batch. Restrict Step 2 commit to report finalization only — phase6 checkbox/deferral updates and npm regression notes belong in Steps 3–4; refresh commit SHA and hydrate STATUS. Proceed with Step 2 commit after batch evidence is captured."
}
```
