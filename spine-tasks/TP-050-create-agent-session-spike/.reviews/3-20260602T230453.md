## Code Review: Step 3 — Prototype or document blockers

### Verdict: APPROVE

### Summary

Step 3 delivers a working **happy-path prototype** behind `lanes.workerBackend: agentSession` in commit `c7df3e2`, without changing the default subprocess path. `worker-host.mjs` branches via `spawnWorkerHandle()` only when `!useStub` and `resolveWorkerBackend(config) === "agentSession"`, preserving `SPINE_WORKER_STUB` / missing-`pi` stub behavior for CI and the subprocess default. New `agent-session-worker.mjs` mirrors `bin/spine-worker-runner.mjs` prompt construction (PROMPT `@` include, review/tools hints, `.DONE` contract), loads `createAgentSession` with injectable deps / env stubs, and exposes a ChildProcess-shaped handle (`wait`, `kill`, `exitCode`) so the existing `.DONE` poll, abort, and stall loops stay unchanged — matching Step 1 guidance and spike doc prototype scope.

Working-tree `git diff` is only unrelated `package-lock.json` bin-path drift; Step 3 code is in `c7df3e2`. Untracked `tests/batch/worker-backend.test.mjs` (11 passing tests with mocks, no real `pi`) is appropriate for Step 4 but already validates the prototype locally.

### Issues Found

None blocking for Step 3 spike scope.

### Missing Items

- **Batch context env for in-process workers** — `spawnWorkerChild()` sets `SPINE_TASK_FOLDER`, `SPINE_WORKTREE`, `SPINE_PROJECT_ROOT`, `SPINE_BATCH_ID`, `SPINE_JOURNAL_ATTACH`, lane ids, etc.; `startAgentSessionWorker()` does not. Real batch trials with `agentSession` will not give spine worker tools / review journal the same context as subprocess until env is propagated (document as follow-up or new blocker).
- **Tests not committed with Step 3** — `tests/batch/worker-backend.test.mjs` is untracked; commit in Step 4 per PROMPT verification step.
- **`worker.md` wiring parity** — Subprocess uses `--append-system-prompt`; agentSession embeds `@.spine/agents/worker.md` in the user prompt. Confirm SDK treats these equivalently before adoption soak.

### Suggestions

- **SIGKILL vs `donePromise` race** — `kill("SIGKILL")` sets `exitCode = 137` immediately, but `donePromise` may later overwrite with 0/1 when `prompt` finishes; align exit code after await or guard writes post-abort.
- **Propagate batch env before `createAgentSession`** — Mirror `spawnWorkerChild` env in `spawnWorkerHandle` for agentSession (or pass explicit context into session options) so `spine_report_progress` / `spine_review_step` work under real batches.
- **Discard unrelated lockfile change** — Restore `package-lock.json` (`pi-ai` bin `./dist/cli.js` → `dist/cli.js`) unless intentionally updating deps.
- **Model / extensions** — Spike blockers B3–B4 still apply (`agents.worker.model: inherit`, extension load in headless engine); keep default `subprocess` until addressed.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 3 commit c7df3e2 adds agent-session-worker behind lanes.workerBackend with unchanged subprocess default, .DONE-driven worker-host polling, mock/stub injection, and prompt parity with spine-worker-runner. Commit worker-backend tests in Step 4; propagate SPINE_* batch env for real agentSession trials; fix SIGKILL exitCode race if hard-abort semantics matter."
}
```
