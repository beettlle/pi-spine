## Code Review: Step 2 — Extract lane run + merge

**Reviewer:** Code review agent  
**Step:** 2 — Extract lane run + merge  
**Worktree:** `lane-4` (`task/spine-lane-4-20260605T191325`)  
**Date:** 2026-06-05

## Scope

Step 2 per `PROMPT.md`: move lane queue/spawn and force-resume reset into `resume-multi-lanes.mjs`, keep the public facade on `resume-multi.mjs`, and share merge helpers with `engine-lanes.mjs`.

`git diff --name-only` (tracked): `.spine/rules-manifest.json`, `spine-tasks/SP-113-resume-multi-strangler/STATUS.md`, `src/batch/resume-multi.mjs`.

Untracked but in scope for this step: `src/batch/resume-multi-lanes.mjs`, `src/batch/resume-multi-validate.mjs` (Step 1 validate slice; bundled in the worktree, not yet committed).

| File | Role |
|------|------|
| `src/batch/resume-multi-lanes.mjs` (new) | `resetFailedTasksForForceResume`, `markTaskCompleteFromDisk`, `runResumedTaskOnLane`, `executeResumeWave` |
| `src/batch/resume-multi.mjs` | Orchestrator: validation gate, wave loop, merge eligibility, integrate gate; re-exports validate symbols |
| `src/batch/engine-lanes.mjs` (unchanged) | Shared `mergeWaveLanesToOrch` (replaces local duplicate) |
| `src/batch/resume-multi-validate.mjs` (new, Step 1) | Validation helpers consumed by lanes via `isTaskResumable` |

## Summary

The strangler Step 2 extraction is a faithful lift with zero observable semantic drift in the resume engine path.

1. **Lane extraction** — Per-lane queue building, serialized lane execution (`lane.tasks_serialized`), disk-complete short-circuit (`markTaskCompleteFromDisk`), and worker spawn (`runResumedTaskOnLane`) moved verbatim into `resume-multi-lanes.mjs`. `executeResumeWave` returns the same result shapes the orchestrator previously handled inline, including early `lane_not_found` exits wrapped as `{ ok: false, result }`.

2. **Orchestrator slim-down** — `resume-multi.mjs` drops from 898 LOC (HEAD) to 231 LOC, under the ≤400 completion target. It retains phase transition, wave iteration, merge eligibility, completion journaling, integrate gate, and crash handling. Public imports from `resume.mjs` / `reconcile.mjs` remain unchanged via re-exports of `computePendingTasks`, `findResumableWave`, and `validateMultiTaskResume`.

3. **Merge helper dedup** — Local `mergeWaveLanesToOrch` removed; orchestrator now imports the shared `engine-lanes.mjs` implementation (mission requirement). Compensating guard `state.mergeResults = state.mergeResults ?? []` added before the wave loop so the shared helper's bare `.push()` remains safe on the resume path.

4. **Dependency graph** — `resume-multi-lanes` → `resume-multi-validate` (`isTaskResumable` only). No circular imports. Lane symbols stay internal (no facade re-export needed; no test or consumer imports them directly).

5. **Behavior parity checks** — Journal events, error codes, abort/fail return payloads, force-resume reset, and parallel lane `Promise.all` semantics match the pre-extraction implementation. Wave-loop indentation is corrected (cosmetic only).

**Verification:** `npm test` — **559** tests, **0** failures (includes `resume-multi-validation`, `resume-multi-sequential`, `resume-multi-engine`, and related batch suites).

## Strengths

- **Clean facade** — External surface (`resumeMultiTaskBatch`, validate helpers) unchanged; strangler boundary is import-only for consumers.
- **Shared merge path** — Eliminates duplicate `mergeWaveLanesToOrch` logic and aligns resume with `engine.mjs`.
- **Explicit wave API** — `executeResumeWave` gives Step 3 a stable unit boundary without altering runtime flow.
- **LOC win on god module** — Primary reliability surface (`resume-multi.mjs`) is now readable orchestration code.

## Findings

### Blocking

None.

### Minor (non-blocking)

1. **`resume-multi-lanes.mjs` size** — 477 LOC exceeds the mission's ≤350 LOC/file guidance. Acceptable for Step 2 (verbatim lift); a future pass could split commit/dirty-check helpers if desired.

2. **Commit boundary** — Both `resume-multi-validate.mjs` (Step 1) and `resume-multi-lanes.mjs` (Step 2) are untracked. Land them together with the slimmed `resume-multi.mjs` under the PROMPT commit message; exclude incidental `.spine/rules-manifest.json` timestamp churn.

3. **Step 3 still open** — PROMPT Step 3 (FULL suite + coverage gate) is not yet recorded in STATUS. Spot-check tests pass; formal Step 3 verification remains.

4. **Dead assignment hygiene** — `batchAborted` is set from `waveOutcome.batchAborted` and then read immediately; the outer `let batchAborted = false` could be replaced by `const batchAborted = waveOutcome.batchAborted` for clarity (optional, no behavior impact).

## Step 2 Checklist

| Item | Status |
|------|--------|
| Lane queue/spawn in `resume-multi-lanes.mjs` | Done |
| Re-export public validate symbols from `resume-multi.mjs` | Done |
| Share merge helpers with `engine-lanes.mjs` | Done (local duplicate removed) |
| `resume-multi.mjs` ≤400 LOC | Done (231) |
| Zero resume-semantics / journal-event change | Done (verified by diff + tests) |
| `resume-multi-sequential` tests pass | Done (full suite green) |

### Verdict: APPROVE

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "SP-113",
  "feedback": "Step 2 lanes extraction verified: executeResumeWave/resetFailedTasksForForceResume/markTaskCompleteFromDisk/runResumedTaskOnLane lifted verbatim to resume-multi-lanes.mjs; resume-multi.mjs slimmed to 231 LOC orchestrator with validate re-exports preserved; local mergeWaveLanesToOrch removed in favor of engine-lanes shared helper with mergeResults init guard. No blocking behavior drift; 559 tests pass. Commit untracked validate+lanes modules with resume-multi.mjs; skip rules-manifest noise; proceed to Step 3 FULL suite + coverage."
}
```
