## Plan Review: Step 1 — Extract validate

### Verdict: APPROVE

### Summary

The Step 1 plan implied by `PROMPT.md` and SP-106 Finding #3 is sound: move TP-039 validation and per-lane worktree health/repair from `resume-multi.mjs` into a new `resume-multi-validate.mjs` with verbatim behavior, keep `resumeMultiTaskBatch` and all lane execution in the god module for this step, and preserve the public import surface via re-exports from `resume-multi.mjs` so `resume.mjs`, `reconcile.mjs`, and existing tests need no changes. The extraction boundary (`isTaskResumable`, `computePendingTasks`, `findResumableWave`, `validateMultiTaskResume` plus worktree existence + `repairLaneWorktreeGitMetadata`) matches the audit's first strangler slice and correctly defers lane queue/spawn, force-resume reset, merge, and integrate gate to Step 2.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| `resume-multi-validate.mjs` (new) | Lift validation helpers and `validateMultiTaskResume` unchanged; ~180 LOC |
| Worktree repair in validate | Keep `assertLaneWorktreeGitHealthy` / `repairLaneWorktreeGitMetadata` loop inside validator |
| Zero behavior change | No edits to error codes, output strings, journal events, or validation ordering |
| Consumers unchanged | Re-export `computePendingTasks`, `findResumableWave`, `validateMultiTaskResume` from `resume-multi.mjs` |
| Step 1 scope only | Do **not** extract `resetFailedTasksForForceResume`, `runResumedTaskOnLane`, wave loop, or merge helpers |
| Tests (Step 3) | `resume-multi-validation.test.mjs` and `worktree-git-paths.test.mjs` keep importing from `resume-multi.mjs` |
| Do NOT | No resume semantics change; no merge-helper swap (`mergeLaneToOrch` → `mergeWaveLanesToOrch`) in Step 1 |

### Suggestions (non-blocking)

1. **`spine-tasks/SP-113-resume-multi-strangler/STATUS.md`** — Add a short Step 1 plan subsection (module exports, re-export list, commit file list) and log this review path before marking Step 1 complete.
2. **Step 1 commit boundary** — If `resume-multi-lanes.mjs` or merge-helper changes already exist in the worktree, land Step 1 as `resume-multi-validate.mjs` plus the slimmed import/re-export edits in `resume-multi.mjs` only; defer lanes extraction and `mergeWaveLanesToOrch` wiring to Step 2.
3. **`isTaskResumable` visibility** — Export from `resume-multi-validate.mjs` for Step 2 lane code if needed; no facade re-export required until an external consumer imports it.
4. **Verification gate** — Step 1 does not require the full suite; spot-check `tests/batch/resume-multi-validation.test.mjs` and `tests/batch/worktree-git-paths.test.mjs` after the Step 1 commit. Reserve FULL suite + coverage for Step 3.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan: extract isTaskResumable, computePendingTasks, findResumableWave, and validateMultiTaskResume (with per-lane worktree repair) into resume-multi-validate.mjs; re-export the three public symbols from resume-multi.mjs for resume.mjs, reconcile.mjs, and tests; leave lane execution, force-resume reset, merge, and journal semantics in resume-multi.mjs until Step 2. Commit validate module + facade re-exports only. Record plan review in STATUS; proceed to Step 1 implementation/commit, then Step 2 lanes extraction."
}
```
