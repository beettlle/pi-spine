### Verdict: APPROVE

### Summary

Step 4 delivery is complete: `.DONE` was created and `STATUS.md` reflects all steps finished. The full SP-570 implementation (commits `fe911b26`–`ce01b86c`) satisfies PROMPT.md: `listSalvageableLanes` reuses journal/reconcile helpers, excludes contract/review failures, exposes read-only `spine batch salvage --batch <id> --dry-run`, and exits non-zero when the journal is missing. Contract `testCommand` passed (7/7 tests, typecheck clean).

Minor note (non-blocking): `parseBatchState` is imported but unused in `src/batch/salvage-batch.mjs`.

```json
{"verdict":"APPROVE","feedback":"Step 4 deliverables (.DONE, STATUS) are in place. Salvage list module, CLI wiring, and tests meet FR-REL220-03 / #158 scope: read-only dry-run listing with per-lane diff stat, contract/review exclusion, and non-zero exit on missing journal. Contract testCommand: 7 pass, 0 fail. Optional cleanup: remove unused parseBatchState import."}
```
