### Verdict: REVISE

### Summary

Step 3 documentation is solid: commit `9aec210` adds a clear **Atomic orchestration writes (SP-318+)** section to `docs/adoption/operator-runbook.md` covering worker-output logs and structured `.DONE` JSON with legacy compatibility, and updates the worktree layout table accordingly. However, the `.DONE` delivery artifact exists only as an untracked working-tree file (`spine-tasks/SP-321-atomic-worker-output-done/.DONE`) and was not included in the delivery commit—unlike prior tasks (e.g. SP-317, SP-329) where `.DONE` is committed. Worker-output unit tests pass in isolation (7/7); typecheck passes.

```json
{"verdict":"REVISE","feedback":"Commit `spine-tasks/SP-321-atomic-worker-output-done/.DONE` (structured JSON with taskId/completedAt is correct) so Step 3 delivery matches project convention and batch pending filtering. The operator-runbook atomic-writes section and STATUS updates in 9aec210 are acceptable. Leave unrelated unstaged PROMPT.md artifactsMustExist edits and rules-manifest timestamp refresh out of the SP-321 integrate commit. docs/EXECUTION-FLOW.md correctly unchanged—no atomic-write detail needed there."}
```
