### Verdict: PASS

### Summary

SP-315 meets all completion criteria and contract checks. Commits `291cabb` and `4fc43a9` implement `reconcileOrphanRunningState` (orphan `running` → `failed` with journal events), hook it into `retryTask`, align `engine_orphaned` `suggestedCommand` with retry, add regression coverage from issue #20's batch shape, update the operator runbook, close GitHub #20, and create `.DONE`. Machine verifier passed; SP-315 regression tests pass 5/5 when run directly.

```json
{"verdict":"PASS","feedback":"Contract verification PASS (testCommand, fileScopeMustChange for reconcile.mjs/retry.mjs, minLineCoverage 87.54%≥77%, artifact exists). PROMPT outcomes satisfied: reconcileOrphanRunningState transitions orphan running tasks to failed with task.failed/lane.died journaling; batch retry works without pause workaround; diagnosis suggests spine batch retry when failedTaskId is known; operator-runbook documents SP-315 recovery path; issue #20 CLOSED; .DONE present. resume-multi-validate.mjs unchanged by design (SP-297 orphan resume preservation) — resume --force covered after reconcile in tests. Non-blocking: resume-multi-validate listed in File Scope but not modified; docs/EXECUTION-FLOW.md not updated (PROMPT check-if-affected only); full npm test run showed one unrelated flaky failure in contract-stall-override.test.mjs while SP-315 file passes 5/5."}
```
