### Verdict: PASS

### Summary
The worker successfully completed the task by adding the required foreground verification guardrail to `.spine/agents/worker.md`. The optional harness hint for `worker_done_missing` was also implemented cleanly with a marker-based approach in `worker-output.mjs` and wired into the diagnosis headline, fully satisfying the requirements. All tests pass and the `.DONE` marker is present.

```json
{
  "verdict": "PASS",
  "feedback": "All requirements met. The prompt guardrail is clear and the optional harness hint was implemented elegantly without new I/O."
}
```