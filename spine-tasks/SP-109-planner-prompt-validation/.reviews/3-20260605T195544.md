## Plan Review: Testing & Verification (Step 3)

### Verdict: APPROVE

### Summary

Step 3 correctly treats verification as a **formal gate** after Steps 1–2 landed the shared validation helper and boundary wiring. The core negative matrix — invalid heading separator, missing testing coverage, missing File Scope, `runSpinePlan` rejection, and valid-packet happy path — already lives in `tests/planner/prompt-validation.test.mjs` from Step 1; Step 3's plan appropriately focuses on **collateral fixture hygiene** and **integration regression** so the FULL suite stays green under the new fail-loud contract.

The Step 3 work plan covers the Step 2 review prerequisite: update `runSpineRules select requires manifest` (TP-801 stub) to `minimalValidPromptMarkdown` so validation runs after structural checks and the test still asserts `/rules-manifest\.json missing/`. Same pattern applies to `tests/cli/spine-router.test.mjs` and `tests/spine-run.test.mjs`, which previously used truncated PROMPT stubs that would now fail at plan/load time. `tests/batch/prompt-parse-fail.test.mjs` is correctly retargeted: invalid PROMPTs reject at **plan time** via `buildPlan()` (through `startBatch`), so the test should `assert.rejects` with `/Invalid PROMPT for TP-998/` instead of expecting lane-time `task.prompt_parse_failed` journal events. Verification gate is `npm test` + `npm run coverage:check` (≥77% threshold); observed coverage ~83% on the worktree.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Unit tests: invalid heading → plan fails | `buildPlan rejects invalid heading separator` in `prompt-validation.test.mjs` (Step 1) |
| Unit tests: missing testing step → plan fails | `buildPlan rejects missing testing coverage` |
| Unit tests: bad file scope → plan fails | `buildPlan rejects missing File Scope section` |
| CLI boundary | `runSpinePlan exits with validation errors for invalid PROMPT` |
| FULL suite + coverage gate | Collateral fixture updates + `npm test` / `coverage:check` |
| Step 2 prerequisite (TP-801 manifest stub) | `minimalValidPromptMarkdown("TP-801", …)` in `spine-rules.test.mjs` |
| Batch engine not weakened | `prompt-parse-fail.test.mjs` still asserts fail-closed; semantics shift to plan-time throw |
| Do NOT change PROMPT schema | No schema edits in Step 3 scope |

### Suggestions (non-blocking)

1. **Optional aggregate test** — Step 1 review suggested a multi-invalid-task case (two bad PROMPTs in scope, both taskIds in aggregate error). `formatPromptValidationFailures` already supports N>1; a focused test in `prompt-validation.test.mjs` would lock that behavior. Defer or add in a follow-up; not required for Step 3 closure.
2. **Optional preflight negative test** — Step 2 review suggested one `runPreflightPlanCheck` / `runBatchPreflight` fixture with an invalid PROMPT asserting `plan` check `ok: false` and validation detail in the message. Preflight inherits validation via `buildPlan()`; planner tests already cover the throw path. Nice-to-have boundary test only.
3. **Step boundary** — Step 3 commit `1d268b0` includes `docs/adoption/operator-runbook.md` §2 preflight troubleshooting (PROMPT Step 4 scope). Content is correct and small; prefer a dedicated Step 4 commit next time for doc-only delivery.
4. **Plan-before-code discipline** — Step 3 landed in the same timestamp window as this plan review (same pattern as Steps 1–2). Acceptable because the diff matches the approved verification approach; keep Step 4 doc closure behind review if not already done.
5. **STATUS hydration** — Record `.reviews/3-20260605T195544.md` in Step 3 Execution Log / Reviews row. Note that core unit tests shipped in Step 1 commit; Step 3 commit = collateral fixtures + batch test semantics + (early) runbook note.
6. **Flaky dashboard test** — `tests/dashboard/cli-startup.test.mjs` may fail under full parallel load (pre-existing timing flake; passes in isolation). Treat as unrelated to SP-109; do not block task completion on it unless CI requires a green full run.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 3 plan: formal verification gate — collateral fixture hygiene (TP-801 manifest stub, spine-router, spine-run → minimalValidPromptMarkdown), batch prompt-parse-fail retargeted to plan-time assert.rejects, FULL npm test + coverage:check (core negative matrix already in prompt-validation.test.mjs from Step 1). Optional: multi-invalid aggregate test and preflight negative test. Step 3 commit bundled runbook §2 note (Step 4 scope); acceptable. Record this review in STATUS; proceed to Step 4 closure or task completion."
}
```
