## Plan Review: Extend to preflight and rules CLI (Step 2)

### Verdict: APPROVE

### Summary

Step 2 correctly extends SP-107 Finding #1 / Category A3 coverage to the remaining operational boundaries with minimal, compositional changes. **Preflight** needs no `bin/spine-preflight.mjs` edits: `runPreflightPlanCheck()` already calls `buildPlan({ scope: "all", … })`, so the Step 1 aggregate validation gate (`collectPromptValidationFailure` + `formatPromptValidationFailures`) applies automatically — invalid PROMPTs surface as a failed `plan` check with `Failed to build plan: …` and preflight exit 1. **Rules CLI** replaces the audit-flagged `fs.readFileSync` + `parsePrompt` path in `resolveTaskPromptFileScope()` with `loadTaskPacket()`, which runs `validatePrompt` and returns `{ ok: false, error, errors, promptPath }` before manifest/profile selection. That closes the `spine rules select` bypass without touching PROMPT schema or batch engine validation.

Commit boundary matches Step 1 review deferral: Step 2 lands only `src/cli/rules.mjs` and `tests/cli/spine-rules.test.mjs` (commit `8c230f0`), with two targeted tests — invalid heading rejected before manifest check (exit 1, `/em dash/i`), and validated file scope happy path through discover → select. Scope stays within PROMPT file list; omitting preflight source edits is the right strangler-friendly choice given the god-module status called out in SP-107.

### Alignment checks

| PROMPT outcome | Plan / approach |
|----------------|-----------------|
| Preflight plan validation uses same helper | Inherited via `buildPlan()` in `runPreflightPlanCheck()` — same `loadTaskPacket` → `validatePrompt` → aggregate throw path as `spine plan` |
| `spine rules select` uses validatePrompt not parsePrompt only | `resolveTaskPromptFileScope` uses `loadTaskPacket(match.folderPath)`; rejects when `!packet.validation.ok` |
| Share validation helper across planner, preflight, rules | Planner/preflight share `formatPromptValidationFailures`; rules uses `loadTaskPacket` validation payload (semantically same gate, lighter single-task error shape) |
| Do NOT change PROMPT schema / weaken batch validation | No schema or `engine-lanes` edits |
| File scope (Step 2) | `src/cli/rules.mjs`, `tests/cli/spine-rules.test.mjs`; preflight covered transitively, no `bin/spine-preflight.mjs` diff required |

### Suggestions (non-blocking)

1. **Collateral test fixture** — `runSpineRules select requires manifest` still writes a structurally invalid TP-801 PROMPT; validation now runs before manifest lookup, so the test fails expecting `/rules-manifest\.json missing/` (currently gets validation errors). Update that stub to `minimalValidPromptMarkdown("TP-801", …)` in Step 2 follow-up or early Step 3 before the FULL suite gate — same pattern as Step 1 planner fixture hygiene.
2. **Error formatting consistency** — Rules joins `validation.errors` with `"; "` while planner/preflight use `formatPromptValidationFailures`. Optional polish: call `formatPromptValidationFailures([collectPromptValidationFailure(packet, taskId)])` in the rules error path for uniform bullet output and taskId/path prefix.
3. **Preflight negative test** — Step 3 could add one `runPreflightPlanCheck` / `runBatchPreflight` case with an invalid PROMPT in a fixture repo asserting `plan` check `ok: false` and message contains validation detail (mirrors `prompt-validation.test.mjs` at the preflight boundary).
4. **Plan-before-code discipline** — Step 2 commit landed before this plan review (same pattern as Step 1). Acceptable because the diff matches the approved approach; keep Step 3 verification behind review closure.
5. **STATUS hydration** — After this review, log `.reviews/2-20260605T192019.md` in Step 2 Execution Log / Reviews row and note the collateral manifest-test fix as a Step 3 prerequisite for FULL suite green.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 2 plan: preflight inherits fail-loud validation through buildPlan() in runPreflightPlanCheck (no spine-preflight.mjs edits); rules resolveTaskPromptFileScope switches to loadTaskPacket/validatePrompt with exit-1 error propagation; Step 2 commit = rules.mjs + spine-rules tests (invalid PROMPT rejection + validated file scope). Fix TP-801 manifest-test stub to minimalValidPromptMarkdown before Step 3 FULL suite. Optional: use formatPromptValidationFailures in rules for consistent error text. Record plan review in STATUS; proceed to Step 3 verification."
}
```
