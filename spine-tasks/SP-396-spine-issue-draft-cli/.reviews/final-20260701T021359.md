### Verdict: PASS

### Summary

SP-396 delivers the `spine issue draft` CLI as specified in PROMPT.md: flag parsing, human/JSON output, optional file write, opt-in `--create` with a clear failure when `gh` is absent, and routing through `bin/spine.mjs` help/dispatch. Contract verification passed (machine verifier and independent runs: typecheck, 8/8 `spine-issue` tests, full suite 1268/1268, coverage ≥77%). Completion criteria are met—idle-repo draft works without batch context, and `--create` defaults off with an explicit error when `gh` is missing.

`src/cli/issue-draft.mjs` (SP-395 dependency) was added outside File Scope but is documented in STATUS as a required in-worktree dependency; this is acceptable and does not block delivery. `.DONE` marker is present; issue #60 was not closed per Do NOT list.

```json
{"verdict":"PASS","feedback":"Implementation matches PROMPT contract: bin/spine-issue.mjs implements all flags and behaviors; bin/spine.mjs wires help and dispatch; tests cover idle-repo draft, --json shape, --create gh guard, and spine.mjs routing. Machine contract verifier PASS (88.31% line coverage). Independent verification: typecheck OK, spine-issue tests 8/8, full suite 1268/1268. Minor note: SP-395 module landed in-worktree (documented discovery); consider merging SP-395 upstream to avoid duplicate ownership."}
```
