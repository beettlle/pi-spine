### Verdict: PASS

### Summary

SP-322 satisfies all PROMPT completion criteria: `src/batch/macro-phase.mjs` exports `MACRO_PHASES`, `deriveMacroPhase`, `macroPhaseLabel`, and `isMacroPhase` with a documented priority mapping table; `tests/batch/macro-phase.test.mjs` covers all eleven macro-phase enum values plus batch-state fixtures (`running-batch.json`, `needs-retry-batch.json`, `limbo-stale-20260531T165700.json`, `taskplane-executing.json`). Machine contract verifier passed; independent run of the contract test command confirms typecheck and 1013/1013 tests passing. Step 3 delivery is complete with `.DONE` and updated `STATUS.md` (commit `de30b8a`).

```json
{"verdict":"PASS","feedback":"All completion criteria met. deriveMacroPhase is a pure priority-ordered mapper from diagnosis, batchPhase, gateRecord, postMergeLimbo, and journalEvents to the eleven-value macro-phase enum. Tests assert every enum value is reachable and integrate reconcileBatch fixtures. Changes stay within file scope; diagnosis taxonomy and batch-state schema are untouched. Coverage gate met (87.09% >= 77%). Non-blocking: unstaged .spine/rules-manifest.json timestamp drift is outside SP-322 scope."}
```
