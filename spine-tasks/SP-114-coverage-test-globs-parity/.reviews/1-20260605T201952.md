## Plan Review: Step 1 — Align globs

### Verdict: APPROVE

### Summary

Step 1 correctly closes SP-108 Finding F1 with a minimal, two-file change: add the missing `tests/agents/*.test.mjs` entry to `TEST_GLOBS` in `scripts/coverage-policy.mjs` (matching the existing `package.json` `test` script order between `tests/batch/` and `tests/adoption/`), and strengthen `tests/coverage/policy.test.mjs` from a one-way inclusion check to ordered bidirectional parity via `assert.deepEqual(TEST_GLOBS, npmTestGlobs)`. That directly satisfies the mission and completion criterion that `coverage:check` runs the same test entrypoints as `npm test`, without touching the 77% threshold, CI wiring, or other audit findings. Step 2 appropriately owns the full `npm run coverage:check` green verification.

### Issues Found

None blocking for Step 1 plan approval.

### Missing Items

- **Stale test-count anchor** — PROMPT Step 1 checkbox cites "559 tests" from the SP-108 audit baseline; the suite is now **570** tests locally. Treat verification as **count parity** (`npm test` vs `coverage:check` both report the same `ℹ tests N`), not a hardcoded number.
- **Step 1 commit boundary** — Limit the Step 1 commit to `scripts/coverage-policy.mjs` and `tests/coverage/policy.test.mjs` per File Scope. `STATUS.md` updates and this review artifact are workflow bookkeeping, not product changes.
- **Step 1 / STATUS closure** — After this review lands, record `.reviews/1-20260605T201952.md` under Step 1 in `STATUS.md` before marking Step 1 complete and proceeding to Step 2 verification.

### Suggestions

- **Parity test shape** — `extractTestGlobsFromScript` + `assert.deepEqual` is the right enforcement: it covers both directions and preserves glob order, which a pair of subset checks would miss. Keep `TEST_GLOBS` comment that it must stay aligned with `package.json`.
- **Plan-before-implementation ordering** — Uncommitted worktree changes for the two scoped files already exist before this plan review (same pattern as prior tasks). Going forward, land the Step 1 commit after plan APPROVE; if already implemented, limit the commit to the two artifact files only.
- **Agents coverage impact** — `tests/agents/template-drift.test.mjs` contributes four subtests; adding the glob closes the audit's 555-vs-559 gap. No new tests are required beyond the strengthened parity guard.
- **Step 2 handoff** — Step 2 should confirm `npm run coverage:check` passes at full count and still meets the 77% gate; do not expand Step 1 into CI or template work.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-114",
  "feedback": "Step 1 plan: add tests/agents/*.test.mjs to TEST_GLOBS in coverage-policy.mjs (match package.json order); replace one-way policy.test.mjs drift check with ordered deepEqual parity against package.json test script globs. Verify npm test and coverage:check report the same test count (parity, not hardcoded 559). Step 1 commit = two scoped files only; record plan review in STATUS; Step 2 owns full coverage:check green run."
}
```
