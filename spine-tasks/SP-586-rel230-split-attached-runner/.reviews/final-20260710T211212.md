### Verdict: PASS

### Summary

SP-586 first-half extract is complete and within File Scope: `attached-runner-promote.mjs` (370 LOC ≤500) holds promote/exit paths; `attached-runner.mjs` (304 LOC) retains reconcile/lock helpers and re-exports promote APIs. Contract checks and `node --test tests/batch/attached-batch-exit.test.mjs` both pass (4/4); `bin/spine-cli/verify.mjs` was not touched; `.DONE` and STATUS completion criteria are satisfied with SP-604 deferred as intended.

```json
{"verdict":"PASS","feedback":"First-half extract meets PROMPT contract: promote module ≤500 LOC with shim re-exports, machine verifier PASS, attached-batch-exit tests 4/4, no verify.mjs edits, .DONE present. Second half correctly deferred to SP-604. No blocking issues."}
```
