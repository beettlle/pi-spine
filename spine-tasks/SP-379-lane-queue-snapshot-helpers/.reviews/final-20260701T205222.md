### Verdict: PASS

### Summary

SP-379 meets all PROMPT completion criteria for issue #58 Tier 1a. Commit `6a6ed65` adds `computeRunningTaskIdForLane` and `computeQueuedTaskIdsForLane`, wires `runningTaskId` / `queuedTaskIds` into `buildLaneRows`, and preserves deprecated `activeTaskIds` as `[runningTaskId, ...queuedTaskIds]`. Tests cover helper semantics, lane.taskIds ordering, and row projection; contract verification passed (typecheck + 1352 tests, 87.83% line coverage ≥ 77%). Step 3 documents the deprecation in STATUS Notes.

```json
{"verdict":"PASS","feedback":"Implementation matches mission: new helpers classify running vs queued tasks in the current wave, queued order follows lane.taskIds, buildLaneRows exposes additive fields, and activeTaskIds remains a backward-compatible union. Scope stayed within src/dashboard/snapshot.mjs and tests/dashboard/snapshot-lanes.test.mjs. Machine verifier and independent test run (npm run typecheck && SPINE_WORKER_STUB=1 npm test -- tests/dashboard/snapshot-lanes.test.mjs) both passed. No blocking issues."}
```
