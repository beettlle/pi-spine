### Verdict: PASS

### Summary
The implementation successfully adds the suite-directory discovery guard by introducing `SUITE_DIR_ALLOWLIST` in `scripts/coverage-policy.mjs` and adding the required tests in `tests/coverage/policy.test.mjs`. The guard ensures that new suite directories cannot be silently omitted from `TEST_GLOBS`, and an additional check correctly enforces that the allowlist stays free of stale entries. The scoped test command passes flawlessly.

```json
{"verdict":"PASS","feedback":"Excellent implementation. The allowlist is well-documented and the test cases cover both the main requirement and the cleanup of stale allowlisted entries."}
```