### Verdict: APPROVE

### Summary
`tests/config/cursor-rules/select-reviewer.test.mjs` covers every Step 2 PROMPT checkbox: worker execution rules excluded via default `neverInclude`, `spineClass: always` rules selected with empty scope, glob rules gated on `scopePaths` (including empty final scope), and `neverLoad` honored. Six focused unit tests follow the existing `select.test.mjs` patterns (`minimalManifest`, `DEFAULT_RULES_PROFILE`, `node:test`); all pass when run directly. No scope creep beyond the contracted test artifact; unstaged `.spine/rules-manifest.json` timestamp is unrelated noise.

```json
{"verdict":"APPROVE","feedback":"Reviewer selection tests satisfy Step 2 contract. Covered: enabled=false empty result; taskplane-worker-cursor and task-authoring excluded; always-class without scope; glob on src/foo.mjs vs empty final scope; standards dedup preserving glob source; neverLoad blocklist; profile.reviewer.maxRules cap. All 6 tests pass (node --test). Minor non-blocking notes: tests landed in the Step 1 commit; minimalManifest is duplicated from select.test.mjs like the worker suite—acceptable for consistency."}
```
