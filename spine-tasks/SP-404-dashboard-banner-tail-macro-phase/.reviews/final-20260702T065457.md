### Verdict: PASS

### Summary

SP-404 completes GitHub issue #68 Tier 1 banner-model work: `buildBannerModel` now delegates tail-state badge resolution to `resolveBannerBadge`, reusing SP-403's `isRunningWithoutActiveWorkers` instead of duplicating reconcile logic. During tail states the model emits `badge-finalizing` with macro-phase labels (`badgeLabel`, `subline`, `tailState`); active workers still get `badge-running`. Three ui-contract tests cover merging, gating, and active-worker paths. Machine contract checks pass; independent rerun confirms typecheck and 1396/1396 tests (including ui-contract), with coverage at 87.99% ≥ 77%.

Rendering of `badge-finalizing` in `dashboard.js`/CSS is correctly deferred to SP-406 per STATUS discoveries; scope stays within declared files.

```json
{"verdict":"PASS","feedback":"Banner tail-state model aligns with SP-403 diagnosis signals; tests and contract gates pass. No blocking scope creep or missing acceptance coverage. Safe to land."}
```
