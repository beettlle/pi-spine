### Verdict: APPROVE

### Summary

SP-352 delivers the scoped `src/planner/file-scope.mjs` module with wave-level overlap detection (`findWaveFileScopeOverlaps`), glob-aware pairwise checks via micromatch, and solid unit coverage in `tests/planner/file-scope-overlap.test.mjs`. Changes stay within File Scope; `.DONE` and STATUS reflect completed delivery. Independent verification: `npm run typecheck` and contract test run passed (1151/1151 tests, exit 0).

### Code review

**Scope compliance:** Commits from `7e72d5b` add only the two scoped source files, task `.DONE`, and STATUS updates. No planner wiring or unrelated refactors—appropriate for this extract-only split from SP-340.

**Required behavior:**
- Overlap detection across tasks in the same wave is implemented via pairwise comparison within each wave (`findWaveFileScopeOverlaps`).
- Prefix overlap (`pathsOverlap`), glob probes, and micromatch cross-checks cover parent/child paths (e.g. issue #31 `internal/safety/revert.go` vs `internal/safety/**`), exact file matches, and wildcard patterns.
- False-positive guard for dotted siblings (`src/foo` vs `src/foo.bar`) is tested.

**Tests:** Eleven focused cases cover normalization, prefix logic, glob edge cases, wave isolation, single-task waves, and the issue #31 scenario. Meets contract artifact requirement.

**Dependencies:** `micromatch` is an existing project dependency; usage matches `src/config/cursor-rules/match-globs.mjs` and `src/batch/contract-verify.mjs` patterns.

**Non-blocking notes (do not block):**
- Probe-expansion logic parallels `match-globs.mjs`; consolidation could be a follow-up outside this task's File Scope.
- Module is not yet imported by `src/planner/index.mjs`; integration appears deferred to sibling work, which aligns with the split scope.

**Working tree:** Uncommitted `.spine/rules-manifest.json` timestamp-only change is spine-runtime noise, not part of SP-352 delivery.

```json
{"verdict":"APPROVE","feedback":"Implementation and tests satisfy SP-352: wave-scoped overlap detection, glob edge cases, and delivery artifacts are in place within File Scope. Typecheck and full test run passed (1151/1151). Safe to proceed."}
```
