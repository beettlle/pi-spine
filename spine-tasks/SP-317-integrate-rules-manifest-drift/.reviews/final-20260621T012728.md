### Verdict: PASS

### Summary

SP-317 resolves GitHub #22: `resolveRulesManifestIntegrateDrift` in `src/batch/rules-manifest-drift.mjs` auto-restores manifest-only drift on `main` when the working tree fingerprint matches orch (worker-generated entries) or when drift is `generatedAt`-only, while refusing integrate when any other path is dirty or manifest content diverges from both HEAD and orch. Worker isolation via `SPINE_RULES_PROJECT_ROOT` in `worker-host.mjs` complements integrate prep. Five regression tests cover the #22 repro, end-to-end integrate, unrelated dirty blocking, mismatch blocking, and generatedAt-only resolution. Contract verification passes; delivery complete (operator-runbook §4, issue #22 closed, `.DONE` committed).

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met. Machine verifier PASS (testCommand, fileScopeMustChange on integrate.mjs, 87.52% coverage ≥77%, artifact present). Independent test run: typecheck + 1015 tests pass including integrate-rules-manifest-drift suite. Fail-closed preserved for non-manifest dirty files. Unstaged generatedAt churn in .spine/rules-manifest.json is harness noise, not a delivery gap."}
```
