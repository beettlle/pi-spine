### Verdict: PASS

### Summary

SP-541 meets all completion criteria and contract checks. `runContractTestCommand` refuses `npm test -- <path>` before subprocess spawn using the shared `NPM_TEST_DASH_DASH_RE` from SP-540, with a clear error message and `refusedBeforeSpawn` flag. Regression tests prove refuse-before-spawn (spawn-marker absent), allow scoped alternatives, and surface failure through `verifyContract`. Machine verifier and contract `testCommand` both pass (5/5).

```json
{"verdict":"PASS","feedback":"FR-STAB-02 runtime guard complete: isRefusedNpmTestDashDashCommand + pre-spawn refusal in runContractTestCommand, shared NPM_TEST_DASH_DASH_RE import without validate logic changes. tests/batch/contract-verify-npm-scope.test.mjs covers matcher, refuse-before-spawn, allowed commands, and verifyContract integration. .DONE and STATUS reflect completion; fileScopeMustChange satisfied on src/batch/contract-verify.mjs."}
```
