### Verdict: APPROVE

### Summary

Step 3 delivery requirements are satisfied: `docs/adoption/operator-runbook.md` documents `worker_done_missing` vs `worker_orphaned` in the orphan recovery tree, diagnosis quick map, and a dedicated SP-313 troubleshooting section; GitHub issue #18 is closed with a fix comment; and `.DONE` exists. Documentation aligns with the Step 1–2 implementation (`worker_done_missing` taxonomy, retry guidance, worker log citation, no orphan-resume paths).

Minor non-blocking note: the runbook lists `worker_done_missing` among diagnoses that default detached `resume` to `--wait-terminal`, but `RESUME_WAIT_TERMINAL_DIAGNOSES` in `src/batch/detached-start.mjs` does not include it—low operator impact since the dedicated section correctly warns against orphan `--force` resume. Optional follow-up: add `worker_done_missing` to `docs/EXECUTION-FLOW.md` taxonomy if that doc is kept in sync.

```json
{"verdict":"APPROVE","feedback":"Step 3 complete: operator-runbook updated with worker_done_missing recovery paths and diagnosis table rows, issue #18 closed, .DONE present. Docs match implementation (symptoms, log tail, spine batch retry, no orphan-resume). Non-blocking: runbook wait-terminal list includes worker_done_missing but detached-start.mjs RESUME_WAIT_TERMINAL_DIAGNOSES does not; EXECUTION-FLOW.md taxonomy optionally stale."}
```
