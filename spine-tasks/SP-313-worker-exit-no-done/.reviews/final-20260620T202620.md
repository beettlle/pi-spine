### Verdict: PASS

### Summary

SP-313 meets all completion criteria and contract checks. The implementation adds a distinct `worker_done_missing` diagnosis (prioritized before `needs_retry` in `deriveDiagnosis`), enriches headlines with worker log path and output tail for zero-scoped-file failures, and suggests `spine batch retry <taskId>` instead of orphan-resume paths. Regression tests reproduce the issue #18 journal pattern; contract test command passes (986/986), line coverage is 86.84% (≥77%), issue #18 is closed, operator-runbook is updated, and `.DONE` is present.

Minor non-blocking notes: `src/batch/diagnosis-worker-done-missing.mjs` is a reasonable helper extraction slightly outside the listed file scope; runbook mentions `worker_done_missing` in detached `--wait-terminal` defaults while `RESUME_WAIT_TERMINAL_DIAGNOSES` in `detached-start.mjs` may not include it; `docs/EXECUTION-FLOW.md` taxonomy was not updated (optional per PROMPT).

```json
{"verdict":"PASS","feedback":"All completion criteria satisfied: worker_done_missing taxonomy in diagnosis.mjs/reconcile.mjs, worker log citation and retry command, regression test for batch 20260620T014612 pattern, operator-runbook SP-313 section, issue #18 closed, .DONE present. Contract verifier PASS; independent verification: typecheck OK, 986/986 tests, 86.84% line coverage. Non-blocking: helper module outside strict file scope, optional EXECUTION-FLOW.md sync, runbook wait-terminal list vs detached-start.mjs."}
```
