### Verdict: PASS

### Summary

SP-351 delivers the stale-worktrees doctor check end-to-end: `buildStaleWorktreesDoctorCheck` lists `.worktrees/spine-*` dirs whose batch IDs do not match the in-progress batch, wires into `runDoctorChecks`, documents manual cleanup in the operator runbook, and ships six regression tests including a `runDoctorChecks` integration case. Machine contract verification passed (testCommand, fileScopeMustChange, 88.18% line coverage ≥ 77%, artifact present); issue #26 is closed and `.DONE` exists.

No blocking issues. `src/doctor/stale-worktrees.mjs` is outside the explicit File Scope list but follows the established doctor-module extraction pattern (`stale-path.mjs`, `worktree-health.mjs`) and is required by the scoped `run-doctor-checks.mjs` change.

```json
{"verdict":"PASS","feedback":"All three required behaviors are implemented and tested; contract gates pass; issue #26 closed; delivery artifacts (.DONE, STATUS) complete. Proceed."}
```
