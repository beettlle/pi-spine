### Verdict: PASS

### Summary

SP-517 is complete: the wave panel no longer shows `[completed]` when active diagnosis is `state_drift`, `engine_orphaned`, or `needs_retry` despite terminal-success artifacts on disk. `snapshot-waves.mjs` gates optimistic completion via `blocksOptimisticWaveCompletion`; `snapshot.mjs` passes reconciliation diagnosis into `buildWaveProgress`. Eight drift-truth tests cover builder and view-model paths. Machine contract verification passed; independent re-run of `testCommand` succeeded (typecheck, 8/8 tests, 88.77% line coverage ≥ 77%). GitHub #186 is closed; `.DONE` marker present.

```json
{"verdict":"PASS","feedback":"All completion criteria met. snapshot-waves.mjs adds DRIFT_BLOCKING_DIAGNOSES and blocksOptimisticWaveCompletion so resolveWaveStatus/buildWaveProgress withhold completed under state_drift, engine_orphaned, or needs_retry when batch endedAt is null; ended batches still allow completed. snapshot.mjs wires reconciliation.diagnosis (ui.mjs unchanged—acceptable integration point). tests/dashboard/wave-panel-drift-truth.test.mjs: 8 tests for all blocking diagnoses, view model, running positive case, and endedAt exception. Contract: typecheck OK, 8/8 tests, 88.77% coverage. Issue #186 CLOSED; .DONE present."}
```
