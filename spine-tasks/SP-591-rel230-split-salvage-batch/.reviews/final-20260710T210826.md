### Verdict: PASS

### Summary

SP-591 first-half extract is complete and within File Scope: `salvage-batch-list.mjs` (291 LOC) holds the list API; `salvage-batch.mjs` re-exports it and retains integrate for SP-605. Machine verifier PASS; contract `testCommand` re-run here 7/7; grandfather `bin/spine-cli/verify.mjs` untouched; `.DONE` present. Completion criteria met — no blocking issues.

```json
{"verdict":"PASS","feedback":"List extract and re-exports match PROMPT; tests 7/7; scope clean; integrate correctly deferred to SP-605. Ship."}
```
