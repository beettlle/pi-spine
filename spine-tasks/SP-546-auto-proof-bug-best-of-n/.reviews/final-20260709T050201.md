### Verdict: PASS

### Summary

SP-546 delivers the contracted fix for external `--project-root` runs in `scripts/best-of-n.mjs`. The worker resolves `@file` prompts from the invoke cwd, passes `--approve` for headless pi, sets `ROOT_WORKTREE_PATH`/`PI_WORKTREE_PATH` on spawn, and adds an empty-log watchdog so silent hangs surface in `bon-run.log`. Regression coverage in `tests/scripts/best-of-n-external-root.test.mjs` exercises dry-run, live mock spawn, and watchdog termination without live cursor/auto. Contract test command (7/7 pass), typecheck, `.DONE`, and GitHub issue #119 closure are all verified.

### Contract & completion

| Check | Status |
|-------|--------|
| `testCommand` (`node --test tests/scripts/best-of-n-external-root.test.mjs`) | PASS (7/7) |
| `fileScopeMustChange` (`scripts/best-of-n.mjs`) | PASS |
| `npm run typecheck` | PASS |
| File scope (`scripts/best-of-n.mjs`, `tests/scripts/best-of-n-external-root.test.mjs`) | PASS |
| Completion criteria (no silent hang; tests pass) | PASS |
| Step 4 delivery (`.DONE`, close #119) | PASS |
| Machine verifier | PASS |

### Notes (non-blocking)

- Commits use `feat(SP-546)` rather than the PROMPT-suggested `fix(SP-546): best-of-n external project-root output`; semantics are correct.
- Exported helpers (`resolvePromptArg`, `buildPiSpawnEnv`, `parseEmptyLogTimeoutMs`) are appropriately tested and support the fix without scope creep.

```json
{"verdict":"PASS","feedback":"All PROMPT completion criteria met. External project-root spawn fixes (@file resolution from invoke cwd, --approve, env vars, empty-log watchdog) are implemented and covered by 7 passing regression tests. Contract verifier passed; typecheck passed; .DONE present; issue #119 closed."}
```
