## Plan Review: Smoke task packet

### Verdict: APPROVE

### Summary

Step 1 correctly delivers a minimal **AD-002-real-pi-smoke** adoption fixture packet (Review Level 1, single implementation step, `REAL-PI-SMOKE.txt` artifact) plus `dependencies.json`, fixture `README.md`, and bootstrap checklist cross-reference — without pulling Step 2 real-pi batch execution, `docs/adoption/real-pi-e2e.md`, or Step 3 worker-template work into this boundary. Folder naming matches `TASK_FOLDER_RE` (`AD-002` task id from `AD-002-real-pi-smoke`), and the packet is intentionally distinct from **AD-001** (stub, Review Level 0, `DONE.txt`) so Step 2 can prove live `pi` + plan review + `spine_report_progress` on a separate task id. Deferring checked-in `.spine` config remains correct (init on copy).

### Issues Found

None blocking for Step 1 plan approval.

### Missing Items

- **`.DONE` contract in AD-002** — Mission and engine semantics require `taskplane-tasks/AD-002-real-pi-smoke/.DONE` for batch completion (`worker-host.mjs` / real `pi` does not auto-stub it). Add an explicit completion criterion and final step checkbox (create `.DONE` after plan review APPROVE and `spine_report_progress`). Conventionally `.DONE` sits outside narrow File Scope like AD-001, but it must not be omitted from completion criteria or a real-pi run will hang at merge.
- **Reviewer tool ordering in AD-002 Step 1** — Add a checkbox to call `spine_review_step` (plan) after writing `REAL-PI-SMOKE.txt` and updating `STATUS.md`, before `.DONE`. Environment already names the tools; the step list should mirror worker checkpoint order for the E2E under test.
- **bootstrap-checklist copy** — Table row for `dependencies.json` still says "Single-task dependency graph" after AD-002 was added; update to "Task dependency graph" (match fixture `README.md`).
- **Step 1 closure on TP-048** — After this review lands, mark TP-048 Step 1 complete in `STATUS.md` (plan review satisfied); keep Step 2 scoped to `SPINE_WORKER_STUB=0` batch on a fixture copy with batch id **`AD-002`**, not the folder slug.

### Suggestions

- **Evidence hook** — In Step 2 runbook (`real-pi-e2e.md`), note that journal evidence should include `task.step_completed` from `spine_report_progress` and `.reviews/1-*.md` from AD-002's plan review (validates reviewer path for Review Level 1).
- **Parallel tasks** — `dependencies.json` keeps AD-001 and AD-002 independent (`[]`); stub CI (`fixture-batch.test.mjs`) can continue targeting AD-001 only; no default npm test change per PROMPT Do NOT.
- **Timestamp format** — Specify UTC `YYYY-MM-DDTHH:mm:ssZ` (or full ISO-8601) in AD-002 Step 1 checkbox so evidence is machine-parseable in the runbook.
- **Forward scope** — Step 3 worker-template verification and `npm run typecheck && SPINE_WORKER_STUB=1 npm test` remain out of Step 1; do not edit `templates/agents/worker.md` until Step 3.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "TP-048",
  "feedback": "Step 1 plan delivers AD-002-real-pi-smoke (Review Level 1, one step, REAL-PI-SMOKE.txt) with deps/README/bootstrap updates and no Step 2/3 scope creep. Before real-pi batch: amend AD-002 PROMPT with explicit .DONE + spine_review_step ordering in checkboxes/completion criteria, and fix bootstrap-checklist 'Single-task' wording. Proceed to Step 2 fixture copy batch with AD-002 and SPINE_WORKER_STUB=0."
}
```
