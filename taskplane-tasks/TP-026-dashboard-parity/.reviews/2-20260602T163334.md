## Plan Review: Step 2 — Slash + action chips + docs

### Verdict: APPROVE

### Summary

The Step 2 plan correctly closes the operator-facing slice of GAP-UX-03: copyable diagnosis action chips (primary + `alternatives[]`, no HTTP mutations), `/spine-dashboard` as a detached `spine dashboard` launcher with operator instructions, and documentation updates (README dashboard section with `dashboard.port` default 8109, gap list, CONTEXT Phase 5). Centralizing chip construction in `view.mjs` (`buildActionChips`, `alternativeActionLabel`) and rendering in `dashboard.js` matches TP-025 patterns and keeps §16.1 labels testable via `ui-contract.test.mjs`. Step 1 parity work is already committed separately (`parity.test.mjs`); Step 2 should commit only slash, UI, and docs files per File Scope.

### Issues Found

None blocking.

### Missing Items

- **Config-aware port in `/spine-dashboard` notify** — The handler parses `--port` or defaults notification text to **8109** while `runSpineDashboardDetached` forwards args to `spine dashboard`, which uses `resolveDashboardPort()` from config. If `dashboard.port` is non-default and the operator omits `--port`, the spawned server and the notify URL can disagree. Plan should call `loadSpineConfig` / reuse `resolveDashboardPort(projectRoot, cliPort)` for the displayed URL (or avoid stating a numeric port when unknown).
- **PRD §15.1 slash table** — `/spine-dashboard` is in PROMPT/File Scope but not yet in `docs/PRD.md` §15.1; optional follow-up outside this step's Must Update list.

### Suggestions

- **Commit scope** — Keep Step 2 commit to `extensions/spine/slash-commands.ts`, `src/dashboard/public/*`, `src/dashboard/view.mjs`, `tests/dashboard/ui-contract.test.mjs`, `README.md`, `docs/compatibility/taskplane-gap-list.md`, `taskplane-tasks/CONTEXT.md`. Do not fold Step 1 parity changes into the same commit.
- **Example config** — `templates/spine-config.json` already documents `dashboard.port: 8109`; README addition satisfies PROMPT item 4 without `spine init` code changes.
- **NFR-OBS-02** — README note on the **2s** SSE poll interval is sufficient alongside Step 1's `DEFAULT_DASHBOARD_POLL_MS` assertion.
- **Slash tests** — Registration is covered by `SPINE_SLASH_COMMAND_NAMES`; optional smoke test for `spine-dashboard` handler notify text after port fix.
- **Chip UX** — Clipboard copy with brief "copied" affordance and `role="group"` on `#banner-actions` align with "operator runs CLI" and accessibility basics.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "TP-026",
  "feedback": "Plan covers action chips (view model + copy-only UI), /spine-dashboard detached spawn, README/gap list/CONTEXT closure, and ui-contract tests without HTTP mutations. Resolve dashboard.port in slash notify URL before implementation; keep Step 2 commit scoped to slash/UI/docs."
}
```
