## Plan Review: Step 1 — Parity regression tests

### Verdict: APPROVE

### Summary

The Step 1 plan correctly targets NFR-OBS-04 / GAP-UX-03 by locking diagnosis contract fields between `reconcileBatch()` and `buildDashboardSnapshot()` across the PROMPT fixture matrix. A shared `assertDiagnosisParity` helper, reuse of `tests/fixtures/batch-state/`, and git-backed scenarios for `needs_integrate` / `completed` / `completed_manual` are the right shape. Because `buildDashboardSnapshot()` delegates diagnosis to `reconcileBatch({ verbose: true })` and `spine status --json` serializes the same reconcile result, direct reconcile-vs-snapshot assertions satisfy the "CLI JSON === dashboard snapshot" checkbox without a separate subprocess layer.

### Issues Found

None blocking.

### Missing Items

- **`needs_merge` / `paused` fixtures** — Not in the PROMPT Step 1 matrix; optional follow-up if a cheap fixture exists, but not required for this step's stated outcomes.
- **Explicit `spine status --json` subprocess test** — Redundant with reconcile parity given current architecture; only add if you want literal CLI wiring regression separate from module contract.

### Suggestions

- **Align `verbose` on both paths** — Keep `reconcileBatch({ verbose: true })` in the helper; `buildDashboardSnapshot` already uses the same flag, so parity matches production dashboard behavior.
- **NFR-OBS-02 poll constant** — The `DEFAULT_DASHBOARD_POLL_MS === 2000` test fits Step 1 but is technically a server concern; acceptable here; could live in `server.test.mjs` if you prefer stricter step scoping.
- **Overlap with `snapshot.test.mjs`** — Idle parity exists there; dedicated `parity.test.mjs` is still worthwhile as the regression suite named in PROMPT File Scope.
- **Commit scope** — Step 1 commit should include only `tests/dashboard/parity.test.mjs` (and any fixture helpers if extracted); defer gap-list, README, slash, and UI chip changes to Step 2 per File Scope.

```json
{
  "verdict": "APPROVE",
  "feedback": "Plan correctly implements NFR-OBS-04 via assertDiagnosisParity across idle, running, needs_retry, needs_integrate, completed, limbo, and git-backed integrate paths; reconcile-vs-snapshot is sufficient for CLI JSON parity. Proceed with parity.test.mjs; keep Step 1 commit test-only."
}
```
