## Plan Review: Step 1 — Show implementation

### Verdict: APPROVE

### Summary

Step 1 correctly delivers FR-CFG-03's read-only CLI foundation: a pure `formatSettingsShow(config, { path?, json? })` in `src/cli/settings-show.mjs` driven by TP-032's `listEditableFields()` / `parseSettingPath()`, a thin `bin/spine-settings.mjs` that loads config via `loadSpineConfig` and wires `spine settings show [path] [--json]`, and integration tests with temp `.spine/spine-config.json` fixtures. The split matches `spine deps` (logic in `src/cli/`, entry in `bin/`, router in `bin/spine.mjs`). JSON shapes `{ fields: [{ path, label, value, type }] }` and `{ path, value }`, plus missing-config exit 1 with `suggestedCommand: spine init`, align with PROMPT deliverables. Scope stays read-only; no set/persist (TP-034) or slash UI (TP-035).

### Issues Found

None blocking.

### Missing Items

- **`bin/spine.mjs` in Step 1 checklist** — File Scope and "Must Update" require `cmdSettings` + help examples; add an explicit bullet so Step 2 verification does not discover a forgotten router hook.
- **JSON error coverage in tests** — Plan lists missing-config human error; add one test that `--json` on missing config emits `{ error, suggestedCommand }` and unknown-path JSON emits `{ error }` (mirrors `formatSettingsShow` branches).
- **Other `loadSpineConfig` failures** — Beyond `CONFIG_MISSING`, parse/validation errors return different `suggestedCommand` values (`spine init --force`, etc.). Plan need not test every code path, but implementation should forward `configResult.error.suggestedCommand` for all error cases, not only missing file.

### Suggestions

- **Module layout** — Keep `getValueAtPath` / `buildSettingsShowFields` as testable helpers; only `formatSettingsShow` and CLI runners need exporting. Human formatting (`(not set)`, `(empty)`, boolean strings) belongs in the formatter, not the registry.
- **Return contract** — Use `{ exitCode, output, error?, suggestedCommand? }` consistently from `formatSettingsShow` and `runSpineSettingsShow` so `cmdSettings` stays a thin stdout/exit wrapper (same shape as other spine subcommands).
- **Argv parsing** — Treat `--json` as a flag; first non-flag arg is the setting path. Reject multiple path args with a clear error.
- **Subcommand surface** — PROMPT specifies `spine settings show` only; avoid documenting bare `spine settings` as supported unless intentionally aliased to `show`.
- **Test registration** — `tests/spine-settings-show.test.mjs` is already covered by `tests/*.test.mjs`; no `package.json` change required.
- **Commit scope** — Step 1 commit should include only TP-033 files (`src/cli/settings-show.mjs`, `bin/spine-settings.mjs`, `bin/spine.mjs` help/router, `tests/spine-settings-show.test.mjs`); do not bundle unrelated lane work.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan correctly implements read-only spine settings show via formatSettingsShow + TP-032 registry, loadSpineConfig CLI wiring, and temp-fixture tests. Add explicit bin/spine.mjs checklist item and JSON error tests; forward all loadSpineConfig suggestedCommand values. Proceed without set/slash scope."
}
```
