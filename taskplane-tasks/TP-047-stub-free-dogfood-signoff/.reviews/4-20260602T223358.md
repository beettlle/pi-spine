## Plan Review: Step 4 — Verification

### Verdict: APPROVE

### Summary

Step 4 correctly closes TP-047 with the PROMPT-mandated stub CI regression gate: run `npm run typecheck && SPINE_WORKER_STUB=1 npm test` from the pi-spine repo root (lane worktree is equivalent), record the pass count in `taskplane-tasks/TP-047-stub-free-dogfood-signoff/STATUS.md`, and finalize the **Automated regression (stub CI path)** section in [`docs/compatibility/stub-free-dogfood-report.md`](../../docs/compatibility/stub-free-dogfood-report.md) with command, date, and outcome. No code changes, no stub-free CI policy change, and no re-run of manual `SPINE_WORKER_STUB=0` dogfood are required — Step 2 already signed the real-pi path. Land-loop checklist rows 6–8 remain **deferred** post-batch; that does not block Step 4 or completion criteria (“Test suite green” + written stub-free sign-off).

Independent verification in this worktree: **321/321 pass** (~67s) under default parallel `npm test` with `SPINE_WORKER_STUB=1`, matching the worker’s proposed count and superseding the earlier 320/321 note tied to `env-overrides.test.mjs` parallel contention (outside TP-047 file scope; no flake fix required for sign-off).

### Issues Found

None blocking for Step 4 plan approval.

### Missing Items

- **Plan-before-implementation discipline** — `STATUS.md` and the stub-free report regression block already show Step 4 complete / 321/321 before this review. Going forward, land Step 4 results in a dedicated Step 4 commit after plan review; do not expand scope into `phase6-dogfood-report.md` test-count refresh (196 baseline) unless explicitly requested — out of File Scope.
- **Step 4 commit boundary** — Commit should include only `taskplane-tasks/TP-047-stub-free-dogfood-signoff/STATUS.md` and `docs/compatibility/stub-free-dogfood-report.md` (automated regression subsection). Exclude `scripts/stub-free-dogfood.sh`, `phase6-dogfood-report.md`, and `tests/worker-tools/*` unless a regression failure forces a fix (not expected).
- **Completion checklist** — After Step 4 commit, tick PROMPT Step 4 checkbox and confirm Completion Criteria: signed stub-free report, Phase 6 manual section updated (Steps 1–3), test suite green (Step 4).

### Suggestions

- **Record evidence in STATUS** — One line: command, pass/fail ratio, duration optional, worktree path; mirrors Step 3 flake-triage note style.
- **Report wording** — Keep regression note concise (321/321, parallel default runner, worker-tools stable); drop stale 320/321 or `--test-concurrency=1` workaround language unless a re-run fails.
- **Do NOT** — No `SPINE_WORKER_STUB=0` in default `npm test` or CI; no new tests; no land-loop checkbox updates in Step 4 (operator post-batch).
- **Post-TP-047** — Operator land loop (gate approve → integrate → complete) updates stub-free rows 6–8 and Phase 6 items 5–6; track outside this step unless batch merge completes during the same session.

```json
{
  "verdict": "APPROVE",
  "step": 4,
  "taskId": "TP-047",
  "feedback": "Step 4 plan: npm run typecheck && SPINE_WORKER_STUB=1 npm test from repo root, record 321/321 in STATUS, finalize stub-free-dogfood-report.md automated regression section; no code/CI/stub-free-rerun scope. Commit STATUS + report regression only after plan review. Land loop 6–8 stays deferred. Proceed with Step 4 commit and PROMPT completion checkboxes."
}
```
