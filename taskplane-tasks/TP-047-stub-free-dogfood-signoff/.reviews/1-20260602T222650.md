## Plan Review: Step 1 — Prep script + checklist template

### Verdict: APPROVE

### Summary

Step 1 correctly scopes operator-facing stub-free infrastructure without pulling Step 2 manual execution, Step 3 `phase6-dogfood-report.md` cross-links, or flaky-test fixes into this boundary. The plan delivers `scripts/stub-free-dogfood.sh` as a guided bash runner (env preflight → `spine preflight` → `spine plan pending --json` → `spine status --diagnose` → optional `SPINE_WORKER_STUB=0 spine batch start <task>` with post-batch land-loop instructions) and `docs/compatibility/stub-free-dogfood-report.md` as the written sign-off artifact. Environment checks align with `worker-host.mjs` / `bin/spine-worker-runner.mjs`: reject `SPINE_WORKER_STUB=1|true`, require `pi` on PATH, require `.spine/spine-config.json`, and use `SPINE_BIN` defaulting to `node bin/spine.mjs` for worktree runs. The nine-row checklist (split gate/integrate/complete vs Phase 6’s combined bullets) matches the Phase 6 manual matrix and is printable via `--checklist-only`.

### Issues Found

None blocking for Step 1 plan approval.

### Missing Items

- **Step boundary in commits** — Current worktree also edits `phase6-dogfood-report.md` (checked items, deferral strikethrough) and fills `stub-free-dogfood-report.md` with batch `20260602T221506` results and sign-off. Those belong to Steps 2–3. Step 1 commit should be `scripts/stub-free-dogfood.sh` plus a **template-only** report (placeholders for date/commit/operator/batch; checklist rows `pending` / blank Notes; no sign-off line).
- **Report template vs execution** — Step 1 checkpoint is “pass/fail per manual item” as structure, not recorded results. Defer populated rows, “Real-pi worker evidence,” and automated regression notes to Step 2; defer Phase 6 checkbox updates to Step 3.
- **Optional `spine doctor` in script** — Report template includes a doctor row; consider a non-fatal `$SPINE doctor` in the script’s Environment section (mirror stub-free report Environment table) so operators catch stale PATH before batch start.
- **Step 1 plan in STATUS** — Add a short Step 1 subsection (script flags, checklist rows, commit file list) before marking the step done; aids Step 2 operators and code review.

### Suggestions

- **Default run is safe** — Without `--batch`, preflight failure warns but does not exit (lane worktree tolerance); `--batch` hard-fails on preflight — document this in script header comments.
- **Batch scope** — Step 2 may use `TP-047` (self-dogfood) or optional `AD-001` from TP-044; script’s `--batch <task-id>` already supports either.
- **Land loop** — Keep gate approve / integrate / complete as operator-printed steps after detached batch (not scripted) — matches PROMPT “guided manual run” and avoids integrate during active batch.
- **Do NOT** — No stub-free CI or `npm test` changes in Step 1; no worker-tools flake work until Step 3/4 per File Scope.
- **README pointer** — Optional one-line link under README stub section in Step 1 or defer to Step 3 with phase6 cross-link.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "TP-047",
  "feedback": "Step 1 plan delivers stub-free-dogfood.sh (pi + unset-stub checks, preflight/plan/status, optional SPINE_WORKER_STUB=0 batch start, checklist-only mode) and stub-free-dogfood-report.md template aligned with Phase 6 manual items, without Step 2 execution or Step 3 phase6/flake scope. Split commits: Step 1 = script + template report only; move filled report and phase6 checkbox updates to later steps. Proceed to Step 2 manual dogfood after plan review lands."
}
```
