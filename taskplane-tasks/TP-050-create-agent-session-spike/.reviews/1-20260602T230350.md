## Plan Review: Step 1 — Research + spike doc

### Verdict: APPROVE

### Summary

Step 1 correctly scopes research-only work before implementation: read `@earendil-works/pi-coding-agent` `createAgentSession` / `AgentSession` APIs (SDK export, not `ExtensionAPI`), compare against the existing subprocess path in `worker-host.mjs` / `bin/spine-worker-runner.mjs`, and deliver `docs/adoption/create-agent-session-spike.md` with dual mermaid sequence diagrams, API surface table (0.78.0), pros/cons, conditional **GO**, blockers B1–B5, and issue template. That satisfies PROMPT Step 1 checkboxes and mission deliverable #1 without touching `worker-host` or config in the Step 1 commit (`c2ee2d5` — spike doc + STATUS only). The doc’s subprocess default + `SPINE_WORKER_STUB` forcing stub/subprocess preserves TP-048’s validated path and CI constraints from the Do NOT list.

### Issues Found

None blocking.

### Missing Items

- **Step 1 plan in STATUS** — No “Step 1 plan” subsection documenting research sources and doc outline (pattern from TP-032/TP-033 reviews). Add before closing Step 1 in future tasks; not required to redo the spike doc.
- **Explicit import path in spike doc** — Table lists APIs but not the canonical import (`import { createAgentSession } from "@earendil-works/pi-coding-agent"`). Optional one-line addition for Step 2 implementers.

### Suggestions

- **Completion signal** — Spike’s agentSession diagram shows `agent_end`; implementation (Steps 3–4) should treat `.DONE` polling in `worker-host` as the authoritative batch completion signal, with `subscribe` / `isStreaming` as optional progress hints only.
- **Forward-looking sections** — “Config flag” and “Prototype scope” in the spike doc are appropriate design notes for Steps 2–3; keep those steps in separate commits and run their own plan/code reviews against PROMPT File Scope.
- **Peer dependency** — Step 2+ should wire `package.json` peer/range alignment with spike version **0.78.0** and PRD §5.2 doctor pin (blocker B5).
- **Reviewer parity** — B1 (`review.mjs` still `pi -p`) is correctly called out; do not flip default `workerBackend` until reviewer backend is addressed or explicitly scoped out of v1.0 pilot.
- **Uncommitted work** — Working tree already contains Step 2+ scaffolding (`worker-backend.mjs`, `agent-session-worker.mjs`, `worker-host` branch). Do not fold into Step 1; commit per PROMPT step boundaries.

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 1 plan delivers research-only spike doc with subprocess vs agentSession sequence diagrams, pi-coding-agent 0.78.0 API table, conditional GO, and blockers; Step 1 commit correctly scoped to docs/STATUS only. Add STATUS plan subsection in future; keep .DONE poll primary for completion; separate commits for flag/scaffold/prototype."
}
```
