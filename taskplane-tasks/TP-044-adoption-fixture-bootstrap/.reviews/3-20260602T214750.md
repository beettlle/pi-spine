# Code Review — Step 3: Integration test + script

**Task:** TP-044 — Adoption fixture repo + bootstrap checklist  
**Baseline:** `669086c..06be366` (`feat(TP-044): add adoption fixture batch test and smoke script`)  
**Files reviewed:** `tests/adoption/fixture-batch.test.mjs`, `scripts/adoption-smoke.sh`, `package.json`, `STATUS.md`

### Verdict: APPROVE

### Summary

Step 3 delivers a focused end-to-end adoption integration test and operator smoke script that match the PROMPT: copy the consumer fixture to a temp git repo, conditionally run `spine init`, plan AD-001 with stub workers, run an attached batch, and assert completion via `.DONE` plus batch state. The test and `./scripts/adoption-smoke.sh` both pass locally (~8.5s), and `package.json` correctly registers `tests/adoption/*.test.mjs` so CI will pick up the new suite under `SPINE_WORKER_STUB=1 npm test`.

The implementation aligns with existing batch integration patterns (CLI invocation via `execFileSync`, `--skip-preflight`, stub env) and correctly handles worktree-local `.DONE` markers via `findTaskDoneMarker`. No network or real pi dependency is introduced.

### Requirements coverage

| Requirement | Status |
|-------------|--------|
| Copy fixture to temp dir | ✅ `provisionAdoptionFixture()` |
| `spine init` skip if pre-inited | ✅ `ensureSpineInit()` checks `.spine/spine-config.json` |
| `SPINE_WORKER_STUB=1 spine plan` | ✅ |
| Stub batch start AD-001 | ✅ `batch start AD-001 --skip-preflight --attached` |
| Assert `.DONE` | ✅ with worktree fallback |
| `adoption-smoke.sh` for operators | ✅ typecheck + targeted test run |
| No network / real pi in CI | ✅ |
| Stay within file scope | ✅ |

### Strengths

- **Batch state assertions** (`phase: completed`, `succeededTasks: 1`) add confidence beyond filesystem markers alone.
- **Git seeding** in the temp copy (init, identity, initial commit, `main` branch) mirrors real consumer bootstrap and satisfies preflight expectations.
- **Cleanup** in `finally` with env restoration and retried `rm` matches other integration fixtures.
- **Smoke script** is appropriately narrow (typecheck + single test) rather than re-running the full suite.

### Non-blocking suggestions (optional polish)

1. **Reuse `destroyGitRepo`** from `tests/helpers/git-fixture.mjs` for teardown instead of inline `rm` — keeps fixture lifecycle helpers consistent.
2. **`SPINE_WORKER_STUB` is set three ways** (process env, per-`runSpine` extraEnv, script export). Setting it once on `process.env` before subprocess calls would be enough; current redundancy is harmless.
3. **`findTaskDoneMarker` scans only `lane-1`** — correct for this single-lane smoke task; document or generalize if multi-lane adoption tests are added later.

### Verification performed

```text
SPINE_WORKER_STUB=1 node --test tests/adoption/fixture-batch.test.mjs  → pass
./scripts/adoption-smoke.sh                                              → pass
```

```json
{"verdict":"APPROVE","feedback":"Step 3 integration test and adoption-smoke.sh meet PROMPT requirements: temp fixture copy, conditional init, stub plan/batch for AD-001, .DONE and batch-state assertions, CI wiring in package.json. Both test and script pass locally with no network dependency. Minor optional polish: reuse destroyGitRepo helper and dedupe SPINE_WORKER_STUB env passing."}
```
