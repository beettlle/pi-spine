## Code Review: `spine run` alias + docs

### Verdict: APPROVE

### Summary

Step 3 delivers a thin, correct `spine run` entry point that prepends `start` and delegates to `runSpineBatch`, matching `cmdBatch` behavior for stdout, exit codes, and flag passthrough. README, gap list, slash-command help, and CONTEXT updates accurately document `pending` vs plan `all`, relaxed batch `all`, the `spine run` alias, and manual wave integrate. Integration tests in `tests/spine-run.test.mjs` confirm parity with `spine batch start` for `pending` and `all` scopes. Full suite passes: **145** tests, typecheck clean.

### What looks good

- **`cmdRun` design.** `args: ["start", ...args]` is the minimal correct alias; no duplicated batch logic.
- **Operator docs.** README clearly separates `spine plan all` (full backlog) from `spine batch start all` / `spine run` (pending-filtered), documents empty-pending failure, on-disk skip, and that mid-batch integrate remains manual.
- **UX surfaces.** `printHelp` lists `run`; slash-command descriptions and idle `/spine` guidance mention `pending` and `spine run pending`.
- **Gap closure.** `GAP-BATCH-PENDING-01` marked closed with accurate mapping to FR-SCHED-06 / PRD §15.2.
- **Tests.** `spine run pending` JSON output matches `spine batch start pending`; `spine run all` respects pending filter.

### Non-blocking notes

- **`tests/spine-run.test.mjs` is untracked** in the worktree (`git status` shows `??`); it is not in `git diff` but is required for Step 3 verification. Include it in the Step 3 commit before marking the step done.
- **Help text alignment.** A few `printHelp` lines (`batch`, `run`, `review step`, examples) use one leading space instead of two—cosmetic only; optional cleanup.
- **`spine run` scope-only contract.** `spine run dismiss` would route to `batch start dismiss` (start wins in `parseBatchArgs`); PRD defines `run` as execute-batch only, so this is acceptable unless misuse is observed in automation.

### Verification

| Check | Result |
|-------|--------|
| `npm run typecheck` | pass |
| `npm test` | 145 pass (includes `spine run` tests) |
| Step 3 PROMPT items | alias, README, CONTEXT, gap list satisfied |

```json
{
  "verdict": "APPROVE",
  "feedback": "Step 3 implementation is correct and well-documented: cmdRun aliases batch start with flag passthrough, docs and slash help align with pending vs plan-all semantics, and spine-run tests pass. Stage tests/spine-run.test.mjs in the commit before closing the step."
}
```
