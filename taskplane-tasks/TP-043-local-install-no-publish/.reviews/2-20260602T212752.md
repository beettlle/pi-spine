## Plan Review: Step 2 — Doctor stale PATH check

### Verdict: APPROVE

### Summary

Step 2 correctly scopes doctor hardening to a pure module plus wiring, mirroring TP-028's advisory check pattern. The plan adds `src/doctor/stale-path.mjs` with `resolveSpineOnPath` (`which spine`), `readPackageVersion`, `readSpineCliVersion` (`node <path> --version`), and `buildStalePathDoctorCheck` comparing PATH `spine` against `bin/spine.mjs` via `realpath`, semver, and mtime. Wiring in `runDoctorChecks` after the package line, passing `packageRoot: PACKAGE_ROOT` and `runningSpinePath: __filename`, ensures `node bin/spine.mjs doctor` passes when PATH is unset or points at the running checkout. Checks are advisory only (`ok: true`, optional `warning`, `suggestedCommand` on missing/stale PATH); they are appended with `checks.push` rather than `record()` so warnings do not increment `issueCount`. Injectable `which` / `spawn` / `stat` / `realpath` defer mock scenarios to Step 3. Label **"spine on PATH (stale)"** and `cd ${packageRoot} && npm link` align with Step 1 `local-install.md` troubleshooting.

### Issues Found

None blocking.

### Missing Items

- **Step 2 commit boundary** — Step 2 commit should include only `src/doctor/stale-path.mjs` and `bin/spine.mjs`. Keep `tests/doctor/stale-path.test.mjs` in Step 3 per PROMPT File Scope.
- **Advisory semantics explicit in plan** — Document that stale/missing PATH must never set `ok: false` or fail doctor/preflight (same contract as TP-028 `lanes.maxParallel sizing`).
- **Step 1 follow-up** — Step 1 review noted stale-PATH doc text ahead of implementation; once Step 2 lands, confirm README and `local-install.md` references to **"spine on PATH (stale)"** match actual doctor output (no doc changes required in Step 2 commit unless labels drift).

### Suggestions

- **Missing PATH detail** — When `which spine` fails, warning detail should mention `npx spine` and `node bin/spine.mjs` (as Step 1 doc does), not only `npm link`.
- **Stat failure path** — Treat un-stat-able PATH binary as warning + `npm link` (already in implementation sketch); keep detail actionable.
- **Version parse fallback** — If `--version` fails, rely on mtime-only stale detection; do not treat `unknown` version alone as stale.
- **Step 3 test matrix** — Cover: PATH absent, PATH matches repo bin, version mismatch, mtime-only mismatch, and `readSpineCliVersion` against real `bin/spine.mjs`.
- **No README change in Step 2** — README already mentions stale PATH from Step 1; defer any doctor-section expansion to Step 3 verification unless output labels change.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "TP-043",
  "feedback": "Step 2 plan correctly adds src/doctor/stale-path.mjs (which/version/mtime vs bin/spine.mjs, realpath-aware, injectable deps) and wires buildStalePathDoctorCheck into runDoctorChecks as an advisory check with suggestedCommand npm link. Keep Step 2 commit to module + bin/spine.mjs; defer stale-path.test.mjs to Step 3. Proceed with implementation."
}
```
