## Plan Review: Step 1 — Startup banner + immediate stdout

### Verdict: APPROVE

### Summary

The Step 1 plan correctly fixes the root cause: `runSpineDashboard` currently builds operator text only after SIGINT, and `cmdDashboard` writes `result.output` once at return. Exporting a pure `formatDashboardStartupMessage`, calling `process.stdout.write` immediately after `listenDashboardServer`, and returning only a one-line shutdown payload preserves listen/shutdown behavior while matching Taskplane-style operator guidance. Adding `resolveDashboardPortWithSource` for `--port` vs `dashboard.port` vs default notes satisfies PROMPT requirements and sets up Step 2 slash alignment without changing HTTP/SSE.

### Issues Found

None blocking.

### Missing Items

- **Resolved listen port** — After `listenDashboardServer`, pass the returned `port` (from `server.address()`) into the startup formatter for the `Listen:` line. The browser/API lines already use `url` from listen; using the pre-listen `port` breaks the display when the OS assigns an ephemeral port (e.g. `--port 0` in tests shows `Listen: …:0` while `Open in browser` is correct).
- **`projectRoot` in formatter signature** — PROMPT lists it on `formatDashboardStartupMessage`; omit unless needed, or document as reserved for future paths.

### Suggestions

- **Commit scope** — Step 1 commit should be `bin/spine-dashboard.mjs` only. Defer `tests/dashboard/cli-startup.test.mjs`, `extensions/spine/slash-commands.ts`, and `README.md` to Steps 2 and 4 per PROMPT (working tree already contains them; split commits accordingly).
- **`formatDashboardNotifyMessage`** — Reasonable to define alongside the startup formatter in `spine-dashboard.mjs`, but wire it from `/spine-dashboard` in Step 2 so Step 1 stays CLI-focused.
- **Layout parity** — Blank line, title, indented fields, and `→` primary action match `spine status`; keep shutdown return as `\nDashboard stopped.\n` only (no duplicate banner via `cmdDashboard`).
- **Regression guard** — Step 2 should assert startup stdout precedes shutdown return; the mocked `process.stdout.write` + SIGINT pattern in the draft test file is appropriate.

```json
{
  "verdict": "APPROVE",
  "feedback": "Plan correctly moves the operator banner to immediate post-listen stdout via formatDashboardStartupMessage and a minimal shutdown return; resolveDashboardPortWithSource covers port provenance. Proceed with spine-dashboard.mjs only in Step 1; pass listen-resolved port to the Listen line; defer tests, slash notify, and README to later steps."
}
```
