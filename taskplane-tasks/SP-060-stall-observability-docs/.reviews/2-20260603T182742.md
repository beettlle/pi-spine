## Plan Review: Operator docs

### Verdict: REVISE

### Summary

Step 2 correctly closes PROMPT operator-doc outcomes and epic test plan item 4 (runbook-only): a **Stall diagnosis (5-minute path)** section in `docs/adoption/operator-runbook.md` aligned with Step 1's SAT-020 journal order (`lane.checkpoint_warning` → `lane.stall_killed` → `lane.salvage_inspection` → `task.failed`), a lane config table, `autoCommitOnStall` retry guidance cross-linked to §18.5, and links to `docs/features/stall-recovery-improvements-brief.md`. Surgical PRD §18.4 updates (checkpoint warning, `extendGraceOnFileScope`, `lane.stall_killed` / salvage sentences) are in scope and match shipped SP-056–058 behavior.

The worktree's current `docs/PRD.md` edit **truncates the file** (~1478 → ~1183 lines), deleting most of §18.5 and all following sections. That must be fixed before Step 2 lands. Keep Step 2 commits limited to runbook + PRD; defer gap-list rows and `CONTEXT.md` to Step 4 per PROMPT step boundaries.

### Issues Found

- **Blocking — PRD truncation** — Uncommitted `docs/PRD.md` removes §18.5 tail and §18.6+ (retry segment rules, abort, testing, phases, etc.). Restore from `HEAD`, then re-apply only the §18.4 paragraph additions (~3 sentences + expanded configurable keys). Verify line count stays ~1478 before commit.

### Missing Items

- **Step 2 commit boundary** — Land `docs/adoption/operator-runbook.md` and surgical `docs/PRD.md` only. Do not include `docs/compatibility/taskplane-gap-list.md`, `taskplane-tasks/CONTEXT.md`, dashboard files, or Step 1 test/fixture paths in the Step 2 commit (Step 3–4 per PROMPT).
- **Epic test plan item 4** — Document `autoCommitOnStall` retry in the runbook (PROMPT Step 2); no integration test required here while SP-059 remains staged. Note that `templates/spine-config.json` does not yet expose `autoCommitOnStall` — runbook should describe it as opt-in when SP-059 ships.
- **Optional runbook polish** — Mention `lane.stall_warning` (emitted immediately before kill) and `evidence/salvage-<taskId>.json` in step 3 for parity with PRD §18.4 and FR-STALL-03.6; not blocking.
- **STATUS closure** — After this review, record `.reviews/2-20260603T182742.md` in `STATUS.md` Step 2 before marking complete.

### Suggestions

- **Diagnosis flow** — Mirror Step 1 `stall-sat020-integration.test.mjs` event order in the runbook table (already correct); cite `spine status --diagnose` `journalHints` as the primary entry point.
- **PRD §18.5** — PROMPT "§18.4–18.5 alignment" means runbook cross-reference to atomic retry, not rewriting §18.5 body in Step 2.
- **Brief link** — Related-docs row + in-section link to `stall-recovery-improvements-brief.md` satisfies "Link SearchATon brief in `docs/features/`"; no new brief file needed.

```json
{
  "verdict": "REVISE",
  "feedback": "Step 2 plan: runbook 5-minute stall path (checkpoint_warning → stall_killed → salvage_inspection → task.failed → retry), config table, autoCommitOnStall retry prose, brief links; surgical PRD §18.4 only. Blocking: restore docs/PRD.md from HEAD if truncated (~1478 lines) before commit. Defer gap-list and CONTEXT to Step 4; defer dashboard to Step 3. Epic item 4 is runbook-only (no test). Proceed after PRD fix and scoped commit."
}
```
