## Plan Review: Worker template + verification

### Verdict: APPROVE

### Summary

Step 3 correctly closes TP-048 with **verification-only** work on `templates/agents/worker.md` plus the PROMPT Environment gate — no new smoke packet, runbook, or real-pi batch re-run. The plan audits that the worker template's `tools:` frontmatter and "Spine worker tools" table document `spine_review_step`, `spine_report_progress`, and `spine_request_gate`, with explicit preference over bash `spine review step` / `spine report progress`, satisfying mission deliverable #4 and Step 2's deferred boundary. Carrying the Step 2 review fix for `scripts/real-pi-adoption-e2e.sh` journal tail (`.spine/runtime/<batchId>/journal/events.jsonl` per `journal.mjs`) stays in scope as operator-script hygiene without touching default `npm test`. Stub CI regression via `npm run typecheck && SPINE_WORKER_STUB=1 npm test` matches PROMPT Do NOT (no real-pi in default test). `docs/compatibility/stub-free-dogfood-report.md` already cross-links `real-pi-e2e.md` from Step 2 — no further doc edit required in Step 3.

### Issues Found

None blocking for Step 3 plan approval.

### Missing Items

- **Script journal guard** — Plan should tail `.spine/runtime/*/journal/events.jsonl` without a broken `[[ -f ... ]]` test: unexpanded globs in `-f` never match (verified: guard fails while `tail` on the same glob works). Drop the `-f` guard or resolve batch id from `.spine/batch-state.json` before tailing.
- **Plan-before-implementation ordering** — Commit `5301414` landed Step 3 changes before this plan review (same pattern as Step 2). Going forward, Step 3 commit should follow plan APPROVE; if already implemented, limit the commit to script guard fix + STATUS/review closure.
- **Step 3 plan review closure** — After this review lands, record `.reviews/3-20260602T225010.md` in TP-048 `STATUS.md` Step 3 (replace the misplaced "Step 2 plan review" line under Step 3).
- **Task completion** — PROMPT completion criteria are satisfied by Steps 1–2; Step 3 should end with `taskplane-tasks/TP-048-real-pi-worker-e2e/.DONE` only after verification green and this review APPROVE (not before).

### Suggestions

- **Template audit outcome** — `templates/agents/worker.md` already lists all three spine tools in frontmatter, documents MCP vs bash preference, and ties checkpoint steps to `spine_review_step` / `spine_report_progress`; no edit required unless audit finds drift from `extensions/spine/worker-tools.ts` registration.
- **Reviewer template** — Out of Step 3 scope (PROMPT File Scope names `worker.md` only); real-pi E2E already exercised reviewer path via AD-002 plan review.
- **Suite record** — Record fresh pass count in `STATUS.md` after verification (321/321 confirmed 2026-06-02 on this worktree).
- **Completion gate** — Do not add real-pi to `package.json` test scripts; keep `./scripts/real-pi-adoption-e2e.sh` manual-only.

```json
{
  "verdict": "APPROVE",
  "step": 3,
  "taskId": "TP-048",
  "feedback": "Step 3 plan: verify templates/agents/worker.md documents spine_review_step, spine_report_progress, spine_request_gate (MCP over bash); fix real-pi-adoption-e2e.sh journal tail to events.jsonl without broken -f glob; run typecheck + SPINE_WORKER_STUB=1 npm test; record Step 3 plan review in STATUS; create .DONE when complete. No default npm test or stub-free report edits."
}
```
