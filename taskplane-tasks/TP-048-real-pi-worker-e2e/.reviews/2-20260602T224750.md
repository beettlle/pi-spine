## Plan Review: Real-pi batch execution

### Verdict: APPROVE

### Summary

Step 2 correctly scopes **consumer-layout real-pi validation** on a temp copy of `tests/fixtures/adoption-repo`, targeting task id **`AD-002`** (not the folder slug) with `SPINE_WORKER_STUB=0`, an attached batch, and evidence captured in **`docs/adoption/real-pi-e2e.md`**. The optional **`scripts/real-pi-adoption-e2e.sh`** mirrors `fixture-batch.test.mjs` provisioning (copy → git init → `spine init` → plan) and `stub-free-dogfood.sh` env guards (`pi` on PATH, reject stub), without wiring real-pi into default `npm test` per PROMPT Do NOT. The runbook covers model config, expected duration, failure recovery, and the evidence hooks from Step 1 review (`task.step_completed`, AD-002 plan review `.reviews/1-*.md`, `.DONE`, lane branch commit). Step 3 worker-template verification remains deferred; bootstrap-checklist and stub-free report cross-links stay within File Scope.

### Issues Found

None blocking for Step 2 plan approval.

### Missing Items

- **Script journal path** — `real-pi-adoption-e2e.sh` tails `.spine/journal.ndjson`, but the engine writes `.spine/runtime/<batchId>/journal/events.jsonl` (`journal.mjs`). The runbook documents the correct path; fix the script evidence tail (resolve batch id from batch state or glob `.spine/runtime/*/journal/events.jsonl`) before treating script output as authoritative.
- **Plan-before-implementation ordering** — Commit `ec785b4` landed Step 2 artifacts before this plan review. Going forward, Step 2 commit should follow plan APPROVE; if already implemented, limit the Step 2 commit to evidence/script refinements called out here.
- **Step 2 plan review closure** — After this review lands, record `.reviews/2-20260602T224750.md` in TP-048 `STATUS.md` Step 2 (mirror Step 1 pattern) before Step 3 verification commit.
- **Evidence baseline SHA** — Runbook correctly pins pi-spine commit `e85680a` (pre-Step-2 batch baseline). Refresh only if re-running batch on a newer checkout; do not conflate with Step 2 doc commit SHA.

### Suggestions

- **Operator flow** — Dry run (`./scripts/real-pi-adoption-e2e.sh`) validates provisioning without cost; full run uses `--batch --keep-tmp` so temp repo survives for journal and `.reviews/` inspection under `.worktrees/<batchId>/lane-1/`.
- **Parallel paths** — Keep stub CI on **AD-001** (`fixture-batch.test.mjs`, `adoption-smoke.sh`); real-pi script targets **AD-002** only — no default test change.
- **Success criteria met** — Batch `20260602T224546` shows Review Level 1 plan review APPROVE, `spine_report_progress` journal events, `.DONE`, and lane commit `75347ee` — satisfies PROMPT mission evidence without Step 3 template edits.
- **Step 3 boundary** — `templates/agents/worker.md` verification and `npm run typecheck && SPINE_WORKER_STUB=1 npm test` belong in Step 3 only; stub-free report cross-link in Step 2 is sufficient for adoption ↔ dogfood doc navigation.

```json
{
  "verdict": "APPROVE",
  "step": 2,
  "taskId": "TP-048",
  "feedback": "Step 2 plan: SPINE_WORKER_STUB=0 attached batch on adoption fixture copy for AD-002, evidence in real-pi-e2e.md (journal, .DONE, lane commit, plan review), optional real-pi-adoption-e2e.sh manual script, no default npm test. Fix script journal tail to events.jsonl path; record this plan review in STATUS. Proceed to Step 3 worker template verification."
}
```
