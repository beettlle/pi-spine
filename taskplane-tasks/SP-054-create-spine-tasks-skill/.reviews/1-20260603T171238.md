## Plan Review: Step 1 — Skill authoring

### Verdict: APPROVE

### Summary

Step 1 correctly scopes skill-only work under `skills/create-spine-tasks/`: a spine-native adaptation of `create-taskplane-task` with `{tasksRoot}` / `.spine/spine-config.json` configuration, PRD→task decomposition workflow, complexity scoring, `dependencies.json` + CONTEXT tracking, and launch via `spine plan` / `spine batch start` (not `/orch`). Reference templates (`prompt-template.md`, `context-template.md`) belong in Step 1; README and `bootstrap-checklist.md` are correctly deferred to Step 2. Local `pi install … -l` is documented and npm publish is not required, matching PROMPT constraints and SP-051/SP-053 boundaries (authoring skill only — no init-time PRD auto-generation).

### Issues Found

None blocking.

### Missing Items

- **De-Taskplane the prompt template** — `references/prompt-template.md` and the STATUS section still include `#### Segment: <repoId>` multi-repo guidance copied from Taskplane. This contradicts SKILL.md § "Unsupported (spine v1)" and PRD §13.2 (segment headers error). Strip segment blocks from both PROMPT and STATUS templates; keep monorepo-only step shapes.
- **PROMPT heading format** — Template uses `# Task: [PREFIX-###] - [Name]` (ASCII hyphen). PRD §13.4 / FR-TASK-01 require an em dash: `# Task: SP-042 — slug name`. Fix the template example and SKILL.md folder-name note to match the regex spine parses.
- **Agent naming** — Replace "task-worker agent" references in templates with spine worker (`.spine/agents/worker.md` / `spine_review_step` discipline).
- **CONTEXT template alignment** — `references/context-template.md` is a slim pointer; when SP-053 lands, cross-link or mirror `templates/tasks/CONTEXT.md` so manual refresh and `spine init` scaffold stay consistent. Until SP-053 merges, the fallback template is acceptable.
- **Step 1 commit boundary** — Commit only `skills/create-spine-tasks/**` and task `STATUS.md`. Do not fold README / `docs/adoption/bootstrap-checklist.md` into Step 1 (Step 2 deliverables).

### Suggestions

- **PRD decomposition** — Keep the dedicated "PRD → Task Decomposition" section (Steps A–C); it is the core mission delta vs single-task creation and is not present in the Taskplane skill.
- **Migrant repos** — Continue using configurable `paths.tasksRoot` + `SPINE_TASKS_ROOT` rather than hard-coding `spine-tasks/`; pi-spine dogfood still uses `taskplane-tasks/` until SP-055.
- **Review level guidance** — Preserve "do not default to Level 0" and per-step `spine_review_step` notes; aligns with pi-spine worker/reviewer contracts.
- **Definition of Ready checklist** — The closing checklist (CONTEXT increment, deps.json, launch commands) is useful; ensure template fixes above land before Step 2 doc links cite the skill as authoritative.
- **Verification** — Step 1 has no test gate; optional smoke: `pi install` loads `./skills/create-spine-tasks` via package.json `"skills": ["./skills"]`.

```json
{
  "verdict": "APPROVE",
  "step": 1,
  "taskId": "SP-054",
  "feedback": "Step 1 plan correctly delivers skills/create-spine-tasks/SKILL.md plus reference templates adapted from create-taskplane-task with spine-native paths, PRD decomposition, dependencies.json/CONTEXT tracking, spine batch launch commands, and local pi install (no npm publish). Defer README and bootstrap-checklist to Step 2. Before or during Step 1 implementation, strip Taskplane segment/polyrepo blocks from prompt-template.md, fix PROMPT heading to em dash per PRD §13.4, and use spine worker naming. Proceed to Step 1 commit, then Step 2 bootstrap doc link + example."
}
```
