# Taskplane runtime artifacts (machine-specific, do not commit)
.pi/batch-state.json
.pi/batch-history.json
.pi/lane-state-*
.pi/merge-result-*
.pi/merge-request-*
.pi/worker-conversation-*
.pi/orch-logs/
.pi/orch-abort-signal
.pi/settings.json
.pi/diagnostics/
.pi/mailbox/
.pi/runtime/
.pi/supervisor/
.pi/telemetry/
.pi/verification/
.pi/taskplane.json
.pi/agents/
.pi/loops/
.worktrees/
.taskplane-tasks/

# Ephemeral dirs from batch tests (legacy cwd mkdtemp leftovers — do not commit)
cp-signals-*/
salvage-scope-*/
cp-warn-*/
cp-journal-*/
hb-*/
hb-scope-*/
hb-step-*/
wtrp-*/
rg-*/
rp-*/
snap-*/

# pi-spine runtime artifacts
.pi/
.spine/runtime/
.spine/batch-state.json
.spine/batch-history.json
.spine/run-metrics.jsonl
.spine/reports/
.spine/handoff.md
.pi-smart-router/

# Pi project-local packages (if using pi install -l)
.pi/npm/

# Graphify rebuild output (post-commit hook writes here; ignore to avoid spine lane DirtyWorktree)
graphify-out/

# Stet runtime state (config.toml is committed; session/lock are per-worktree)
.review/lock
.review/session.json
.review/spine-stet-baseline.ref

# Coverage suite full TAP (written by scripts/run-coverage.mjs; uploaded on CI)
coverage-run.tap.log

# Dependencies
node_modules/
